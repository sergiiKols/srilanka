# 🔄 КАК ПЕРЕЗАПУСТИТЬ DEV SERVER

Если сервер упал или не запускается:

## Способ 1: Через BAT файл

Просто **двойной клик** на:
```
START_DEV_SERVER.bat
```

## Способ 2: Через командную строку

1. Откройте **cmd** (не PowerShell!)
   - Win+R → `cmd` → Enter

2. Перейдите в папку проекта:
   ```bash
   cd C:\Users\User\Desktop\sri
   ```

3. Остановите старые процессы:
   ```bash
   taskkill /F /IM node.exe
   ```

4. Запустите dev server:
   ```bash
   node node_modules\astro\astro.js dev
   ```

## Что должно появиться:

```
🚀  astro  v5.16.11 ready in XXXms

┃ Local    http://localhost:XXXX/
┃ Network  use --host to expose

watching for file changes...
```

## Откройте в браузере:

```
http://localhost:XXXX/test-tenant-form
```

(Где XXXX - номер порта из вывода)

---

## ⚠️ Если ошибки компиляции:

Проверьте последние изменения в файлах:
- `src/components/TenantRequestForm.tsx`
- `src/config/sriLankaLocations.ts`
- `src/utils/tenantTranslations.ts`

Все изменения корректны, проблема скорее всего в том, что несколько процессов node пытаются занять один порт.

---

## 🔧 Полная очистка:

Если ничего не помогает:

1. Закройте все окна VSCode
2. Откройте Task Manager (Ctrl+Shift+Esc)
3. Найдите все процессы "Node.js"
4. Завершите их (End Task)
5. Запустите заново через cmd

---

**Последние изменения работают корректно!**
Проблема только в запуске сервера.
