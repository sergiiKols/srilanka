# 🧪 Руководство по тестированию Keep-Alive исправлений

## Шаг 1: Проверка текущих ошибок

1. Откройте в браузере: **http://localhost:4321/admin/keep-alive**
2. Откройте Developer Tools (F12) → Console
3. Скопируйте все ошибки из консоли

## Шаг 2: Тестирование исправлений в Supabase

### Вариант A: Быстрый тест (рекомендуется)

1. Откройте ваш Supabase проект: https://supabase.com/dashboard
2. Перейдите в **SQL Editor**
3. Скопируйте содержимое файла `tmp_rovodev_test_keep_alive.sql`
4. Вставьте в SQL Editor и нажмите **Run**

**Что проверяет тест:**
- ✅ **users** - правильность полей (first_name, last_name, user_type, role)
- ✅ **landlords** - primary_email, business_name, verified, subscription_tier
- ✅ **properties** - все новые поля включая city, price_per_month, status
- ✅ **rental_requests** - check_in, budget_per_night_min/max, title

**Ожидаемый результат:**
```
NOTICE: ✅ USERS: OK
NOTICE: ✅ LANDLORDS: OK
NOTICE: ✅ PROPERTIES: OK
NOTICE: ✅ RENTAL_REQUESTS: OK
NOTICE: 🧹 Test data cleaned up
```

**Если видите ошибки:**
```
NOTICE: ❌ USERS ERROR: column "first_name" does not exist
```
→ Значит схема базы отличается от той, что в коде. Пришлите мне ошибку.

### Вариант B: Применение полного исправления

Если быстрый тест прошел успешно:

1. Откройте **SQL Editor** в Supabase
2. Скопируйте **ВСЕ** содержимое файла `supabase_keep_alive_DYNAMIC.sql`
3. Выполните скрипт
4. Перезагрузите страницу `/admin/keep-alive`

## Шаг 3: Проверка результатов

После применения исправлений:

1. Обновите страницу **http://localhost:4321/admin/keep-alive**
2. Нажмите кнопку **"Запустить сейчас"** (Run Now)
3. Переключитесь на вкладку **"Logs"**
4. Проверьте, что:
   - ❌ Ошибки исчезли или значительно уменьшились
   - ✅ В логах появляются записи со статусом "success"
   - ✅ В таблицах появляется `last_success_at` дата

## Шаг 4: Детальная диагностика

Если остались ошибки для конкретных таблиц:

### Проверка схемы таблицы:
```sql
SELECT 
  column_name, 
  data_type, 
  is_nullable,
  column_default
FROM information_schema.columns
WHERE table_name = 'название_таблицы'
ORDER BY ordinal_position;
```

### Проверка последних ошибок:
```sql
SELECT 
  table_name,
  error_message,
  created_at
FROM keep_alive_logs
WHERE status = 'error'
ORDER BY created_at DESC
LIMIT 20;
```

## Что делать дальше?

**Если тест прошел успешно:**
→ Скажите "применить исправления" и я обновлю SQL в Supabase

**Если есть ошибки:**
→ Скопируйте текст ошибок и отправьте мне для анализа

**Если нужна помощь:**
→ Просто напишите какая таблица вызывает проблемы
