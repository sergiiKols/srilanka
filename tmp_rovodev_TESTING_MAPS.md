# 🗺️ Тестирование карт после интеграции saved_properties

## ✅ Что было сделано

### Проблема
- Бот сохранял объекты в таблицу `saved_properties`
- Карты (админская и персональная) читали из `property_listings`
- **Результат:** объекты сохранялись, но не отображались на картах

### Решение
1. ✅ Создан API endpoint `/api/saved-properties` для загрузки данных
2. ✅ AdminMasterMap.tsx переделан для чтения из `saved_properties`
3. ✅ Создан новый компонент PersonalMap.tsx для персональных карт
4. ✅ Обновлена страница `/map/personal/[userId]/[token]`

---

## 🧪 Как тестировать

### 1. Тестирование API (базовая проверка)

**Запустить dev сервер:**
```bash
npm run dev
```

**Открыть тестовую страницу:**
```
http://localhost:4321/tmp_rovodev_test_api.html
```

**Проверить:**
- Кнопка "Load All Properties" - должна показать все объекты из БД
- Ввести Telegram User ID и нажать "Load User Properties" - должны показаться объекты конкретного пользователя

---

### 2. Тестирование персональной карты

**URL формат:**
```
http://localhost:4321/map/personal/[USER_ID]/[TOKEN]
```

**Пример:**
```
http://localhost:4321/map/personal/123456789/some_token_here
```

**Что проверить:**
- ✅ Карта загружается
- ✅ Показывается количество объектов пользователя
- ✅ Маркеры отображаются на карте
- ✅ Клик по маркеру открывает детали
- ✅ Кнопка "Refresh" перезагружает данные
- ❌ Если нет объектов - показывается "No Properties Yet"

---

### 3. Тестирование админской карты

**URL:**
```
http://localhost:4321/admin/master-map
```

**Что проверить:**
- ✅ Показывается общее количество объектов
- ✅ Показывается количество уникальных пользователей
- ✅ Все объекты отображаются на карте
- ✅ Фильтры работают (по дате, по слоям)
- ✅ Heatmap режимы переключаются

---

### 4. End-to-End тест с ботом

**Полный цикл:**

1. Отправить /start боту в Telegram
2. Получить ссылку на персональную карту
3. Переслать боту объявление с фото + текстом
4. Бот должен ответить "Объект сохранён"
5. Открыть персональную карту → объект должен появиться
6. Открыть админскую карту → объект тоже должен быть виден

---

## 🔍 Где искать проблемы

### Если объекты не появляются на карте:

1. **Проверить API:**
   - Открыть `/api/saved-properties` в браузере
   - Должен вернуть JSON с массивом `data`

2. **Проверить консоль браузера:**
   - F12 → Console
   - Искать ошибки загрузки или парсинга

3. **Проверить БД:**
   - В Supabase Dashboard открыть Table Editor
   - Таблица `saved_properties` → должны быть записи
   - Проверить что `latitude` и `longitude` заполнены

4. **Проверить формат данных:**
   - В консоли должно быть: `✅ Loaded N properties`
   - Если N = 0, значит данных нет или фильтр не работает

---

## 📊 Структура данных

### Таблица saved_properties
```sql
- id (UUID)
- telegram_user_id (BIGINT) -- ID пользователя Telegram
- latitude (DECIMAL) -- Координаты обязательны!
- longitude (DECIMAL)
- title (TEXT)
- description (TEXT)
- price (DECIMAL)
- currency (TEXT)
- photos (TEXT[]) -- Массив URL фото
- property_type (TEXT)
- created_at (TIMESTAMPTZ)
```

### API Response формат
```json
{
  "data": [
    {
      "id": "uuid",
      "telegram_user_id": 123456789,
      "latitude": 6.9271,
      "longitude": 79.8612,
      "title": "Villa in Negombo",
      "price": 500,
      "currency": "USD",
      "photos": ["https://..."],
      "created_at": "2026-01-30T..."
    }
  ]
}
```

---

## 🐛 Известные ограничения

1. **Токен не проверяется** - персональные карты доступны всем (TODO: добавить проверку)
2. **Нет real-time обновлений** на персональной карте (AdminMasterMap имеет подписку)
3. **Максимум 1000 объектов** в админской карте (лимит в запросе)

---

## 🎯 Следующие шаги

После успешного тестирования:
- [ ] Добавить проверку токена в API
- [ ] Добавить real-time обновления для PersonalMap
- [ ] Добавить пагинацию для больших объемов данных
- [ ] Улучшить UI feedback (loading states, errors)
