# 🚀 Быстрое решение: "Призрачные" объекты на карте

**Проблема:** Объекты показываются на карте, но не могут быть удалены (404 ошибка)

---

## ✅ Решение #1: Очистить кеш браузера (99% случаев)

### Вариант A: Hard Refresh
- **Windows/Linux:** `Ctrl + Shift + R`
- **Mac:** `Cmd + Shift + R`

### Вариант B: Полная очистка кеша
1. `Ctrl + Shift + Delete` (или `Cmd + Shift + Delete`)
2. Выбрать "Cached images and files"
3. Нажать "Clear data"
4. Перезагрузить страницу

---

## ✅ Решение #2: Проверить базу данных

**SQL в Supabase Dashboard:**
```sql
-- Файл: tmp_rovodev_check_ghost_object.sql

SELECT 
  'saved_properties' as table_name,
  id,
  title
FROM saved_properties
WHERE telegram_user_id = 8311531873

UNION ALL

SELECT 
  'archived_properties' as table_name,
  id,
  title
FROM archived_properties
WHERE telegram_user_id = 8311531873;
```

**Если объект `d6bf7a8b-c054-44b9-a8f4-903586e34948` в обеих таблицах:**

```sql
-- Удалить дубликат из saved_properties
DELETE FROM saved_properties 
WHERE id = 'd6bf7a8b-c054-44b9-a8f4-903586e34948';
```

---

## ✅ Решение #3: API тест

Откройте в браузере (новая вкладка в режиме инкогнито):
```
https://srilanka-37u2.vercel.app/api/saved-properties?userId=8311531873&token=gjd2Xh
```

**Проверьте:** Есть ли `d6bf7a8b-c054-44b9-a8f4-903586e34948` в ответе?
- **НЕТ** = Кеш браузера (решение #1)
- **ДА** = Проблема базы данных (решение #2)

---

## 📊 После исправления

1. Hard Refresh (`Ctrl + Shift + R`)
2. Проверить карту: https://srilanka-37u2.vercel.app/map/personal/8311531873/gjd2Xh
3. Должен остаться только 1 объект (или 0)

---

## 🎯 Итог

**Скорее всего это кеш браузера!** Просто сделайте Hard Refresh.
