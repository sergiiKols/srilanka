# 🎯 ПОДРОБНЫЙ ПРОЦЕСС РАБОТЫ С КЛИЕНТОМ

**Дата:** 2026-01-29  
**Вопрос:** Что происходит после /start?

---

## 📱 ПОЛНЫЙ ПУТЬ КЛИЕНТА (Step-by-Step)

### **ШАГ 1: Клиент запускает бота** 👤

```
Клиент в Telegram:
→ Находит бота (по ссылке или username)
→ Нажимает START или отправляет /start
```

---

### **ШАГ 2: Бот получает команду** 🤖

```javascript
// В telegram-webhook.ts

async function handleCommand(message) {
  const command = message.text; // "/start"
  const userId = message.from.id; // Например: 1000089271
  
  if (command === '/start') {
    // 1. Вызывается getOrCreateTenant()
    const tenant = await getOrCreateTenant(userId);
  }
}
```

---

### **ШАГ 3: Создание tenant в базе данных** 💾

```javascript
// В tenant-bot-db.ts

async function getOrCreateTenant(telegramUserId) {
  // 1. Проверяем: есть ли уже этот пользователь?
  const existing = await supabase
    .from('tenants')
    .select('*')
    .eq('telegram_user_id', telegramUserId)
    .single();
  
  if (existing) {
    // Пользователь уже есть - просто обновляем last_active_at
    return existing;
  }
  
  // 2. Новый пользователь! Создаём запись
  const token = generateMapToken(); // Генерирует "aB7cDx" (6 символов)
  const mapUrl = buildPersonalMapUrl(userId, token); 
  // → "https://site.com/map/personal/1000089271/aB7cDx"
  
  // 3. Вставляем в БД
  const newTenant = await supabase
    .from('tenants')
    .insert({
      telegram_user_id: 1000089271,
      map_secret_token: 'aB7cDx',
      personal_map_url: 'https://site.com/map/personal/1000089271/aB7cDx',
      saved_properties_count: 0
    });
  
  return newTenant;
}
```

**Что создалось в БД:**

```sql
-- Таблица tenants, новая запись:
id:                      uuid-123-456
telegram_user_id:        1000089271
map_secret_token:        'aB7cDx'
personal_map_url:        'https://site.com/map/personal/1000089271/aB7cDx'
saved_properties_count:  0
created_at:              2026-01-29 15:30:00
last_active_at:          2026-01-29 15:30:00
```

---

### **ШАГ 4: Бот отправляет приветствие** 📨

```javascript
// Бот отправляет клиенту
await sendTelegramMessage({
  chatId: '1000089271',
  text: `👋 Привет! Я твоя личная записная книжка!
  
📝 Как работает:
1. Нашёл объявление? Переслай мне!
2. Я автоматически сохраню на карте
3. Все объекты в одном месте

🗺️ Твоя карта:
https://site.com/map/personal/1000089271/aB7cDx`,
  
  replyMarkup: {
    inline_keyboard: [
      [{ text: '🗺️ Моя карта', url: 'https://...' }],
      [{ text: '❓ Помощь', callback_data: 'help' }]
    ]
  }
});
```

**Клиент видит:**

```
┌─────────────────────────────────────┐
│ 👋 Привет! Я твоя личная записная   │
│ книжка для объектов недвижимости!   │
│                                     │
│ 📝 Как работает:                    │
│ 1. Нашёл объявление? Переслай мне!  │
│ 2. Я автоматически сохраню на карте │
│ 3. Все объекты в одном месте        │
│                                     │
│ 🗺️ Твоя карта:                     │
│ https://site.com/map/personal/...   │
└─────────────────────────────────────┘

[🗺️ Моя карта] [❓ Помощь]
```

---

### **ШАГ 5: Клиент нажимает "🗺️ Моя карта"** 🗺️

```
Клиент нажимает кнопку
→ Telegram открывает браузер
→ Переходит по ссылке: https://site.com/map/personal/1000089271/aB7cDx
```

---

### **ШАГ 6: Открывается личная карта (ПОКА НЕ СОЗДАНА!)** ⚠️

**ВАЖНО:** Сейчас эта страница ещё не существует!

**Что должно быть (когда создадим):**

```typescript
// src/pages/map/personal/[userId]/[token].astro

---
const { userId, token } = Astro.params;

// 1. Проверка токена
const { data: tenant } = await supabase
  .from('tenants')
  .select('*')
  .eq('telegram_user_id', userId)
  .eq('map_secret_token', token)
  .single();

if (!tenant) {
  return Astro.redirect('/'); // Неверный токен
}

// 2. Загрузка объектов пользователя
const { data: properties } = await supabase
  .from('saved_properties')
  .select('*')
  .eq('telegram_user_id', userId);

// properties = [] (пустой массив, т.к. ещё ничего не добавлено)
---

<html>
  <body>
    <h1>Моя карта ({properties.length} объектов)</h1>
    
    {properties.length === 0 && (
      <div>
        📭 Пока нет сохранённых объектов
        
        💡 Переслай боту объявление с фото - 
        оно появится здесь!
      </div>
    )}
    
    <Map properties={properties} />
  </body>
</html>
```

**Клиент видит:**

```
┌─────────────────────────────────────┐
│ 🗺️ Моя карта (0 объектов)          │
├─────────────────────────────────────┤
│                                     │
│ 📭 Пока нет сохранённых объектов    │
│                                     │
│ 💡 Переслай боту объявление с фото -│
│ оно появится здесь!                 │
│                                     │
│ [Карта Шри-Ланки - пустая]          │
│                                     │
└─────────────────────────────────────┘
```

---

### **ШАГ 7: Клиент пересылает первое объявление** 📸

```
Клиент находит объявление в группе:

┌─────────────────────────────────────┐
│ Группа "Жильё Шри-Ланка"           │
├─────────────────────────────────────┤
│ 📸📸 [2 фото студии]                │
│                                     │
│ Studio $500/month                   │
│ Near beach Negombo                  │
│ WiFi, AC, kitchen                   │
│ +94 77 123 4567                     │
└─────────────────────────────────────┘

→ Нажимает Forward
→ Выбирает бота
→ Отправляет
```

---

### **ШАГ 8: Бот обрабатывает forward** 🤖

```javascript
// telegram-webhook.ts

async function handleCompleteMessage(message) {
  const userId = message.from.id; // 1000089271
  
  // 1. Получаем tenant (уже существует)
  const tenant = await getOrCreateTenant(userId);
  // tenant.saved_properties_count = 0
  
  // 2. Парсим forward метаданные
  const forwardMeta = parseForwardMetadata(message);
  // {
  //   source_type: 'forward_channel',
  //   forward_from_chat_title: 'Жильё Шри-Ланка',
  //   forward_from_message_id: 12345
  // }
  
  // 3. AI анализ описания
  const text = message.caption;
  const aiResult = await analyzeWithFallback(text);
  // {
  //   type: 'studio',
  //   price: 500,
  //   currency: 'USD',
  //   amenities: ['WiFi', 'AC', 'Kitchen'],
  //   contact: { phone: '+94 77 123 4567' }
  // }
  
  // 4. Загрузка фото
  const propertyId = generateUUID(); // 'uuid-abc-123'
  const photoUrls = await uploadTelegramPhotos(
    botToken,
    message.photo,
    userId,
    propertyId
  );
  // photoUrls = [
  //   'https://supabase.co/.../tenant-photos/1000089271/uuid-abc-123/photo_1.jpg',
  //   'https://supabase.co/.../tenant-photos/1000089271/uuid-abc-123/photo_2.jpg'
  // ]
  
  // 5. Сохранение в БД
  const property = await saveProperty({
    telegram_user_id: 1000089271,
    title: 'Studio',
    description: text,
    latitude: 7.2008,
    longitude: 79.8384,
    price: 500,
    currency: 'USD',
    property_type: 'studio',
    photos: photoUrls,
    contact_phone: '+94 77 123 4567',
    amenities: JSON.stringify(['WiFi', 'AC', 'Kitchen']),
    source_type: 'forward_channel',
    forward_from_chat_title: 'Жильё Шри-Ланка'
  });
  
  // 6. Триггер автоматически обновляет счётчик:
  // tenants.saved_properties_count: 0 → 1
  
  // 7. Отправка ответа
  await sendTelegramMessage({
    chatId: userId,
    text: `✅ Объект сохранён! (всего: 1)

🏠 Studio, $500/месяц
📍 Негомбо, у моря
📸 2 фотографии

🗺️ Твоя карта:
${tenant.personal_map_url}`
  });
}
```

**Что создалось в БД:**

```sql
-- Таблица saved_properties, новая запись:
id:                    'uuid-abc-123'
telegram_user_id:      1000089271
title:                 'Studio'
description:           'Studio $500/month Near beach...'
latitude:              7.2008
longitude:             79.8384
price:                 500
currency:              'USD'
property_type:         'studio'
photos:                ['https://.../photo_1.jpg', 'https://.../photo_2.jpg']
contact_phone:         '+94 77 123 4567'
amenities:             '["WiFi","AC","Kitchen"]'
source_type:           'forward_channel'
forward_from_chat_title: 'Жильё Шри-Ланка'
created_at:            2026-01-29 15:35:00

-- Таблица tenants, обновлена:
saved_properties_count: 1  (было 0, триггер увеличил!)
```

---

### **ШАГ 9: Клиент снова открывает карту** 🗺️

```
Клиент нажимает кнопку "🗺️ Открыть карту"
→ https://site.com/map/personal/1000089271/aB7cDx
```

**Теперь клиент видит:**

```
┌─────────────────────────────────────┐
│ 🗺️ Моя карта (1 объект)            │
├─────────────────────────────────────┤
│                                     │
│ [Карта Шри-Ланки с 1 маркером]     │
│                                     │
│ 📍 Маркер в Негомбо                 │
│    (клик открывает попап)           │
│                                     │
│ ┌─────────────────────────────┐     │
│ │ 📸📸 [Фото студии]          │     │
│ │                             │     │
│ │ $500 Studio                 │     │
│ │ Негомбо, у моря             │     │
│ │                             │     │
│ │ 📞 +94 77 123 4567          │     │
│ │ 📨 От: "Жильё Шри-Ланка"   │     │
│ │                             │     │
│ │ [⭐ Избранное]              │     │
│ │ [📝 Заметка]                │     │
│ │ [🗑️ Удалить]                │     │
│ └─────────────────────────────┘     │
└─────────────────────────────────────┘
```

---

### **ШАГ 10: Клиент продолжает добавлять объекты** 🔄

```
День 1: Forward 5 объектов → карта показывает 5 маркеров
День 2: Forward 3 объекта  → карта показывает 8 маркеров
День 3: Удаляет 2          → карта показывает 6 маркеров
```

**Каждый раз:**
1. Бот сохраняет в `saved_properties`
2. Триггер обновляет счётчик в `tenants`
3. Карта автоматически показывает все объекты

---

## 🎯 КЛЮЧЕВЫЕ МОМЕНТЫ

### **1. Карта НЕ создаётся физически**

```
❌ НЕТ: Создание отдельной HTML страницы для каждого пользователя
✅ ДА: Динамическая страница, которая загружает данные из БД по токену
```

**Как это работает:**

```
URL: /map/personal/[userId]/[token]

Astro видит: userId=1000089271, token=aB7cDx
→ Проверяет токен в БД
→ Загружает объекты WHERE telegram_user_id = 1000089271
→ Отображает их на карте
```

**Один URL, разные данные для каждого пользователя!**

---

### **2. Токен = ключ доступа**

```
Без токена: ❌ Доступ запрещён
С токеном:  ✅ Видит свои объекты
```

**Безопасность:**

- 62^6 = 56 миллиардов комбинаций
- Практически невозможно угадать
- Можно делиться ссылкой с друзьями

---

### **3. Счётчик обновляется автоматически**

```sql
-- Триггер в БД делает это автоматически!

CREATE TRIGGER update_properties_count_on_insert
AFTER INSERT ON saved_properties
FOR EACH ROW
EXECUTE FUNCTION update_tenants_properties_count();

-- Когда добавляется property:
-- → триггер увеличивает saved_properties_count
-- Когда удаляется:
-- → триггер уменьшает saved_properties_count
```

---

## 📋 ИТОГОВАЯ СХЕМА

```
/start
  ↓
Создание tenant (если новый)
  ↓
Генерация токена "aB7cDx"
  ↓
Генерация URL: /map/personal/USER_ID/TOKEN
  ↓
Отправка приветствия с ссылкой
  ↓
Клиент открывает карту (0 объектов)
  ↓
Клиент пересылает объявление
  ↓
Бот обрабатывает → AI анализ → Загрузка фото → Сохранение в БД
  ↓
Счётчик +1 (триггер)
  ↓
Ответ: "Сохранено! (всего: 1)"
  ↓
Клиент открывает карту (1 объект)
  ↓
Цикл повторяется...
```

---

## ⚠️ ЧТО ОСТАЛОСЬ СОЗДАТЬ

**СЕЙЧАС НЕ РАБОТАЕТ:**

```
❌ Страница /map/personal/[userId]/[token]
   → Нужно создать (30 минут)
```

**ВСЁ ОСТАЛЬНОЕ РАБОТАЕТ:**

```
✅ /start → создание tenant
✅ Генерация токена
✅ Генерация URL
✅ Forward → AI анализ
✅ Загрузка фото
✅ Сохранение в БД
✅ Счётчик
```

---

## 🚀 СЛЕДУЮЩИЙ ШАГ

**После деплоя:**
1. Настроить webhook ✅
2. Протестировать /start ✅
3. Протестировать forward ✅
4. **Создать личную карту** ⏳ (30 минут)

**Или:**
- Сначала протестировать бота (карта покажет 404)
- Потом создать карту
- Потом полное тестирование

---

Понятна логика? Готовы к деплою? 🚀
