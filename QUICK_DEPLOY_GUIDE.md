# ⚡ Быстрая инструкция - Деплой через GitHub

## 🎯 ШАГ 1: Push в GitHub (СЕЙЧАС)

Выполните в вашем локальном проекте:

```bash
# Добавить все новые файлы Docker
git add .

# Коммит
git commit -m "Add Docker deployment with GitHub Container Registry"

# Push в GitHub
git push origin main
```

---

## 🎯 ШАГ 2: Проверить сборку на GitHub (2-3 минуты)

1. Откройте: **https://github.com/sergiiKols/srilanka/actions**
2. Увидите workflow: **"Build and Push Docker Image"**
3. Дождитесь зеленой галочки ✅ (~3-5 минут)

### Что происходит:
- ✅ GitHub Actions собирает Docker образ
- ✅ Пушит в GitHub Container Registry
- ✅ Образ доступен: `ghcr.io/sergiikols/srilanka:latest`

---

## 🎯 ШАГ 3: Сделать образ публичным

После успешной сборки:

1. Откройте: **https://github.com/sergiiKols/srilanka**
2. Справа найдите раздел **"Packages"** → `srilanka`
3. Нажмите на пакет
4. **Package settings** → **Change visibility** → **Public**
5. Введите название репозитория для подтверждения: `srilanka`
6. Нажмите **"I understand, change package visibility"**

**Зачем:** Чтобы Docker панель могла скачать образ без авторизации.

---

## 🎯 ШАГ 4: Создать сервис в Docker панели

Вернитесь в вашу Docker панель (traveler/production).

### Нажмите кнопку **"+ Create Service"**

Заполните форму:

#### 📝 Основная информация
```
Service Name: sri-lanka-app
Image: ghcr.io/sergiikols/srilanka:latest
```

#### 🔌 Порты (Ports)
```
Container Port: 3000
Host Port: 80  (или 3000, если 80 занят)
Protocol: TCP
```

#### 🔐 Переменные окружения (Environment Variables)

**ОБЯЗАТЕЛЬНЫЕ:**
```env
PUBLIC_SUPABASE_URL=https://ваш-проект.supabase.co
PUBLIC_SUPABASE_ANON_KEY=eyJxxxxxxxxx
TELEGRAM_BOT_TOKEN=123456789:ABCDEFxxxxxxxxx
TELEGRAM_ADMIN_CHAT_ID=123456789
PUBLIC_GOOGLE_MAPS_API_KEY=AIzaSyxxxxxxxxx
SITE_API_URL=https://ваш-домен.com/api
```

**ОПЦИОНАЛЬНЫЕ:**
```env
PERPLEXITY_API_KEY=pplx-xxxxxx
GROQ_API_KEY=gsk_xxxxxx
```

⚠️ **ВАЖНО:** Замените на ваши реальные значения из `.env` файла!

#### ⚙️ Дополнительные настройки (если есть)
```
Restart Policy: Always
Pull Policy: Always (чтобы всегда брать свежий образ)
```

---

## 🎯 ШАГ 5: Запустить!

1. Нажмите **"Create"** / **"Deploy"** / **"Start"**
2. Дождитесь статуса **"Running"** 🟢
3. Проверьте логи (если есть кнопка "Logs")

---

## ✅ ШАГ 6: Проверка

### Найдите URL вашего приложения
В панели должен быть URL или IP адрес, например:
- `http://123.456.789.0:3000`
- `http://your-domain.com`

### Откройте в браузере:
```
http://ваш-ip:3000
```

### Проверка Health:
```
http://ваш-ip:3000/health.json
```

Должно вернуть:
```json
{"status": "ok"}
```

---

## 🔄 Автоматические обновления

Теперь при каждом `git push origin main`:
1. GitHub Actions автоматически соберет новый образ
2. В Docker панели просто нажмите **"Redeploy"** или **"Restart"**

---

## 🆘 Частые проблемы

### ❌ "Image not found"
**Решение:** Убедитесь что образ публичный (Шаг 3)

### ❌ "Container exits immediately"
**Решение:** 
- Проверьте логи в панели
- Убедитесь что ВСЕ обязательные переменные заполнены
- Проверьте что значения правильные (нет лишних пробелов)

### ❌ "Port already in use"
**Решение:** Измените Host Port с 80 на 3000 или другой свободный

### ❌ "Cannot connect"
**Решение:**
- Проверьте firewall (порт должен быть открыт)
- Проверьте что контейнер запущен (статус Running)

---

## 📋 Чеклист

- [ ] Git push выполнен
- [ ] GitHub Actions успешно завершился ✅
- [ ] Образ сделан публичным
- [ ] Сервис создан в панели
- [ ] Все переменные окружения заполнены
- [ ] Сервис запущен (статус Running)
- [ ] Приложение открывается в браузере
- [ ] Health check работает

---

## 🎉 Готово!

Если все шаги выполнены - ваше приложение работает в Docker!

### Следующие шаги (опционально):
1. Настроить домен
2. Настроить SSL/HTTPS
3. Настроить мониторинг
4. Настроить backup

---

**Вопросы?** Пишите на каком шаге возникла проблема!
