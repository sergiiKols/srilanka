# 🔍 ДИАГНОСТИКА: Бот не отвечает

**Время:** 2026-01-30 21:45  
**Проблема:** Бот не даёт обратной связи при отправке фото

---

## 📍 ЧТО НУЖНО ПРОВЕРИТЬ СЕЙЧАС

### 1. Проверить логи Vercel (КРИТИЧНО!)

**Откройте:**
```
https://vercel.com/sergiikolss-projects/srilanka-37u2/logs
```

**Или:**
1. Vercel Dashboard → Проект srilanka-37u2
2. Вкладка "Logs" 
3. Выберите "Functions"
4. Найдите `/api/telegram-webhook`

**Что искать:**
- ❌ Ошибки (красные строки)
- ⚠️ Таймауты
- 🔴 500/502 ошибки

---

### 2. Проверить webhook статус

Выполните в PowerShell:
```powershell
$webhook = Invoke-RestMethod -Uri "https://api.telegram.org/bot7958965950:AAFg63FTJ46hcRT6M2wSBzn9RHCrzvfV3q8/getWebhookInfo" -Method Post
$webhook.result
```

**Проверьте:**
- `pending_update_count` - должно быть 0 или малое число
- `last_error_date` - не должно быть
- `last_error_message` - не должно быть

---

### 3. Проверить endpoint доступен

```powershell
Invoke-RestMethod -Uri "https://srilanka-37u2.vercel.app/api/telegram-webhook" -Method Get
```

**Должен вернуть:**
```json
{
  "status": "Telegram webhook is active",
  "method": "GET"
}
```

---

## 🐛 ВОЗМОЖНЫЕ ПРИЧИНЫ

### Причина 1: Ошибка в коде (синтаксис)

**Симптомы:**
- В логах Vercel: `SyntaxError` или `ReferenceError`
- Webhook показывает ошибку

**Решение:**
- Откатить последний коммит
- Проверить код

### Причина 2: Timeout (долгая обработка)

**Симптомы:**
- В логах: `Task timed out after 10.00 seconds`
- Webhook показывает много pending updates

**Решение:**
- Увеличить timeout в vercel.json
- Оптимизировать код

### Причина 3: Environment variables

**Симптомы:**
- В логах: `undefined` или `null` при обращении к env
- Ошибки подключения к Supabase

**Решение:**
- Проверить в Vercel → Settings → Environment Variables
- Обязательные переменные:
  ```
  PUBLIC_SUPABASE_URL
  PUBLIC_SUPABASE_ANON_KEY
  TELEGRAM_BOT_TOKEN
  ```

### Причина 4: Vercel deployment failed

**Симптомы:**
- Последний деплой показывает "Error" или "Failed"

**Решение:**
- Проверить Build Logs в Vercel
- Исправить ошибки сборки
- Пересобрать

---

## 🚨 СРОЧНЫЕ ДЕЙСТВИЯ

### Действие 1: Проверить деплой

```
1. Откройте: https://vercel.com/dashboard
2. Найдите последний деплой (коммит b840eff)
3. Статус должен быть "Ready" (зелёная галочка)
4. Если "Error" - откройте Build Logs
```

### Действие 2: Проверить Runtime Logs

```
1. Vercel → Functions → Logs
2. Выберите последние 30 минут
3. Найдите POST /api/telegram-webhook
4. Посмотрите что там
```

### Действие 3: Отправить тестовый запрос

```powershell
# Проверка что endpoint жив
Invoke-RestMethod -Uri "https://srilanka-37u2.vercel.app/health.json" -Method Get
```

---

## 📝 ИНСТРУКЦИЯ ДЛЯ СБОРА ИНФОРМАЦИИ

Выполните по порядку и отправьте результаты:

### 1. Webhook Info
```powershell
$w = Invoke-RestMethod -Uri "https://api.telegram.org/bot7958965950:AAFg63FTJ46hcRT6M2wSBzn9RHCrzvfV3q8/getWebhookInfo" -Method Post
Write-Host "URL: $($w.result.url)"
Write-Host "Pending: $($w.result.pending_update_count)"
Write-Host "Last Error: $($w.result.last_error_message)"
```

### 2. Deployment Status
```
Откройте Vercel Dashboard и скажите:
- Статус последнего деплоя: Ready / Error / Building
- Когда задеплоен: время
- Build Logs: есть ли ошибки
```

### 3. Runtime Logs
```
Vercel → Logs → Functions → telegram-webhook
Скопируйте последние 10 строк
```

### 4. Test Endpoint
```powershell
try {
  Invoke-RestMethod -Uri "https://srilanka-37u2.vercel.app/api/telegram-webhook" -Method Get
  Write-Host "✅ Endpoint доступен"
} catch {
  Write-Host "❌ Endpoint недоступен: $_"
}
```

---

## 🔧 ВРЕМЕННОЕ РЕШЕНИЕ (если нужно срочно)

Если ничего не помогает - откатить изменения:

```bash
git revert HEAD
git push
```

Это вернёт код к предыдущей версии (до изменений с timeout).

---

**СРОЧНО выполните проверки и отправьте результаты!**
