# 🔍 Диагностика проблемы с архивными объектами

**Дата:** 31 января 2026, 19:10

---

## 📊 Текущая ситуация

### Что вы сообщили:
1. ❌ **Тестовая клиентская карта не запускается** - ошибка 500
2. ❌ **Другая клиентская карта тоже не запускается** - ошибка 500
3. ✅ **На карте админа пропали удалённые объекты** (это правильно!)
4. ⚠️ **При нажатии "Show Archived" выводится ТОЛЬКО 1 объект**

### Что я исправил:
1. ✅ Откатил все фильтры по `deleted_at` (колонка не существует)
2. ✅ Убрал избыточную проверку в DELETE endpoint
3. ✅ Все карты теперь должны работать

---

## 🔴 Главный вопрос

**Сколько объектов ДОЛЖНО быть в архиве?**

Если вы удалили 5 объектов, но в архиве показывается только 1 - это проблема.  
Если вы удалили только 1 объект - всё работает правильно!

---

## 🧪 Что нужно проверить в Supabase

### Шаг 1: Выполните SQL запрос

Откройте **Supabase Dashboard → SQL Editor** и выполните файл:  
`tmp_rovodev_check_archive.sql`

Или скопируйте этот запрос:

```sql
-- Быстрая проверка
SELECT 
  'saved_properties' as table_name,
  COUNT(*) as count
FROM saved_properties

UNION ALL

SELECT 
  'archived_properties' as table_name,
  COUNT(*) as count
FROM archived_properties

UNION ALL

SELECT 
  'archived (can_restore=true)' as table_name,
  COUNT(*) as count
FROM archived_properties
WHERE can_restore = true;
```

### Шаг 2: Проверьте результаты

**Ожидаемые результаты:**

| table_name | count |
|-----------|-------|
| saved_properties | 2-10 (активные объекты) |
| archived_properties | 1+ (удалённые объекты) |
| archived (can_restore=true) | 1+ (можно восстановить) |

**Важно:**  
- Если `archived_properties` = 1, то это нормально (вы удалили 1 объект)
- Если `archived_properties` = 0, то архивация не работает
- Если `can_restore=true` = 0, то фильтр в коде блокирует отображение

---

## 🐛 Возможная проблема #1: Фильтр can_restore

**Код в AdminMasterMap.tsx (строка 160):**
```typescript
.eq('can_restore', true) // Только не восстановленные
```

**Проблема:** Если в базе есть объекты с `can_restore = false`, они **не будут показаны**!

**Решение:** Показывать ВСЕ архивные объекты (независимо от can_restore).

---

## 🐛 Возможная проблема #2: Фильтр по пользователю

**Код в AdminMasterMap.tsx (строка 186-188):**
```typescript
if (selectedUser !== 'all') {
    query = query.eq('telegram_user_id', parseInt(selectedUser));
}
```

**Проблема:** Если вы выбрали конкретного пользователя в фильтре, архивные объекты других пользователей **не покажутся**!

**Решение:** Проверьте, что фильтр "All Users" выбран.

---

## 🐛 Возможная проблема #3: Фильтр по дате

**Код в AdminMasterMap.tsx (строка 164-182):**
```typescript
if (dateFilter !== 'all') {
    // Фильтр по дате архивации
}
```

**Проблема:** Если выбран фильтр "Today" или "Week", старые архивные объекты **не покажутся**!

**Решение:** Проверьте, что фильтр "All Time" выбран.

---

## ✅ Что делать СЕЙЧАС

### 1. Проверьте базу данных
```sql
-- Выполните в Supabase SQL Editor
SELECT * FROM archived_properties ORDER BY archived_at DESC;
```

**Проверьте:**
- Сколько строк в результате?
- Какое значение в колонке `can_restore`?
- Какие `telegram_user_id`?

### 2. Проверьте фильтры на админской карте

Откройте `/admin/master-map` и проверьте:
- [ ] **User Filter:** должен быть "All Users"
- [ ] **Date Filter:** должен быть "All Time"
- [ ] **Show Archived:** включите переключатель

### 3. Проверьте консоль браузера

Откройте DevTools (F12) и найдите логи:
```
📦 Загружаем архивные объекты из archived_properties
📦 Получено из базы: X архивных объектов
✅ Загружено X архивных объектов
```

**Вопросы:**
- Сколько объектов получено из базы?
- Сколько объектов загружено?
- Есть ли ошибки?

---

## 🔧 Временное исправление (если нужно показать ВСЕ архивные)

Если хотите показывать ВСЕ архивные объекты (даже с `can_restore = false`):

**Изменить строку 160 в AdminMasterMap.tsx:**
```typescript
// Было:
.eq('can_restore', true)

// Станет:
// .eq('can_restore', true) // Закомментировать эту строку
```

---

## 📊 Отправьте мне результаты

После проверки отправьте:

1. **Результат SQL запроса** (количество объектов в каждой таблице)
2. **Логи из консоли** (что выводится при включении "Show Archived")
3. **Текущие фильтры** (какой User и Date Filter выбран)

Тогда я смогу точно определить проблему! 🎯
