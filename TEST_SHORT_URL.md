# 🧪 Тестирование разворачивания коротких ссылок

## Как протестировать:

### 1. Откройте консоль браузера (F12)

### 2. Перейдите на вкладку "Console"

### 3. Попробуйте импортировать короткую ссылку

### 4. Вы должны увидеть такие логи:

```
🔗 Начинаем разворачивание короткой ссылки: https://maps.app.goo.gl/xxx
Метод 1: Пробуем серверный API...
🔗 Используем серверный API для разворачивания: https://maps.app.goo.gl/xxx
📥 Ответ от API: {status: 200, ok: true, statusText: "OK"}
📦 Данные от API: {success: true, expandedUrl: "https://...", method: "server-fetch"}
✅ Сервер успешно развернул ссылку через: server-fetch
✅ Развернутый URL: https://www.google.com/maps/...
```

## Возможные проблемы:

### Проблема 1: API endpoint не отвечает
**Симптомы:**
```
❌ Ошибка серверного API: Failed to fetch
```

**Решение:**
- Проверьте что dev сервер запущен
- Проверьте что API endpoint существует: http://localhost:XXXX/api/expand-url
- Попробуйте перезапустить сервер

### Проблема 2: API возвращает success: false
**Симптомы:**
```
⚠️ API вернул success=false: Не удалось развернуть ссылку
```

**Решение:**
- Google блокирует серверные запросы
- Короткая ссылка требует JavaScript выполнения
- Переходим к fallback методам

### Проблема 3: CORS ошибки
**Симптомы:**
```
Access to fetch at '...' from origin '...' has been blocked by CORS policy
```

**Решение:**
- Это нормально для клиентских методов
- Серверный API должен работать (нет CORS на сервере)
- Используется fallback на ручной режим

## Тестовая ссылка:

```
https://maps.app.goo.gl/KSZKYnL8PmKigKPe7?g_st=it
```

## Ожидаемый результат:

Одна из попыток должна сработать:
- ✅ Серверный API (метод 1)
- ✅ Клиентский fetch (метод 2)
- ✅ Hidden iframe (метод 3)
- ✅ Ручной помощник (fallback)

## Текущий статус методов:

| Метод | Работает | Примечание |
|-------|----------|-----------|
| Серверный fetch | ❓ | Зависит от Google |
| HTML parsing | ✅ | Парсит мета-теги |
| Клиентский fetch | ⚠️ | CORS блокирует |
| Hidden iframe | ⚠️ | CORS блокирует |
| Ручной режим | ✅ | Всегда работает |

## Как улучшить:

### Вариант 1: Puppeteer на сервере
Запустить headless браузер для JavaScript выполнения

### Вариант 2: Прокси сервис
Использовать сторонний прокси без CORS

### Вариант 3: Browser Extension
Создать расширение с полными правами

### Вариант 4: Пользователь копирует URL (текущий fallback)
Самый надежный метод - работает в 100% случаев
