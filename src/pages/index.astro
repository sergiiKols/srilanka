---
// Redirect to admin panel as default page
return Astro.redirect('/admin');
---

<style>
	.main {
		flex: 1;
		display: flex;
		flex-direction: column;
		position: relative;
		overflow: hidden;
	}

	.map-container {
		flex: 1;
		background-color: #e5e7eb; /* Placeholder color */
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.placeholder-map {
		color: var(--color-text-muted);
	}
</style>
