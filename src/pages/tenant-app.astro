---
/**
 * TENANT REQUEST FORM PAGE
 * 
 * Страница формы для арендаторов, открываемая через Telegram Web App.
 * URL: /tenant-app
 */

export const prerender = true; // Force static generation

import TenantRequestForm from '../components/TenantRequestForm';
import '../styles/tenant-form.css';

const pageTitle = 'Find Accommodation in Sri Lanka';
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
  <meta name="theme-color" content="#7C3AED" />
  
  <title>{pageTitle}</title>
  <meta name="version" content="2.1" />
  
  <!-- Telegram Web App Script - Official SDK -->
  <script src="https://telegram.org/js/telegram-web-app.js"></script>
  
  <!-- Prevent zoom on iOS -->
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover" />
  
  <!-- Favicon -->
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  
  <style>
    /* Reset & Base */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    html, body {
      height: 100%;
      overflow-x: hidden;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }
    
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: var(--tg-bg, #ffffff);
      color: var(--tg-text, #000000);
    }
    
    /* Loading State */
    .app-loading {
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      font-size: 18px;
      color: var(--tg-hint, #999);
    }
    
    .spinner {
      display: inline-block;
      width: 40px;
      height: 40px;
      border: 4px solid rgba(124, 58, 237, 0.1);
      border-top-color: var(--tg-button, #7C3AED);
      border-radius: 50%;
      animation: spin 0.8s linear infinite;
    }
    
    @keyframes spin {
      to { transform: rotate(360deg); }
    }
  </style>
</head>
<body>
  <!-- Loading fallback -->
  <div id="app-loading" class="app-loading">
    <div class="spinner"></div>
  </div>
  
  <!-- Main App -->
  <div id="app" style="display: none;">
    <TenantRequestForm client:only="react" />
  </div>
  
  <script type="module">
    // Version 3.1 - Fixed deployment, always show form
    console.log('🚀 Tenant Form v3.1 - Production ready');
    
    // Wait for Telegram SDK to load
    function initApp() {
      if (window.Telegram?.WebApp) {
        const tg = window.Telegram.WebApp;
        console.log('✅ Telegram WebApp loaded:', tg.version);
        console.log('Platform:', tg.platform);
        console.log('InitData present:', !!tg.initData);
        
        // Initialize WebApp
        tg.ready();
        tg.expand();
        
        // Apply theme
        if (tg.themeParams.bg_color) {
          document.body.style.backgroundColor = tg.themeParams.bg_color;
        }
      } else {
        console.log('⚠️ Telegram WebApp not loaded yet');
      }
      
      // Show form
      document.getElementById('app-loading').style.display = 'none';
      document.getElementById('app').style.display = 'block';
    }
    
    // Wait for both DOM and Telegram SDK
    let attempts = 0;
    function checkAndInit() {
      attempts++;
      if (window.Telegram?.WebApp || attempts > 20) {
        initApp();
      } else {
        setTimeout(checkAndInit, 100);
      }
    }
    
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', checkAndInit);
    } else {
      checkAndInit();
    }
  </script>
</body>
</html>
