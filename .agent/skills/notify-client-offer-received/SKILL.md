## Name: Notify Client Offer Received

## Description:
Отправка уведомления клиенту о новом предложении

## Purpose:
Отправить клиенту уведомление, что по его заявке пришло новое предложение.

---

## НАЗНАЧЕНИЕ
Уведомить клиента о новом предложении через Telegram

## КОГДА ЗАПУСКАЕТСЯ
Сразу после создания rental_offer (в skill #2) или после валидации property

## ЧТО НА ВХОДЕ
- `client_id` (кому отправлять)
- `offer_id` (какое предложение)
- `property` данные (название, цена, landlord)

## ЧТО НА ВЫХОДЕ
- Уведомление отправлено в Telegram
- В БД логируется факт отправки

## ЧТО СИСТЕМА ДЕЛАЕТ

### Шаг 1: Получение telegram_id
```javascript
const client = await supabase
  .from('users')
  .select('telegram_id')
  .eq('id', client_id)
  .single();
```

### Шаг 2: Формирование сообщения
```javascript
const message = `
🏠 Новое предложение на твою заявку!

${property.name}
💰 $${property.price}/ночь
🛏️ ${property.bedrooms} спален

Посмотри все предложения на карте 👇
${mapUrl}
`;
```

### Шаг 3: Отправка
```javascript
const result = await bot.sendMessage(client.telegram_id, message);
```

### Шаг 4: Логирование
```javascript
await supabase.from('notifications').insert({
  client_id, offer_id, sent_at: new Date(), message_id: result.message_id
});
```

## ВАЖНЫЕ ВОПРОСЫ
- **Частота:** Одно уведомление за offer
- **Текст:** См. выше
- **Кнопки:** Да, inline кнопка "Открыть карту"
- **Защита от спама:** Max 10 уведомлений/день одному клиенту

## Expected Output
```json
{
  "status": "sent",
  "notification_id": 789,
  "message_id": 12345
}
```
