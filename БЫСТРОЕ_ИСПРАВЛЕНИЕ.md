# 🔥 БЫСТРОЕ ИСПРАВЛЕНИЕ - Архивация не работает!

## ❌ **ПРОБЛЕМА:**
Функция `soft_delete_property` **НЕ СОЗДАНА** в базе данных!

Вот почему объекты не архивируются при удалении через кнопку Delete.

---

## ✅ **РЕШЕНИЕ (2 минуты):**

### Шаг 1: Выполните SQL-скрипт
Откройте **Supabase SQL Editor** и выполните файл:
**`FIX_ARCHIVE_FUNCTIONS.sql`**

Этот скрипт:
1. ✅ Создаст функцию `soft_delete_property`
2. ✅ Даст права на выполнение
3. ✅ Создаст тестовый объект
4. ✅ Проверит что всё работает

---

### Шаг 2: Проверьте результат

После выполнения скрипта вы увидите:

```
function_name          | created | status
-----------------------|---------|------------------
soft_delete_property   | true    | ✅ ФУНКЦИЯ СОЗДАНА!
```

И тестовый объект:
```
id                                   | title
-------------------------------------|-------------------------------
xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx | ТЕСТ - Удалить через функцию
```

---

### Шаг 3: Удалите тестовый объект

**Скопируйте ID** из результата выше и выполните:

```sql
-- Замените 'ВСТАВЬТЕ_ID' на реальный ID
SELECT soft_delete_property('ВСТАВЬТЕ_ID'::UUID, 999999999, 'test_deletion');
```

**Должен вернуть:** `true`

---

### Шаг 4: Проверьте архивацию

```sql
-- Проверяем что объект переместился
SELECT COUNT(*) as "В saved_properties" 
FROM saved_properties 
WHERE telegram_user_id = 999999999;
-- Должно быть: 0

SELECT COUNT(*) as "В archived_properties" 
FROM archived_properties 
WHERE telegram_user_id = 999999999;
-- Должно быть: 1

-- Смотрим детали
SELECT * FROM archived_properties WHERE telegram_user_id = 999999999;
```

---

### Шаг 5: Проверьте на карте

1. Обновите страницу `/admin/master-map` (F5)
2. Откройте Admin Panel (⚙️ Admin)
3. Включите "Show Archived"

**Результат:** Должна показывать **"Show Archived (1)"** и серый маркер в Galle!

---

## 🎯 **ПОСЛЕ ЭТОГО:**

### Теперь архивация будет работать автоматически! ✨

Когда пользователь удаляет объект через PropertyDrawer:
1. Клик на кнопку "Delete"
2. Фронтенд вызывает `DELETE /api/saved-properties/[id]`
3. API вызывает `soft_delete_property()`
4. Объект перемещается в `archived_properties`
5. Admin может увидеть через "Show Archived"

---

## 🐛 **Если что-то не работает:**

### Ошибка: "Property not found or access denied"
**Причина:** User ID не совпадает или объект не существует
**Решение:** Проверьте ID и user_id в запросе

### Ошибка: "permission denied for function soft_delete_property"
**Причина:** Нет прав на выполнение
**Решение:** Выполните:
```sql
GRANT EXECUTE ON FUNCTION soft_delete_property TO anon, authenticated;
```

### Объект архивируется, но не показывается на карте
**Причина:** RLS блокирует чтение
**Решение:** Выполните:
```sql
CREATE POLICY "Allow anon read archived"
  ON archived_properties FOR SELECT
  TO anon, authenticated
  USING (can_restore = true);
```

---

## 📊 **Техническая информация:**

### Что делает функция `soft_delete_property`:

```
1. Получает объект из saved_properties
   ↓
2. Проверяет что user_id совпадает
   ↓
3. Вычисляет сколько дней объект был активен
   ↓
4. Копирует все данные в archived_properties с:
   - can_restore = TRUE
   - archive_reason = 'user_deleted' (или другая причина)
   - archived_at = текущее время
   - days_active = количество дней
   ↓
5. Удаляет из saved_properties
   ↓
6. Возвращает TRUE
```

### Параметры функции:

```sql
soft_delete_property(
    property_id UUID,        -- ID объекта
    user_id BIGINT,          -- Telegram User ID (для проверки прав)
    reason TEXT              -- Причина архивации (по умолчанию 'user_deleted')
)
RETURNS BOOLEAN
```

---

## ✅ **Итог:**

**До исправления:**
- ❌ Функция `soft_delete_property` не существует
- ❌ При удалении объект полностью удаляется
- ❌ "Show Archived (0)" - нечего показывать

**После исправления:**
- ✅ Функция создана и работает
- ✅ При удалении объект архивируется
- ✅ "Show Archived (N)" - показывает архивные объекты
- ✅ Можно восстановить в будущем

---

**Выполните `FIX_ARCHIVE_FUNCTIONS.sql` прямо сейчас!** 🚀

---

## 📞 **После выполнения:**

Напишите мне результат, покажите:
1. ✅ Скриншот с "✅ ФУНКЦИЯ СОЗДАНА!"
2. ✅ Результат проверки архивации (0 в saved, 1 в archived)
3. ✅ Скриншот карты с "Show Archived (1)"

Тогда я буду знать что всё работает! 🎉
