# 🤖 ПЛАН РЕАЛИЗАЦИИ TELEGRAM БОТА ДЛЯ АРЕНДАТОРОВ

**Дата:** 2026-01-29  
**Статус:** ✅ База данных готова → 🚀 Переходим к боту

---

## 📋 ЧТО УЖЕ ЕСТЬ

### ✅ База данных (100%)
- `tenants` - пользователи бота
- `saved_properties` - сохранённые объекты
- `access_attempts` - попытки доступа
- Триггеры для счётчиков
- Keep-alive система

### ✅ Документация (100%)
- `NEW_BOT_LOGIC_2026-01-29.md` - новая логика
- `TENANT_BOT_CORRECT_LOGIC_2026-01-29.md` - концепция "записной книжки"
- `DATABASE_TABLE_CREATION_POLICY.md` - политика создания таблиц
- `ADMIN_MASTER_MAP_CONCEPT.md` - концепция админской карты

### 🟡 Существующий код (частично)
- `src/lib/telegram.ts` - библиотека работы с Telegram (старая)
- `src/pages/api/tenant-request.ts` - API для tenant form (не для бота)
- Карта на `/map.astro` - но нет личных карт

---

## 🎯 ПЛАН РЕАЛИЗАЦИИ (10 ШАГОВ)

### **ШАГ 1: Подготовка утилит**
**Файл:** `src/lib/tenant-bot-utils.ts`

```typescript
// Функции:
- generateMapToken(): string // Генерация 6-символьного токена
- buildPersonalMapUrl(userId, token): string
- parseForwardMetadata(message): ForwardMetadata
- extractPropertyInfo(text, location): PropertyData
```

**Зависимости:** нет  
**Время:** ~15 минут

---

### **ШАГ 2: Telegram Bot API Endpoint**
**Файл:** `src/pages/api/telegram-bot.ts`

```typescript
POST /api/telegram-bot
Body: {
  message: TelegramMessage,
  update_id: number
}

Логика:
1. Проверить подпись Telegram (security)
2. Определить тип сообщения (forward/direct)
3. Извлечь данные (фото, текст, location)
4. Вызвать обработчики
```

**Зависимости:** ШАГ 1  
**Время:** ~20 минут

---

### **ШАГ 3: Регистрация пользователей**
**Файл:** `src/lib/tenant-bot-db.ts`

```typescript
// Функции:
async function getOrCreateTenant(telegramUserId: number): Promise<Tenant>
  → Проверить существование в `tenants`
  → Если нет - создать с новым token
  → Вернуть данные пользователя
```

**Зависимости:** ШАГ 1  
**Время:** ~10 минут

---

### **ШАГ 4: Парсинг Forward метаданных**
**Файл:** `src/lib/telegram-forward-parser.ts`

```typescript
interface ForwardMetadata {
  source_type: 'direct' | 'forward_user' | 'forward_channel',
  forward_from_user_id?: number,
  forward_from_username?: string,
  forward_from_chat_id?: number,
  forward_from_chat_title?: string,
  forward_date?: Date,
  original_message_link?: string
}

function parseForward(message: TelegramMessage): ForwardMetadata
```

**Зависимости:** нет  
**Время:** ~15 минут

---

### **ШАГ 5: Сохранение объекта в БД**
**Файл:** `src/lib/tenant-bot-db.ts`

```typescript
async function saveProperty(data: SavePropertyInput): Promise<Property>
  → INSERT INTO saved_properties
  → Триггер автоматически обновит счётчик
  → Вернуть ID и данные объекта
```

**Зависимости:** ШАГ 3, ШАГ 4  
**Время:** ~15 минут

---

### **ШАГ 6: Загрузка фотографий**
**Файл:** `src/lib/tenant-bot-photos.ts`

```typescript
async function uploadPhotosFromTelegram(
  photos: TelegramPhoto[],
  userId: number,
  propertyId: string
): Promise<string[]>
  → Скачать через Telegram Bot API
  → Загрузить в Supabase Storage: tenant-photos/{userId}/{propertyId}/
  → Вернуть массив публичных URL
```

**Зависимости:** ШАГ 5  
**Время:** ~20 минут

---

### **ШАГ 7: Парсинг описания объекта**
**Файл:** `src/lib/property-parser.ts`

```typescript
function parsePropertyDescription(text: string): PropertyInfo
  → Извлечь цену (regex: $500, 500$, 500 USD)
  → Извлечь тип (house, apartment, room, studio)
  → Извлечь параметры (2 bed, 1 bath)
  → Извлечь контакты (phone, name)
```

**Зависимости:** нет  
**Время:** ~20 минут

---

### **ШАГ 8: Отправка ответа пользователю**
**Файл:** `src/lib/telegram-bot-sender.ts`

```typescript
async function sendMapLink(
  chatId: number,
  mapUrl: string,
  propertyCount: number
): Promise<void>
  → Отправить сообщение через Telegram Bot API
  → Форматирование:
    "✅ Объект сохранён! (всего: 5)
     🗺️ Ваша карта: {mapUrl}
     
     💡 Пересылайте сюда объявления - они автоматически 
     добавятся на карту!"
```

**Зависимости:** ШАГ 3  
**Время:** ~15 минут

---

### **ШАГ 9: Личная карта пользователя**
**Файлы:** 
- `src/pages/map/[userId]/[token].astro`
- `src/components/map/PersonalMap.tsx`

```typescript
GET /map/{userId}/{token}
  → Проверить access_attempts
  → Проверить token в tenants
  → Загрузить saved_properties WHERE telegram_user_id = userId
  → Отобразить карту с маркерами
  → Кнопка "Удалить" для каждого объекта
```

**Зависимости:** ШАГ 5  
**Время:** ~30 минут

---

### **ШАГ 10: Тестирование**

**Тест 1: Регистрация нового пользователя**
```bash
→ Forward сообщение боту
→ Проверить запись в tenants
→ Проверить генерацию token
```

**Тест 2: Сохранение объекта**
```bash
→ Forward объявление с фото
→ Проверить запись в saved_properties
→ Проверить загрузку фото в Storage
→ Проверить счётчик в tenants
```

**Тест 3: Личная карта**
```bash
→ Открыть ссылку /map/{userId}/{token}
→ Проверить отображение маркеров
→ Проверить попап с информацией
```

**Тест 4: Forward метаданные**
```bash
→ Forward из группы
→ Forward от пользователя
→ Проверить заполнение полей source_type, forward_from_*
```

**Время:** ~30 минут

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Telegram Bot Setup

```env
# .env
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_BOT_WEBHOOK_URL=https://yourdomain.com/api/telegram-bot
```

### Supabase Storage Setup

```sql
-- Создать bucket для фотографий
INSERT INTO storage.buckets (id, name, public)
VALUES ('tenant-photos', 'tenant-photos', true);

-- RLS policy для публичного доступа
CREATE POLICY "Public Access"
ON storage.objects FOR SELECT
USING (bucket_id = 'tenant-photos');
```

### Структура папок фотографий

```
tenant-photos/
  ├── 1000089271/           (telegram_user_id)
  │   ├── uuid-1/           (property_id)
  │   │   ├── photo_1.jpg
  │   │   └── photo_2.jpg
  │   └── uuid-2/
  │       └── photo_1.jpg
  └── 987654321/
      └── uuid-3/
          └── photo_1.jpg
```

---

## 📊 ОЦЕНКА ВРЕМЕНИ

| Шаг | Описание | Время |
|-----|----------|-------|
| 1 | Утилиты | 15 мин |
| 2 | API Endpoint | 20 мин |
| 3 | Регистрация | 10 мин |
| 4 | Forward парсинг | 15 мин |
| 5 | Сохранение в БД | 15 мин |
| 6 | Загрузка фото | 20 мин |
| 7 | Парсинг описания | 20 мин |
| 8 | Отправка ответа | 15 мин |
| 9 | Личная карта | 30 мин |
| 10 | Тестирование | 30 мин |
| **ИТОГО** | | **~3 часа** |

---

## 🚀 ПОРЯДОК ВЫПОЛНЕНИЯ

### Фаза 1: Ядро (60 минут)
- ✅ ШАГ 1: Утилиты
- ✅ ШАГ 3: Регистрация
- ✅ ШАГ 4: Forward парсинг
- ✅ ШАГ 7: Парсинг описания

### Фаза 2: API (40 минут)
- ✅ ШАГ 2: Telegram Bot API
- ✅ ШАГ 5: Сохранение в БД
- ✅ ШАГ 8: Отправка ответа

### Фаза 3: Медиа (20 минут)
- ✅ ШАГ 6: Загрузка фото

### Фаза 4: Фронтенд (30 минут)
- ✅ ШАГ 9: Личная карта

### Фаза 5: Тестирование (30 минут)
- ✅ ШАГ 10: Все тесты

---

## ✅ КРИТЕРИИ ГОТОВНОСТИ

### Минимум (MVP):
- ✅ Бот принимает forward сообщения
- ✅ Сохраняет в saved_properties
- ✅ Отправляет ссылку на карту
- ✅ Карта показывает объекты

### Полная версия:
- ✅ Загрузка фотографий
- ✅ Парсинг forward метаданных
- ✅ Парсинг описания (цена, тип)
- ✅ Счётчик объектов
- ✅ Удаление объектов

### Будущие улучшения:
- 🔜 Админская мастер-карта
- 🔜 Тепловая визуализация
- 🔜 Фильтры на карте
- 🔜 Экспорт в Excel
- 🔜 Уведомления о новых объектах

---

## 🎯 СЛЕДУЮЩИЙ ШАГ

**Начинаем с Фазы 1 (Ядро)?**

Создаём:
1. `src/lib/tenant-bot-utils.ts` - утилиты
2. `src/lib/tenant-bot-db.ts` - работа с БД
3. `src/lib/telegram-forward-parser.ts` - парсинг forward
4. `src/lib/property-parser.ts` - парсинг описания

**Готовы?** 🚀
