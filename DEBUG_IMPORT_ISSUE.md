# 🐛 Отладка проблемы с кнопкой Import

## Проблема:
При нажатии на кнопку "Import" карта пропадает, появляется серый экран

## Возможные причины:

### 1. JavaScript ошибка в компоненте
**Симптомы:**
- Серый экран
- Модальное окно не открывается
- Ошибка в консоли браузера

**Проверка:**
1. Откройте http://localhost:4332/
2. Нажмите F12 (открыть консоль)
3. Перейдите на вкладку "Console"
4. Нажмите кнопку "Import"
5. Смотрите ошибки в консоли

**Что искать:**
```
❌ Error: Cannot read property 'xxx' of undefined
❌ TypeError: xxx is not a function
❌ SyntaxError: Unexpected token
❌ Failed to compile
```

---

### 2. Проблема с импортами
**Возможная причина:**
- PropertyImporterAI импортирует несуществующую функцию
- Circular dependency
- Missing dependency

**Проверка:**
Смотрите в консоли:
```
❌ Module not found
❌ Cannot find module
❌ Failed to resolve import
```

---

### 3. React rendering error
**Возможная причина:**
- Неправильный JSX
- Missing return statement
- Unclosed tags

**Проверка:**
```
❌ Objects are not valid as a React child
❌ React.createElement: type is invalid
❌ Element type is invalid
```

---

## Как исправить:

### Шаг 1: Откройте консоль браузера
```
F12 → Console
```

### Шаг 2: Попробуйте открыть Import
```
Нажмите кнопку "🤖 Import"
```

### Шаг 3: Скопируйте ошибку
```
Скопируйте ПОЛНЫЙ текст ошибки из консоли
```

### Шаг 4: Отправьте мне ошибку
```
Пришлите текст ошибки для диагностики
```

---

## Быстрые фиксы:

### Если ошибка: "Cannot read property of undefined"
```typescript
// Проблема: state.coordinates is undefined
const lat = state.coordinates.lat; // ❌

// Решение: Optional chaining
const lat = state.coordinates?.lat; // ✅
```

### Если ошибка: "xxx is not a function"
```typescript
// Проблема: функция не импортирована
import { myFunction } from './utils'; // Проверьте путь

// Или функция не экспортирована
export function myFunction() { ... } // Добавьте export
```

### Если ошибка: "Module not found"
```typescript
// Проблема: неправильный путь
import Component from './components/Component'; // ❌

// Решение: проверьте путь
import Component from '../components/Component'; // ✅
```

---

## Временное решение:

### Если ничего не помогает, используйте ручной режим:

1. Нажмите кнопку переключения режима (↻) рядом с Import
2. Переключитесь на ручной режим
3. Используйте старый компонент PropertyImporter (без AI)

---

## Для диагностики мне нужно:

1. **Текст ошибки из консоли** (F12 → Console)
2. **Скриншот серого экрана** (если возможно)
3. **Номер строки ошибки** (из консоли)

**Пример хорошего отчета:**
```
Ошибка: TypeError: Cannot read property 'step' of undefined
Файл: PropertyImporterAI.tsx:245
Стек: 
  at PropertyImporterAI (PropertyImporterAI.tsx:245)
  at Explorer (Explorer.tsx:123)
```

---

## Проверка сейчас:

### Откройте: http://localhost:4332/
### Нажмите F12
### Нажмите Import
### Что вы видите в консоли?

Пришлите мне полный текст ошибки!
