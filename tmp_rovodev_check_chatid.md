# 🔍 ПРОВЕРКА CHAT_ID

## Найдите в логах Vercel:

Откройте лог с отправкой сообщения:
```
19:15:45 POST 200 /api/telegram-webhook
Message send result: { success: true, message_id: 564 }
```

Нажмите на эту строку чтобы развернуть детали.

В детальных логах найдите строку:
```
📤 sendTelegramMessage called: { chatId: "XXXXXXX", ... }
```

**Скопируйте этот CHAT_ID и отправьте мне!**

---

## Проверьте ваш USER ID:

1. Откройте бота: https://t.me/userinfobot
2. Отправьте `/start`
3. Он покажет ваш ID: например `1000089271`

**Сравните:**
- ID из userinfobot: `?`
- chat_id из логов: `?`

**Они совпадают?**

- ✅ **Да** - значит проблема в другом
- ❌ **Нет** - значит сообщения идут не туда!

---

## Быстрый тест:

Выполните в PowerShell:

```powershell
# Замените YOUR_CHAT_ID на ваш реальный ID
$chatId = "1000089271"  # <-- ВАШЕ ЗНАЧЕНИЕ
$botToken = "7958965950:AAFg63FTJ46hcRT6M2wSBzn9RHCrzvfV3q8"
$text = "🧪 Test message from PowerShell"

$body = @{
    chat_id = $chatId
    text = $text
} | ConvertTo-Json

Invoke-RestMethod -Uri "https://api.telegram.org/bot$botToken/sendMessage" -Method Post -Body $body -ContentType "application/json"
```

**Получили сообщение от бота?**

- ✅ **Да** - значит бот работает, проблема в chat_id в коде
- ❌ **Нет** - значит бот заблокирован или удалён диалог
