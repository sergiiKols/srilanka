# ⚠️ АНАЛИЗ: 5-символьные токены

**Дата:** 2026-01-29  
**Вопрос:** Достаточно ли 5 символов для токена?

---

## 🔢 МАТЕМАТИКА БЕЗОПАСНОСТИ

### Варианты 5-символьного токена:

#### Только цифры (0-9):
```
Символов: 10
Комбинаций: 10^5 = 100,000

Примеры:
/map/1000089271/12345
/map/1000089271/98765
/map/1000089271/00001
```

**Время перебора:**
- 100,000 попыток
- При 1 попытке в секунду = 27 часов
- При 10 попытках в секунду = 2.7 часа
- При 100 попытках в секунду = 16 минут

⚠️ **ОПАСНО!** Легко взломать!

---

#### Цифры + буквы нижнего регистра (a-z, 0-9):
```
Символов: 36 (26 букв + 10 цифр)
Комбинаций: 36^5 = 60,466,176

Примеры:
/map/1000089271/a7b3c
/map/1000089271/x5y2z
/map/1000089271/m9k4p
```

**Время перебора:**
- 60 миллионов попыток
- При 100 попытках в секунду = 7 дней
- При 1000 попытках в секунду = 17 часов

⚠️ **РИСКОВАННО!** Можно взломать за день!

---

#### Цифры + буквы обоих регистров (a-z, A-Z, 0-9):
```
Символов: 62 (26 + 26 + 10)
Комбинаций: 62^5 = 916,132,832

Примеры:
/map/1000089271/aB7cD
/map/1000089271/X5yZ2
/map/1000089271/M9kP4
```

**Время перебора:**
- 916 миллионов попыток
- При 100 попытках в секунду = 106 дней
- При 1000 попытках в секунду = 10.6 дней

✅ **ПРИЕМЛЕМО** с Rate Limiting!

---

## 🛡️ С ЗАЩИТОЙ (Rate Limiting)

### Добавляем ограничение: максимум 10 попыток в минуту

```
Комбинаций: 916,132,832
Попыток в минуту: 10
Попыток в день: 14,400
Попыток в год: 5,256,000

Время полного перебора: 174 года!
```

✅ **БЕЗОПАСНО!**

---

## 💡 РЕКОМЕНДАЦИИ

### Вариант 1: 5 символов + защита ✅

```
Токен: 5 символов (a-z, A-Z, 0-9)
Пример: aB7cD

Комбинаций: 916 млн

+ Rate Limiting:
  - Максимум 10 попыток в минуту с одного IP
  - Блокировка на 1 час после 50 неудачных попыток
  - Логирование подозрительной активности

Результат: 174 года на перебор = БЕЗОПАСНО
```

#### Реализация:

```sql
-- Таблица tenants
CREATE TABLE tenants (
  telegram_user_id BIGINT PRIMARY KEY,
  map_secret_token VARCHAR(5) UNIQUE NOT NULL,
  created_at TIMESTAMPTZ
);

-- Функция генерации токена (5 символов)
CREATE OR REPLACE FUNCTION generate_short_token()
RETURNS VARCHAR(5) AS $$
DECLARE
  chars TEXT := 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789';
  result VARCHAR(5) := '';
  i INTEGER;
BEGIN
  FOR i IN 1..5 LOOP
    result := result || substr(chars, floor(random() * length(chars) + 1)::int, 1);
  END LOOP;
  RETURN result;
END;
$$ LANGUAGE plpgsql;

-- Генерация при создании
INSERT INTO tenants (telegram_user_id, map_secret_token)
VALUES (1000089271, generate_short_token());
```

```sql
-- Rate Limiting
CREATE TABLE access_attempts (
  ip_address INET,
  telegram_user_id BIGINT,
  token_attempt VARCHAR(5),
  success BOOLEAN,
  attempted_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_access_attempts_ip ON access_attempts(ip_address, attempted_at);

-- Проверка перед доступом
CREATE OR REPLACE FUNCTION check_rate_limit(p_ip INET)
RETURNS BOOLEAN AS $$
DECLARE
  attempts_count INT;
BEGIN
  -- Считаем попытки за последнюю минуту
  SELECT COUNT(*) INTO attempts_count
  FROM access_attempts
  WHERE ip_address = p_ip
  AND attempted_at > NOW() - INTERVAL '1 minute';
  
  RETURN attempts_count < 10;
END;
$$ LANGUAGE plpgsql;
```

---

### Вариант 2: 6 символов (еще безопаснее) ✅✅

```
Токен: 6 символов (a-z, A-Z, 0-9)
Пример: aB7cDx

Комбинаций: 62^6 = 56,800,235,584 (56 миллиардов!)

Без Rate Limiting:
- При 1000 попыток/сек = 1.8 года

С Rate Limiting (10/мин):
- 10,800 лет на перебор!

Результат: ОЧЕНЬ БЕЗОПАСНО
```

---

### Вариант 3: 8 символов (как у банков) ✅✅✅

```
Токен: 8 символов (a-z, A-Z, 0-9)
Пример: aB7cDx9Y

Комбинаций: 62^8 = 218,340,105,584,896 (218 триллионов!)

С Rate Limiting:
- 415 миллионов лет на перебор!

Результат: БАНКОВСКИЙ УРОВЕНЬ
```

---

## 🎯 МОЯ РЕКОМЕНДАЦИЯ

### Для MVP: **6 символов** (a-z, A-Z, 0-9) + Rate Limiting ⭐⭐⭐

**Почему 6, а не 5:**
```
5 символов: 916 млн комбинаций
6 символов: 56 млрд комбинаций (в 62 раза больше!)

Разница в URL: всего 1 символ
Разница в безопасности: 62x

URL примеры:
5: /map/1000089271/aB7cD      (короче)
6: /map/1000089271/aB7cDx     (безопаснее)
```

**С Rate Limiting (10 попыток/мин):**
- 5 символов: 174 года
- 6 символов: 10,800 лет ✅

**Преимущества 6 символов:**
- ✅ Всё ещё короткий и удобный URL
- ✅ В 62 раза безопаснее чем 5
- ✅ С Rate Limiting = практически невзломаемо
- ✅ Легко запомнить и набрать вручную

---

## 📱 ПРИМЕРЫ URL

### С 5 символами:
```
https://site.com/map/1000089271/aB7cD
                                ^^^^^
                                Токен
```

### С 6 символами:
```
https://site.com/map/1000089271/aB7cDx
                                ^^^^^^
                                Токен
```

### С 8 символами:
```
https://site.com/map/1000089271/aB7cDx9Y
                                ^^^^^^^^
                                Токен
```

**Все варианты короткие и удобные!**

---

## 🔒 ДОПОЛНИТЕЛЬНАЯ ЗАЩИТА

### 1. Rate Limiting (обязательно!)

```javascript
// Middleware на сервере
async function checkRateLimit(ip, userId) {
  const attempts = await getAttemptsInLastMinute(ip);
  
  if (attempts >= 10) {
    return {
      blocked: true,
      message: 'Слишком много попыток. Попробуй через минуту.'
    };
  }
  
  return { blocked: false };
}
```

### 2. Блокировка после неудачных попыток

```sql
-- После 50 неудачных попыток - блокировка на 1 час
CREATE TABLE blocked_ips (
  ip_address INET PRIMARY KEY,
  blocked_until TIMESTAMPTZ,
  reason TEXT
);
```

### 3. Уведомления о подозрительной активности

```javascript
// Если кто-то пытается перебирать токены
if (failedAttempts > 5) {
  bot.sendMessage(userId,
    '⚠️ Предупреждение!\n\n' +
    'Обнаружены попытки несанкционированного доступа к твоей карте.\n' +
    'IP: ' + ip + '\n' +
    'Время: ' + time + '\n\n' +
    'Если это был не ты, рекомендуем сменить токен.',
    {
      reply_markup: {
        inline_keyboard: [[
          { text: '🔄 Сменить токен', callback_data: 'regenerate_token' }
        ]]
      }
    }
  );
}
```

### 4. Автоматическая смена токена

```javascript
// Опция: автоматически менять токен каждые 30 дней
if (tokenAge > 30 days) {
  regenerateToken(userId);
  bot.sendMessage(userId,
    '🔒 Токен твоей карты обновлён для безопасности.\n' +
    'Новая ссылка: ' + newUrl
  );
}
```

---

## 📊 ИТОГОВАЯ ТАБЛИЦА

| Длина | Комбинаций | Без защиты | С Rate Limiting (10/мин) | Рекомендация |
|-------|-----------|------------|--------------------------|--------------|
| 4 символа | 14.7 млн | 4 часа | 2.8 года | ❌ Слабо |
| 5 символов | 916 млн | 10.6 дней | 174 года | ⚠️ Приемлемо |
| **6 символов** | **56 млрд** | **1.8 года** | **10,800 лет** | **✅ Оптимально** |
| 7 символов | 3.5 трлн | 111 лет | 670,000 лет | ✅ Избыточно |
| 8 символов | 218 трлн | 6,900 лет | 41 млн лет | ✅✅ Банковский |

---

## ✅ ФИНАЛЬНОЕ РЕШЕНИЕ

### Рекомендую: **6 символов (a-z, A-Z, 0-9)**

```sql
CREATE TABLE tenants (
  telegram_user_id BIGINT PRIMARY KEY,
  map_secret_token VARCHAR(6) UNIQUE NOT NULL,
  token_created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Генерация токена
CREATE OR REPLACE FUNCTION generate_token_6chars()
RETURNS VARCHAR(6) AS $$
DECLARE
  chars TEXT := 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789';
  result VARCHAR(6) := '';
  i INTEGER;
BEGIN
  FOR i IN 1..6 LOOP
    result := result || substr(chars, floor(random() * length(chars) + 1)::int, 1);
  END LOOP;
  
  -- Проверка уникальности
  WHILE EXISTS (SELECT 1 FROM tenants WHERE map_secret_token = result) LOOP
    result := '';
    FOR i IN 1..6 LOOP
      result := result || substr(chars, floor(random() * length(chars) + 1)::int, 1);
    END LOOP;
  END LOOP;
  
  RETURN result;
END;
$$ LANGUAGE plpgsql;
```

**URL пример:**
```
https://srilanka-37u2.vercel.app/map/1000089271/aB7cDx
```

**Безопасность:**
- ✅ 56 миллиардов комбинаций
- ✅ С Rate Limiting: 10,800 лет на перебор
- ✅ Короткий и удобный URL
- ✅ Легко набрать вручную при необходимости

---

**Согласны на 6 символов вместо 5?** 🔒

Или настаиваете на 5? (тогда обязательно нужен строгий Rate Limiting!)
