# 🛡️ Управление токенами и лимитами API

## 🔢 Система мониторинга токенов

### **Проблема:**
- Google Places API имеет лимиты запросов
- OpenAI/Perplexity токены ограничены
- Нужно остановить парсинг до исчерпания

### **Решение: Multi-layer защита**

---

## 📊 Типы лимитов

### **1. Google Places API лимиты**

#### **Free tier:**
- $200 кредитов в месяц
- Nearby Search: $32 за 1000 запросов
- Place Details: $17 за 1000 запросов

#### **Расчет допустимых запросов:**
```javascript
const GOOGLE_CREDITS = 200; // USD в месяц
const NEARBY_COST = 0.032;  // USD за запрос
const DETAILS_COST = 0.017; // USD за запрос

// Максимум запросов на $200
const MAX_NEARBY = Math.floor(GOOGLE_CREDITS / NEARBY_COST);    // 6,250
const MAX_DETAILS = Math.floor(GOOGLE_CREDITS / DETAILS_COST);  // 11,764
```

#### **Мониторинг в коде:**
```typescript
class GoogleTokenManager {
  private usedCredits = 0;
  private readonly maxCredits = 200;
  private readonly safetyBuffer = 10; // Останавливаемся при $10 остатке
  
  trackRequest(type: 'nearby' | 'details') {
    const cost = type === 'nearby' ? 0.032 : 0.017;
    this.usedCredits += cost;
    
    const remaining = this.maxCredits - this.usedCredits;
    
    // КРИТИЧЕСКАЯ ПРОВЕРКА
    if (remaining < this.safetyBuffer) {
      throw new Error(`
        ⚠️ ОСТАНОВКА: Осталось $${remaining.toFixed(2)} кредитов!
        
        Лимит безопасности: $${this.safetyBuffer}
        Использовано: $${this.usedCredits.toFixed(2)}
        
        🛑 Парсинг остановлен для предотвращения превышения лимита.
        
        Действия:
        1. Дождитесь следующего месяца (обновление $200)
        2. Или добавьте платежный метод в Google Cloud
        3. Текущий прогресс сохранен в progress.json
      `);
    }
    
    // Предупреждение при 20% остатке
    if (remaining < this.maxCredits * 0.2) {
      console.warn(`⚠️ Внимание: Осталось $${remaining.toFixed(2)} (${(remaining/this.maxCredits*100).toFixed(1)}%)`);
    }
  }
  
  getStatus() {
    return {
      used: this.usedCredits,
      remaining: this.maxCredits - this.usedCredits,
      percent: (this.usedCredits / this.maxCredits * 100).toFixed(1)
    };
  }
}
```

---

### **2. Rate limiting (запросов в секунду)**

Google Places API ограничения:
- **Free tier:** 60 запросов в минуту
- **Paid:** До 100,000 запросов в минуту (но дорого)

#### **Защита:**
```typescript
class RateLimiter {
  private requestsThisMinute = 0;
  private minuteStartTime = Date.now();
  private readonly maxRequestsPerMinute = 55; // С запасом
  
  async waitIfNeeded() {
    const now = Date.now();
    const elapsed = now - this.minuteStartTime;
    
    // Сброс счетчика каждую минуту
    if (elapsed >= 60000) {
      this.requestsThisMinute = 0;
      this.minuteStartTime = now;
    }
    
    // Если достигли лимита - ждем
    if (this.requestsThisMinute >= this.maxRequestsPerMinute) {
      const waitTime = 60000 - elapsed;
      console.log(`⏳ Rate limit: ожидание ${Math.ceil(waitTime/1000)}с`);
      await sleep(waitTime);
      this.requestsThisMinute = 0;
      this.minuteStartTime = Date.now();
    }
    
    this.requestsThisMinute++;
  }
}
```

---

## 🔄 Интеграция в скрипт парсинга

### **Пример использования:**

```typescript
// parse_pass_1.ts
import { GoogleTokenManager } from './utils/token-manager';
import { RateLimiter } from './utils/rate-limiter';

const tokenManager = new GoogleTokenManager();
const rateLimiter = new RateLimiter();

async function parseRegion(region: string) {
  try {
    for (const cell of region.cells) {
      // Проверка лимитов ПЕРЕД запросом
      await rateLimiter.waitIfNeeded();
      
      try {
        // Nearby Search запрос
        tokenManager.trackRequest('nearby');
        const nearbyResults = await googlePlaces.nearbySearch(cell);
        
        for (const place of nearbyResults) {
          // Проверка снова для каждого запроса
          await rateLimiter.waitIfNeeded();
          
          // Place Details запрос
          tokenManager.trackRequest('details');
          const details = await googlePlaces.getDetails(place.place_id);
          
          // Сохраняем POI
          await savePOI(details);
        }
        
        // Checkpoint каждые 50 POIs
        if (savedCount % 50 === 0) {
          await saveProgress();
          console.log(`✅ Checkpoint: ${savedCount} POIs, ${tokenManager.getStatus().remaining}$ осталось`);
        }
        
      } catch (error) {
        if (error.message.includes('ОСТАНОВКА')) {
          // Сохраняем прогресс и выходим
          await saveProgress();
          console.log(`
            🛑 ПАРСИНГ ОСТАНОВЛЕН
            
            Причина: Достигнут лимит токенов
            Сохранено POIs: ${savedCount}
            Прогресс: ${progress.json}
            
            Возобновите позже или добавьте кредиты.
          `);
          process.exit(0);
        }
        throw error;
      }
    }
  } catch (error) {
    console.error('Ошибка парсинга:', error);
    await saveProgress();
  }
}
```

---

## 📊 Мониторинг в реальном времени

### **В консоли:**
```
🇱🇰 Sri Lanka POIs Parsing
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Region: Южное побережье
Progress: ████████░░ 80% (36/45 cells)
POIs: 342/450

💰 Бюджет Google Places:
   Использовано: $4.50 / $200 (2.3%)
   Осталось: $195.50 (97.7%)
   Запросов: 150/6,250 (2.4%)
   
⚡ Rate limiting:
   Запросов в минуту: 42/55
   До следующего сброса: 18s

⏱️ Время: 32 минуты
   Ожидается: 8 минут
```

---

## 🚨 Автоматические остановки

### **Триггеры остановки:**

#### **1. Токены близки к лимиту**
```
⚠️ ОСТАНОВКА: Осталось $9.50 кредитов

Лимит безопасности: $10.00
Использовано: $190.50 из $200.00

🛑 Парсинг остановлен автоматически.

Действия:
1. Дождитесь 1-го числа следующего месяца
2. Кредиты обновятся до $200
3. Запустите: npm run parse:continue
```

#### **2. Rate limit превышен**
```
⏳ Rate limit: 60/60 запросов в минуту

Автоматическое ожидание: 60 секунд
Прогресс сохранен: checkpoint_342.json

Продолжение через: 60s... 59s... 58s...
```

#### **3. Слишком много ошибок**
```
🚨 ОСТАНОВКА: Превышен лимит ошибок

Последовательных ошибок: 10
Лимит: 10

Возможные причины:
- API ключ недействителен
- Сетевые проблемы
- Сервис Google недоступен

Прогресс сохранен. Проверьте logs/errors.log
```

---

## 📝 Обновление config.json

### **Добавить секцию limits:**

```json
{
  "limits": {
    "google_places": {
      "monthly_budget_usd": 200,
      "safety_buffer_usd": 10,
      "stop_at_percent": 95,
      "max_requests_per_minute": 55
    },
    
    "parsing": {
      "max_consecutive_errors": 10,
      "auto_stop_on_limit": true,
      "show_budget_warnings": true
    },
    
    "checkpoints": {
      "save_interval": 50,
      "backup_on_stop": true
    }
  }
}
```

---

## 🔄 Возобновление после остановки

### **Команда:**
```bash
npm run parse:continue
```

### **Что произойдет:**
1. Загрузится `progress.json`
2. Проверятся лимиты токенов
3. Если лимиты в порядке - продолжит с последнего checkpoint
4. Если лимиты все еще превышены - покажет когда можно продолжить

### **Пример:**
```
📊 Загрузка прогресса...

Последний checkpoint: 2025-01-22 18:45
Сохранено POIs: 342
Регион: Южное побережье (80% завершено)

💰 Проверка лимитов...
   Google Places: $9.50 / $200 ⚠️ Ниже лимита!
   
🚫 Невозможно продолжить

Причина: Недостаточно кредитов ($9.50 < $10.00 лимит)

Когда продолжить:
- Следующий месяц: 2025-02-01 (через 9 дней)
- Или добавьте платежный метод для увеличения лимита

Текущий прогресс сохранен и безопасен.
```

---

## 📈 Рекомендации по управлению бюджетом

### **Стратегия 1: Распределение по месяцам**
```
Месяц 1 ($200):
  ✅ Проход 1 (береговая линия) - $20
  ✅ Проход 2 (прибрежные города) - $25
  ✅ Проход 3 (пригороды) - $15
  ✅ Проход 4 (внутренние) - $18
  ✅ Проход 5 (центр) - $12
  ✅ Проход 6 (отдаленные) - $10
  
  ИТОГО: $100 (50% бюджета)
  Запас: $100 (на retry и ошибки)
```

### **Стратегия 2: Экономный режим**
```javascript
// В config.json
{
  "data_collection": {
    "collect_photos": false,        // Экономия: не скачивать фото
    "skip_place_details": false,    // Экономия: меньше деталей
    "use_cache": true,              // Экономия: кешировать результаты
    "batch_size": 50                // Экономия: больше за раз
  }
}
```

Экономия: ~30% токенов

---

## ✅ Итоговая защита

### **Многоуровневая система:**

```
Уровень 1: Проверка перед каждым запросом
   ↓ (если OK)
Уровень 2: Rate limiting (60/мин)
   ↓ (если OK)
Уровень 3: Мониторинг бюджета (реал-тайм)
   ↓ (если OK)
Уровень 4: Checkpoint каждые 50 POIs
   ↓ (если OK)
Уровень 5: Автостоп при $10 остатке
   ↓
💾 Безопасное сохранение прогресса
```

**Результат:** Невозможно превысить лимит! 🛡️

---

## 🎯 Практические примеры

### **Пример 1: Нормальная работа**
```
POI 1: ✅ $0.049 (осталось $199.951)
POI 2: ✅ $0.049 (осталось $199.902)
...
POI 342: ✅ $0.049 (осталось $183.242)

Checkpoint saved: 342 POIs
```

### **Пример 2: Приближение к лимиту**
```
POI 3800: ✅ $0.049 (осталось $14.50)
⚠️ Внимание: Осталось $14.50 (7.3%)

POI 3850: ✅ $0.049 (осталось $12.00)
⚠️ Внимание: Осталось $12.00 (6.0%)

POI 3890: ✅ $0.049 (осталось $10.50)
⚠️ КРИТИЧНО: Осталось $10.50 (5.3%)

POI 3891: 🚨 ОСТАНОВКА
💾 Прогресс сохранен: 3891 POIs
```

### **Пример 3: Возобновление**
```
$ npm run parse:continue

📊 Проверка возможности продолжения...
   Дата: 2025-02-01 00:00
   Google кредиты обновлены: $200.00 ✅
   
✅ Можно продолжить!

Загрузка checkpoint: 3891 POIs
Продолжение парсинга...

POI 3892: ✅ $0.049 (осталось $199.951)
```

---

**СТАТУС: Система защиты токенов готова** ✅
**СЛЕДУЮЩИЙ ШАГ: Валидация на Google Maps**
