# 🚨 КРИТИЧЕСКИЕ ИСПРАВЛЕНИЯ ПАРСИНГА - CHANGELOG

**Дата создания:** 2026-01-24  
**Цель:** Журнал критических багов и их исправлений для предотвращения повторения

---

## 📋 ИСТОРИЯ ИСПРАВЛЕНИЙ

### 2026-01-24: Исправление маппинга категорий

#### 🐛 БАГ #1: Culture объединялись с Attraction
**Обнаружено:** 2026-01-24  
**Серьезность:** КРИТИЧЕСКАЯ  
**Затронутые данные:** 1,085 POI (159 POI были неправильно категоризированы)

**Описание проблемы:**
Храмы, музеи и другие культурные объекты маппились в категорию `attraction` вместо отдельной категории `culture`. Это привело к тому, что категория `culture` фактически не существовала как отдельная, хотя планировалась.

**Причина:**
В файле `SRI/scripts/parseNegomboTangalle.js` в функции `determineCategory()` типы `hindu_temple`, `buddhist_temple`, `church`, `mosque`, `temple`, `museum`, `art_gallery`, `zoo`, `aquarium` были маппированы в `'attraction'` вместо `'culture'`.

**БЫЛО (НЕПРАВИЛЬНО):**
```javascript
// 10. Culture & Temples → ОБЪЕДИНЕНО С ATTRACTION
'hindu_temple': 'attraction',
'buddhist_temple': 'attraction',
'church': 'attraction',
'mosque': 'attraction',
'place_of_worship': 'attraction',
'temple': 'attraction',
'historical_landmark': 'attraction',
'museum': 'attraction',
'art_gallery': 'attraction',
'zoo': 'attraction',
'aquarium': 'attraction',
```

**СТАЛО (ПРАВИЛЬНО):**
```javascript
// 10. Culture & Temples (ИСПРАВЛЕНО: отдельная категория culture)
'hindu_temple': 'culture',
'buddhist_temple': 'culture',
'church': 'culture',
'mosque': 'culture',
'place_of_worship': 'culture',
'temple': 'culture',
'historical_landmark': 'culture',
'museum': 'culture',
'art_gallery': 'culture',
'zoo': 'culture',
'aquarium': 'culture',
```

**Решение:**
Изменен маппинг всех культурных типов с `'attraction'` на `'culture'` в файле `SRI/scripts/parseNegomboTangalle.js` (строки 364-374).

**Последствия:**
- После повторного парсинга категория `culture` появится как отдельная
- Категория `attraction` уменьшится (~194 → ~100-140 POI)
- Категория `culture` станет одной из крупнейших (~180-220 POI)

**Как проверить исправление:**
```bash
grep "'hindu_temple':" SRI/scripts/parseNegomboTangalle.js
# Должно быть: 'hindu_temple': 'culture',
```

**Документация обновлена:**
- ✅ `SRI/PARSING_CONDITIONS_UPDATED.md`
- ✅ `SRI/PARSING_CATEGORIES.md`

---

#### 🐛 БАГ #2: Bar маппился в Food вместо Nightlife
**Обнаружено:** 2026-01-24  
**Серьезность:** ВЫСОКАЯ  
**Затронутые данные:** ~10-20 POI были неправильно категоризированы

**Описание проблемы:**
Бары маппились в категорию `food` вместо `nightlife`. Это привело к тому, что категория `nightlife` содержала только ночные клубы, но не бары.

**Причина:**
В файле `SRI/scripts/parseNegomboTangalle.js` в функции `determineCategory()` тип `'bar'` был в секции Food вместо Nightlife.

**БЫЛО (НЕПРАВИЛЬНО):**
```javascript
// Food (приоритет 1 - первый!)
'restaurant': 'food',
'cafe': 'food',
'bar': 'food',  // ❌ НЕПРАВИЛЬНО
'meal_takeaway': 'food',
```

**СТАЛО (ПРАВИЛЬНО):**
```javascript
// Food (приоритет 1 - первый!)
'restaurant': 'food',
'cafe': 'food',
'meal_takeaway': 'food',

// 2b. Nightlife (отдельно от food)
'night_club': 'nightlife',
'nightclub': 'nightlife',
'bar': 'nightlife',  // ✅ ИСПРАВЛЕНО
```

**Решение:**
Удален `'bar': 'food'` из секции Food и добавлен `'bar': 'nightlife'` в секцию Nightlife в файле `SRI/scripts/parseNegomboTangalle.js`.

**Последствия:**
- После повторного парсинга категория `nightlife` увеличится (~17 → ~30-50 POI)
- Категория `food` уменьшится (~258 → ~220-240 POI)
- Бары будут корректно отображаться на карте в фильтре Nightlife

**Как проверить исправление:**
```bash
grep "'bar':" SRI/scripts/parseNegomboTangalle.js
# Должно быть: 'bar': 'nightlife',
# НЕ должно быть: 'bar': 'food',
```

**Документация обновлена:**
- ✅ `SRI/PARSING_CONDITIONS_UPDATED.md`
- ✅ `SRI/PARSING_CATEGORIES.md`

---

#### 🐛 БАГ #3: Beauty_salon блокировался черным списком
**Обнаружено:** 2026-01-24  
**Серьезность:** ВЫСОКАЯ  
**Затронутые данные:** Неизвестное количество салонов красоты не было спарсено

**Описание проблемы:**
`beauty_salon` был одновременно в черном списке (strictBlacklist) и в маппинге spa. Из-за приоритета черного списка все салоны красоты блокировались и не попадали в данные.

**Причина:**
Конфликт между черным списком и маппингом в файле `SRI/scripts/parseNegomboTangalle.js`.

**БЫЛО (НЕПРАВИЛЬНО):**
```javascript
// В черном списке (строка 237):
const strictBlacklist = [
  // ...
  'beauty_salon',  // ❌ БЛОКИРОВАЛ все салоны
  // ...
];

// В маппинге (строка 331):
'beauty_salon': 'spa',  // Не работало из-за блокировки выше
```

**СТАЛО (ПРАВИЛЬНО):**
```javascript
// В черном списке (строка 237):
const strictBlacklist = [
  // ...
  'parking',
  // 'beauty_salon' УДАЛЕНО из блэк-листа - теперь маппится в spa
  'dentist',
  // ...
];

// В маппинге (строка 332):
'beauty_salon': 'spa',  // ✅ Теперь работает
```

**Решение:**
Удален `'beauty_salon'` из черного списка (strictBlacklist) в файле `SRI/scripts/parseNegomboTangalle.js` (строка 237).

**Последствия:**
- После повторного парсинга категория `spa` увеличится (~25 → ~40-60 POI)
- Салоны красоты будут найдены и добавлены в данные

**Как проверить исправление:**
```bash
grep "beauty_salon" SRI/scripts/parseNegomboTangalle.js
# Должен быть закомментирован в черном списке
# Должен быть активным в маппинге: 'beauty_salon': 'spa',
```

**Документация обновлена:**
- ✅ `SRI/PARSING_CONDITIONS_UPDATED.md`
- ✅ `SRI/PARSING_CATEGORIES.md`

---

#### ✅ ДОПОЛНИТЕЛЬНО: Добавлены 9 поисковых типов
**Дата:** 2026-01-24  
**Серьезность:** СРЕДНЯЯ  
**Цель:** Найти 7 отсутствующих категорий (beach, pharmacy, supermarket, yoga, diving, surf, liquor)

**Описание:**
В конфигурацию `CONFIG.CATEGORIES` добавлены дополнительные типы для поиска, чтобы увеличить покрытие отсутствующих категорий.

**Добавленные типы:**
1. `'diving_center'` → для категории `diving`
2. `'surf_school'` → для категории `surf`
3. `'drugstore'` → для категории `pharmacy` (альтернативный тип)
4. `'doctor'` → для категории `hospital` (дополнительно)
5. `'convenience_store'` → для категории `supermarket`
6. `'beauty_salon'` → для категории `spa` (теперь ищется активно)
7. `'bus_stop'` → для категории `bus` (дополнительно)
8. `'zoo'` → для категории `culture`
9. `'park'` → для категории `attraction`

**Файл:** `SRI/scripts/parseNegomboTangalle.js` (строки 57-103)

**Последствия:**
- Появятся 7 новых категорий: beach, pharmacy, supermarket, yoga, diving, surf, liquor
- Общее количество POI увеличится на 200-400

**Документация обновлена:**
- ✅ `SRI/PARSING_CONDITIONS_UPDATED.md`

---

## 🎯 ПРАВИЛА ДЛЯ БУДУЩИХ ПАРСИНГОВ

### ✅ ОБЯЗАТЕЛЬНЫЕ ПРАВИЛА МАППИНГА:

#### 1. Culture ≠ Attraction
- **Culture:** храмы, музеи, галереи, зоопарки, аквариумы
- **Attraction:** туристические достопримечательности, парки развлечений, аттракционы
- ❌ **НЕ ОБЪЕДИНЯТЬ ИХ!**

```javascript
// ✅ ПРАВИЛЬНО:
'temple': 'culture',
'museum': 'culture',
'tourist_attraction': 'attraction',
'amusement_park': 'attraction',

// ❌ НЕПРАВИЛЬНО:
'temple': 'attraction',  // НЕТ!
```

#### 2. Nightlife ≠ Food
- **Nightlife:** ночные клубы, бары
- **Food:** рестораны, кафе, точки питания
- ❌ **НЕ МАППИТЬ БАРЫ В FOOD!**

```javascript
// ✅ ПРАВИЛЬНО:
'bar': 'nightlife',
'restaurant': 'food',

// ❌ НЕПРАВИЛЬНО:
'bar': 'food',  // НЕТ!
```

#### 3. Beauty_salon → Spa (НЕ блокировать)
- `beauty_salon` должен маппиться в `spa`
- ❌ **НЕ ДОБАВЛЯТЬ В ЧЕРНЫЙ СПИСОК!**

```javascript
// ✅ ПРАВИЛЬНО:
// beauty_salon НЕ в strictBlacklist
'beauty_salon': 'spa',  // В маппинге

// ❌ НЕПРАВИЛЬНО:
// strictBlacklist: ['beauty_salon']  // НЕТ!
```

#### 4. Порядок проверки важен
- Специфичные типы (temple, museum) должны проверяться ПЕРЕД общими (point_of_interest)
- Food должен проверяться ПЕРЕД attraction (чтобы бары-рестораны попали в food)

---

## 📝 ЧЕКЛИСТ ПЕРЕД ПАРСИНГОМ

Перед запуском парсинга ВСЕГДА проверяйте:

- [ ] `'temple'` маппится в `'culture'` (НЕ `'attraction'`)
- [ ] `'museum'` маппится в `'culture'` (НЕ `'attraction'`)
- [ ] `'bar'` маппится в `'nightlife'` (НЕ `'food'`)
- [ ] `'beauty_salon'` НЕ в strictBlacklist
- [ ] `'beauty_salon'` маппится в `'spa'`
- [ ] Все 16 категорий имеют поисковые типы в CONFIG.CATEGORIES
- [ ] Порядок проверки типов корректный (специфичные перед общими)

---

## 🔍 КОМАНДЫ ДЛЯ ПРОВЕРКИ

### Проверка маппинга категорий:
```bash
# Проверить culture
grep "'temple':" SRI/scripts/parseNegomboTangalle.js
grep "'museum':" SRI/scripts/parseNegomboTangalle.js

# Проверить nightlife
grep "'bar':" SRI/scripts/parseNegomboTangalle.js

# Проверить spa
grep "'beauty_salon':" SRI/scripts/parseNegomboTangalle.js

# Проверить черный список
grep -A 30 "strictBlacklist" SRI/scripts/parseNegomboTangalle.js | grep "beauty_salon"
```

### После парсинга - проверка результата:
```bash
node -e "
const data = require('./SRI/parsed_data/negombo_tangalle/pass_1_0-1km.json');
const categories = {};
data.forEach(poi => {
  categories[poi.category] = (categories[poi.category] || 0) + 1;
});

console.log('Categories found:');
Object.entries(categories).sort((a,b) => b[1]-a[1]).forEach(([cat, count]) => {
  console.log(\`  \${cat}: \${count}\`);
});

// Проверка критических категорий
const critical = ['culture', 'nightlife', 'spa', 'beach', 'pharmacy', 'supermarket'];
console.log('\\nCritical categories:');
critical.forEach(cat => {
  const count = categories[cat] || 0;
  const status = count > 0 ? '✅' : '❌';
  console.log(\`  \${status} \${cat}: \${count}\`);
});
"
```

---

## 📚 СВЯЗАННЫЕ ДОКУМЕНТЫ

- `SRI/PARSING_CONDITIONS_UPDATED.md` - Полные условия парсинга
- `SRI/PARSING_CATEGORIES.md` - Маппинг категорий
- `SRI/scripts/parseNegomboTangalle.js` - Код парсинга
- `tmp_rovodev_CATEGORY_FIXES_SUMMARY.md` - Детальное описание исправлений (временный)
- `tmp_rovodev_FINAL_REPORT.md` - Итоговый отчет (временный)

---

## ✅ СТАТУС

**Последнее обновление:** 2026-01-24  
**Автор исправлений:** Rovo Dev AI  
**Статус кода:** ✅ Все исправления применены  
**Статус документации:** ✅ Вся документация обновлена  
**Готовность к парсингу:** ✅ ГОТОВО

---

## 📞 СЛЕДУЮЩИЕ ШАГИ

1. ✅ Код исправлен
2. ✅ Документация обновлена
3. ⏳ Запустить повторный парсинг: `node SRI/scripts/parseNegomboTangalle.js --pass=1`
4. ⏳ Проверить результаты (см. команды выше)
5. ⏳ Обновить отчеты о прогрессе

---

**ВАЖНО:** Этот файл содержит критически важную информацию. НЕ УДАЛЯТЬ!  
При любых изменениях в логике парсинга обновляйте этот файл.
