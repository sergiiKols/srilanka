# 🗺️ Анализ слоев LEAFLET для парсинга POI

## 📊 Текущая архитектура карты

### 1. TileLayer (Базовая карта)
- **Источник:** OpenStreetMap (OSM)
- **URL:** `https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png`
- **Покрытие:** Весь мир
- **Zoom уровни:** 0-19
  - z=0 (весь мир)
  - z=8 (текущие настройки - Шри-Ланка)
  - z=15+ (улицы и детали)

### 2. Маркеры (Наши POI)
- **Источник:** JSON файл парсинга
- **Загрузка:** `/SRI/parsed_data/negombo_tangalle/pass_1_0-1km.json`
- **Отображение:** 40+ категорий с emoji icons
- **Региональное ограничение:** Negombo-Tangalle (6.1°N - 6.9°N, 80.0°E - 81.3°E)

---

## ✅ Соответствие категорий парсинга слоям карты

### Полное соответствие (40+ категорий поддерживаются)

| Emoji | Категория | Google Types | Статус |
|-------|-----------|-------------|--------|
| 🍽️ | food | restaurant, cafe, bar, meal_delivery | ✅ |
| 🏖️ | beach | beach, water_sports, surf, diving | ✅ |
| 💊 | pharmacy | pharmacy | ✅ |
| 🛒 | supermarket | supermarket, liquor_store | ✅ |
| 🏥 | hospital | hospital, clinic | ✅ |
| 💆 | spa | spa, yoga | ✅ |
| 🏧 | atm | atm, bank | ✅ |
| 🛺 | tuktuk | tuktuk, taxi | ✅ |
| 🚌 | bus | bus_station | ✅ |
| ⭐ | attraction | tourist_attraction, amusement_park | ✅ |
| 🏛️ | culture | temple, church, mosque, museum, zoo | ✅ |
| 🎉 | nightlife | night_club | ✅ |
| 🏄 | surf | surf_school | ✅ |
| 🧘 | yoga | yoga_studio | ✅ |
| 🤿 | diving | diving_center | ✅ |
| 🍷 | liquor | liquor_store | ✅ |
| ✂️ | barber | barber | ✅ |
| 🧺 | laundry | laundry | ✅ |
| 💻 | coworking | coworking_space | ✅ |

---

## 🎯 Как загружать слои для конкретного региона

### Метод 1: Парсинг ограничивает регион (ТЕКУЩИЙ)

**Преимущества:**
- ✅ Только данные нужного региона в JSON
- ✅ Маленький размер файла (1-2 MB)
- ✅ Быстрая загрузка карты
- ✅ Меньше запросов API

**Недостатки:**
- ❌ Нельзя зумить дальше региона
- ❌ Разные JSON для разных регионов

**Реализация:**
```javascript
// В parseNegomboTangalle.js
const NEGOMBO_TANGALLE_BOUNDS = {
  north: 6.9,   // Негомбо (северная граница)
  south: 6.1,   // Тангалла (южная граница)
  east: 81.3,
  west: 80.0
};

// Фильтр перед парсингом
if (lat > NEGOMBO_TANGALLE_BOUNDS.north || 
    lat < NEGOMBO_TANGALLE_BOUNDS.south) {
  continue; // пропустить
}
```

### Метод 2: Карта фильтрует видимые маркеры

**Преимущества:**
- ✅ Динамическая фильтрация
- ✅ Пользователь видит что ему нужно

**Недостатки:**
- ❌ Все данные нужны в памяти
- ❌ Медленнее при большом количестве маркеров

**Реализация:**
```javascript
// В Explorer.tsx
const visibleMarkers = allMarkers.filter(marker => {
  const inBounds = marker.position[0] >= 6.1 && 
                   marker.position[0] <= 6.9 &&
                   marker.position[1] >= 80.0 && 
                   marker.position[1] <= 81.3;
  return inBounds && activeLayers.includes(marker.type);
});
```

### Метод 3: Multi-Region с несколькими JSON (РЕКОМЕНДУЕТСЯ)

**Преимущества:**
- ✅ Масштабируемость
- ✅ Модульность
- ✅ Может быть расширено на весь остров
- ✅ Оптимальное использование памяти

**Структура:**
```
SRI/parsed_data/
├── negombo_tangalle/
│   └── pass_1_0-1km.json (Negombo-Tangalle)
├── colombo_area/
│   └── pass_2_1-2km.json (Colombo region)
├── galle_area/
│   └── pass_3_2-3km.json (Galle region)
└── kandy_area/
    └── pass_4_3-3.5km.json (Kandy region)
```

**Реализация в Map.tsx:**
```javascript
const [regionData, setRegionData] = useState<POI[]>([]);

useEffect(() => {
  // Определяем текущий регион на основе zoom/bounds
  const region = detectRegion(map.getCenter(), map.getZoom());
  
  // Загружаем нужный JSON
  fetch(`/SRI/parsed_data/${region}/pass_*.json`)
    .then(r => r.json())
    .then(data => setRegionData(data));
}, [map]);
```

---

## 🌍 Координаты регионов Шри-Ланки

### Negombo-Tangalle (ТЕКУЩИЙ РЕГИОН)
```
NW Corner (Negombo): 6.9°N, 80.0°E
SE Corner (Tangalle): 6.1°N, 81.3°E
Центр: ~6.5°N, 80.65°E
```

### Возможные расширения
```
Colombo Area:       6.9°N - 7.3°N, 79.8°E - 80.3°E
Kandy Area:         6.9°N - 7.5°N, 80.6°E - 81.0°E
Galle Area:         5.9°N - 6.1°N, 80.2°E - 80.5°E
North Area:         7.3°N - 8.0°N, 80.3°E - 81.5°E
```

---

## 📊 Поток данных: От парсинга к карте

```
1. ПАРСИНГ
   ├─ Google Places API ищет 29 локаций
   ├─ Фильтрует по региону (Negombo-Tangalle)
   ├─ Фильтрует по категориям (исключает отели)
   └─ Сохраняет → /SRI/parsed_data/negombo_tangalle/pass_1_0-1km.json

2. JSON СТРУКТУРА
   ├─ Array of POI objects
   ├─ Каждый объект:
   │  ├─ id, name, coordinates
   │  ├─ category (food, beach, pharmacy, etc)
   │  ├─ address, phone, website
   │  ├─ hours, rating, mainPhoto
   │  └─ types (Google Types)
   └─ Копируется → /public/SRI/parsed_data/...

3. REACT ЗАГРУЗКА
   ├─ Explorer.tsx: fetch('/SRI/parsed_data/...')
   ├─ Парсит JSON
   ├─ Фильтрует по activeLayers (Filters)
   └─ Отправляет маркеры → Map.tsx

4. LEAFLET ОТОБРАЖЕНИЕ
   ├─ Map.tsx получает маркеры
   ├─ TileLayer рисует OpenStreetMap
   ├─ Каждый маркер = точка на карте
   ├─ onClick = показывает Popup
   └─ Popup = информация POI (адрес, телефон, часы, фото)

5. ФИЛЬТРАЦИЯ
   ├─ Filters компонент управляет activeLayers
   ├─ filteredMarkers = фильтруются по типу
   └─ Карта обновляется в реальном времени
```

---

## 🔍 Проблемы и решения

### Проблема 1: Неправильная категоризация
**Симптом:** "Love Bar" отображается как attraction вместо food

**Причина:** Порядок проверки типов в parsingType mapping

**Решение:**
```javascript
// Проверяем food types ПЕРЕД generic point_of_interest
if (types.includes('bar') || types.includes('restaurant')) {
  return 'food'; // ✅ Проверка ПЕРВОЙ
}
// ...остальные категории...
if (types.includes('point_of_interest')) {
  return 'attraction'; // ✅ Проверка ПОСЛЕДНЕЙ (fallback)
}
```

### Проблема 2: Отели попадают в данные
**Симптом:** "Villa Primrose" - это гостевой дом, а не POI

**Причина:** Google не маркирует Airbnb как 'lodging'

**Решение:**
```javascript
// Добавить фильтрацию по названию
const lodgingKeywords = ['villa', 'cottage', 'resort', 'airbnb', 'homestay'];
if (lodgingKeywords.some(kw => name.toLowerCase().includes(kw))) {
  return null; // Пропустить
}
```

### Проблема 3: Недостаток данных (фото, веб-сайт, часы)
**Симптом:** Поля пусты для большинства POI

**Причина:** Нужны дополнительные API запросы Place Details

**Решение:**
```javascript
// Для полной информации нужно:
// 1. Каждому POI делать дополнительный запрос Place Details
// 2. Это увеличит затраты API (~$0.005 за запрос)
// 3. Увеличит время парсинга в 2-3 раза

// Рекомендуется использовать локальные источники данных:
// - Wikidata
// - Собственная база данных
// - User-contributed data
```

---

## 💡 Рекомендации по улучшению

### Краткосрочные (текущий стек)
1. ✅ **Исправить порядок проверки типов** в парсинге
2. ✅ **Добавить фильтрацию по названию** для отелей
3. ✅ **Добавить переключатель карт** (OpenStreetMap, Satellite, Topographic)
4. ✅ **Добавить геолокацию пользователя** с ориентиром на карту

### Среднесрочные (расширение)
1. 🔄 **Multi-region support** (разные регионы)
2. 🔄 **Кластеризация маркеров** при zoom out
3. 🔄 **Heat map слой** для популярности регионов
4. 🔄 **Search по названию/адресу**

### Долгосрочные (полная система)
1. 📊 **WebGL rendering** для 10000+ маркеров
2. 📊 **Backend API** для динамической загрузки
3. 📊 **User reviews layer** (составной слой)
4. 📊 **Real-time update** система

---

## 🎓 Заключение

**Категории карты ИДЕАЛЬНО соответствуют нашему парсингу!** ✅

- 40+ категорий в Map.tsx = 40+ типов данных из Google Places
- Каждая категория имеет собственный emoji и стиль
- Фильтры работают корректно
- JSON структура оптимальна

**Текущие проблемы - это проблемы парсинга, не карты:**
- Неправильная категоризация (bar → attraction) - ФИХ в порядке проверки
- Отели не фильтруются (villa) - ФИХ добавить фильтр по названию
- Недостаток данных (фото, часы) - ФИХ требует дополнительных API запросов

**Рекомендуемый подход:**
1. Исправить парсинг (порядок типов, фильтр названий)
2. Добавить Place Details API для полной информации
3. Расширить на Multi-Region по мере необходимости
4. Добавить переключатель TileLayer (разные карты)
