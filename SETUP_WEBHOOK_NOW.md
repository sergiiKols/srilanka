# 🤖 НАСТРОЙКА TELEGRAM WEBHOOK - ФИНАЛ

**Статус:** Деплой #2 успешен ✅  
**Осталось:** Настроить webhook и протестировать

---

## 📋 ШАГ 1: УЗНАТЬ URL ВАШЕГО САЙТА

Откройте Vercel Dashboard и скопируйте URL:

```
Например: https://srilanka-37u2.vercel.app
```

---

## 🔗 ШАГ 2: НАСТРОИТЬ WEBHOOK

### **ВАРИАНТ A: PowerShell (Windows)** ⭐ Рекомендую

Замените `YOUR-SITE` на ваш URL и выполните:

```powershell
$botToken = "7958965950:AAFg63FTJ46hcRT6M2wSBzn9RHCrzvfV3q8"
$webhookUrl = "https://YOUR-SITE.vercel.app/api/telegram-webhook"

Write-Output "🔗 Настройка webhook..."
Write-Output "Bot Token: $botToken"
Write-Output "Webhook URL: $webhookUrl"
Write-Output ""

$response = Invoke-RestMethod -Uri "https://api.telegram.org/bot$botToken/setWebhook" `
  -Method Post `
  -ContentType "application/json" `
  -Body (@{url = $webhookUrl} | ConvertTo-Json)

Write-Output "📊 Результат:"
$response | ConvertTo-Json -Depth 10
```

---

### **ВАРИАНТ B: Браузер**

Откройте в браузере (замените YOUR-SITE):

```
https://api.telegram.org/bot7958965950:AAFg63FTJ46hcRT6M2wSBzn9RHCrzvfV3q8/setWebhook?url=https://YOUR-SITE.vercel.app/api/telegram-webhook
```

---

### **✅ ОЖИДАЕМЫЙ ОТВЕТ:**

```json
{
  "ok": true,
  "result": true,
  "description": "Webhook was set"
}
```

Если видите это - ВСЁ ГОТОВО! ✅

---

## ✅ ШАГ 3: ПРОВЕРИТЬ WEBHOOK

Выполните:

```powershell
$botToken = "7958965950:AAFg63FTJ46hcRT6M2wSBzn9RHCrzvfV3q8"

Invoke-RestMethod -Uri "https://api.telegram.org/bot$botToken/getWebhookInfo"
```

### **✅ ДОЛЖНО ВЕРНУТЬ:**

```json
{
  "ok": true,
  "result": {
    "url": "https://YOUR-SITE.vercel.app/api/telegram-webhook",
    "has_custom_certificate": false,
    "pending_update_count": 0,
    "last_error_date": 0,
    "max_connections": 40
  }
}
```

**Важно проверить:**
- ✅ `url` правильный
- ✅ `pending_update_count: 0` (нет ошибок)
- ✅ `last_error_date: 0` (не было ошибок)

---

## 🧪 ШАГ 4: ТЕСТИРОВАНИЕ

### **ТЕСТ 1: Команда /start** ⭐ Главный тест

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте: `/start`

**✅ Ожидаемый ответ (через 1-2 секунды):**

```
👋 Привет! Я твоя личная записная книжка для объектов недвижимости!

📝 Как работает:
1. Нашёл объявление? Переслай мне!
2. Я автоматически сохраню на карте
3. Все объекты в одном месте

🗺️ Твоя карта:
https://your-site.vercel.app/map/personal/YOUR_ID/TOKEN

[🗺️ Моя карта] [❓ Помощь]
```

**❌ Если бот НЕ отвечает:**
- Проверьте webhook (getWebhookInfo)
- Проверьте логи в Vercel (Functions → /api/telegram-webhook)
- Убедитесь что endpoint доступен

---

### **ТЕСТ 2: Forward сообщения с фото**

1. Найдите любое объявление с фото
2. Нажмите Forward
3. Выберите вашего бота
4. Отправьте

**✅ Ожидаемый ответ (через 2-3 секунды):**

```
✅ Объект сохранён! (всего: 1)

🏠 Studio, $500/месяц
📍 Негомбо
📸 2 фотографии

🗺️ Твоя карта:
https://your-site.vercel.app/map/personal/...

💡 Пересылай сюда объявления - они автоматически добавятся на карту!

[🗺️ Открыть карту] [⭐ В избранное]
[✏️ Заметка] [🗑️ Удалить]
```

---

### **ТЕСТ 3: Проверка БД** 💾

Откройте Supabase Dashboard:

```sql
-- Проверить что tenant создался
SELECT 
  telegram_user_id,
  map_secret_token,
  personal_map_url,
  saved_properties_count,
  created_at
FROM tenants 
ORDER BY created_at DESC 
LIMIT 5;
```

**✅ Должна быть запись с:**
- Вашим telegram_user_id
- Токеном (6 символов)
- URL карты
- Счётчиком = 1

---

```sql
-- Проверить что объект сохранился
SELECT 
  id,
  title,
  property_type,
  price,
  latitude,
  longitude,
  photos,
  source_type,
  created_at
FROM saved_properties 
ORDER BY created_at DESC 
LIMIT 5;
```

**✅ Должна быть запись с:**
- Данными объекта
- Координатами
- Массивом фото (если были)
- source_type (direct или forward_channel)

---

### **ТЕСТ 4: Проверка Storage** 📸

Supabase Dashboard → Storage → tenant-photos

**✅ Должна быть структура:**

```
tenant-photos/
  └── {ваш telegram_user_id}/
      └── {uuid объекта}/
          ├── photo_1.jpg
          └── photo_2.jpg
```

---

### **ТЕСТ 5: Админская карта** 🗺️

Откройте в браузере:

```
https://your-site.vercel.app/admin/master-map
```

**✅ Должны видеть:**
- Карту Шри-Ланки
- Панель управления слева
- Статистику:
  - 🔵 POI: 0 (или количество из таблицы pois)
  - 🔴 Properties: 1
  - 👤 Users: 1
- Красный маркер на карте (ваш объект)
- При клике на маркер - попап с деталями

---

## 🐛 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

### **Проблема: Бот не отвечает на /start**

**Проверить:**

1. **Webhook настроен?**
```powershell
$botToken = "7958965950:AAFg63FTJ46hcRT6M2wSBzn9RHCrzvfV3q8"
Invoke-RestMethod -Uri "https://api.telegram.org/bot$botToken/getWebhookInfo"
```

Если `url` пустой или неправильный → повторить setWebhook

2. **Endpoint доступен?**
```powershell
Invoke-RestMethod -Uri "https://your-site.vercel.app/api/telegram-webhook" -Method Post
```

Должен вернуть "OK" или "Error" (не 404)

3. **Логи в Vercel:**
- Dashboard → Deployments → Latest
- Functions → найти `/api/telegram-webhook`
- Смотреть ошибки в реальном времени

4. **Environment Variables:**
- Settings → Environment Variables
- Проверить что TELEGRAM_BOT_TOKEN есть
- Если добавляли переменные → Redeploy!

---

### **Проблема: Объект не сохраняется**

**Проверить:**

1. **Логи бота в Vercel:**
   - Найти ошибки типа "Failed to save property"
   - Проверить что Supabase доступна

2. **Supabase Service Role Key:**
   - Environment Variables → SUPABASE_SERVICE_ROLE_KEY
   - Должен быть правильный ключ

3. **Таблица saved_properties:**
```sql
-- Проверить что таблица существует
SELECT * FROM saved_properties LIMIT 1;
```

---

### **Проблема: Фото не загружаются**

**Проверить:**

1. **Storage bucket:**
```sql
SELECT * FROM storage.buckets WHERE name = 'tenant-photos';
```

Если нет → выполнить `database/04_create_storage_bucket.sql`

2. **RLS policies:**
```sql
SELECT policyname FROM pg_policies 
WHERE tablename = 'objects' 
AND policyname LIKE '%tenant%';
```

Должно быть минимум 2 policy

---

### **Проблема: AI не работает**

**Проверить:**

1. **GROQ_API_KEY в Vercel:**
   - Settings → Environment Variables
   - Проверить что ключ правильный

2. **В логах:**
   - Искать "AI analysis failed"
   - Если есть → проверить ключи AI сервисов

---

## ✅ ЧЕК-ЛИСТ ФИНАЛЬНОГО ТЕСТИРОВАНИЯ

```
□ Webhook настроен (setWebhook выполнен)
□ getWebhookInfo показывает правильный URL
□ /start команда работает
□ Бот отвечает через 1-2 секунды
□ Forward сообщения работает
□ Объект сохранился в БД (saved_properties)
□ Tenant создался в БД (tenants)
□ Счётчик обновился (saved_properties_count = 1)
□ Фото загрузилось в Storage
□ Админская карта открывается
□ Админская карта показывает объект
□ Статистика правильная (1 property, 1 user)
```

---

## 🎉 ПОСЛЕ УСПЕШНОГО ТЕСТИРОВАНИЯ

**ВСЁ РАБОТАЕТ?** ПОЗДРАВЛЯЮ! 🎊

### **Что у вас теперь есть:**

✅ **Telegram Bot:**
- AI анализ описаний
- Загрузка фото
- 2 режима работы (быстрый/пошаговый)
- Команды и кнопки

✅ **Админская карта:**
- Показывает все объекты клиентов
- Real-time обновления
- Тепловая карта
- Фильтры и статистика

✅ **База данных:**
- Tenants с уникальными токенами
- Saved properties с полными данными
- Auto-increment счётчики

---

### **Осталось (опционально):**

⏹️ **Личная карта клиента** `/map/personal/[userId]/[token]`
- Пока клиент получает 404 при открытии ссылки
- Можно создать за 30 минут
- Или оставить на потом

---

## 🚀 СИСТЕМА ГОТОВА К ИСПОЛЬЗОВАНИЮ!

**Можете:**
- Тестировать бота
- Добавлять объекты
- Смотреть статистику на админке
- Делиться ботом с клиентами

---

Удачи! 🎉
