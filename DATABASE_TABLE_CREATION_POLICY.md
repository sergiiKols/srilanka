# 📋 ПОЛИТИКА СОЗДАНИЯ ТАБЛИЦ В SUPABASE

**Дата:** 2026-01-29  
**Статус:** Обязательная политика для всех новых таблиц  
**Цель:** Автоматическая интеграция таблиц в системы администрирования и мониторинга

---

## 🎯 ОБЯЗАТЕЛЬНЫЙ ЧЕК-ЛИСТ

### ✅ При создании ЛЮБОЙ новой таблицы ОБЯЗАТЕЛЬНО выполнить:

```
□ 1. Создать SQL скрипт в папке database/
□ 2. Добавить таблицу в админку (API + компонент)
□ 3. Добавить таблицу в keep-alive систему
□ 4. Настроить RLS policies (если нужно)
□ 5. Создать тестовые данные
□ 6. Проверить отображение в админке
□ 7. Документировать в README
```

---

## 📂 ШАГ 1: SQL СКРИПТ (database/)

### Структура файлов:

```
database/
├── README.md
├── 01_create_table_name.sql      ← Основная таблица
├── 02_create_indexes.sql         ← Индексы (если много)
├── 03_create_triggers.sql        ← Триггеры (если есть)
├── 04_test_data.sql              ← Тестовые данные
└── policy_example.sql            ← Пример для будущих таблиц
```

### Шаблон SQL скрипта:

```sql
-- ============================================
-- ТАБЛИЦА: {table_name}
-- Описание: {что делает таблица}
-- Категория: {tenant_system / superbase_crm / telegram_listing}
-- ============================================

CREATE TABLE IF NOT EXISTS {table_name} (
  -- ОБЯЗАТЕЛЬНЫЕ ПОЛЯ (для всех таблиц):
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW(),
  
  -- СПЕЦИФИЧНЫЕ ПОЛЯ:
  -- ваши поля здесь
);

-- КОММЕНТАРИИ (для документации)
COMMENT ON TABLE {table_name} IS 'Описание таблицы';
COMMENT ON COLUMN {table_name}.id IS 'Уникальный идентификатор';

-- ИНДЕКСЫ (минимум 1 для основного поиска)
CREATE INDEX IF NOT EXISTS idx_{table_name}_created ON {table_name}(created_at DESC);

-- ТРИГГЕР для updated_at (если нужно)
CREATE TRIGGER update_{table_name}_updated_at
  BEFORE UPDATE ON {table_name}
  FOR EACH ROW
  EXECUTE FUNCTION update_updated_at_column();
```

---

## 🎨 ШАГ 2: АДМИНКА (обязательно!)

### Файл 1: `src/pages/api/admin/tables.ts`

Добавить в массив `knownTables`:

```typescript
const knownTables = [
  // ... существующие таблицы
  
  // НОВАЯ ТАБЛИЦА
  { 
    name: '{table_name}',
    size: '48 kB',
    columns: {количество_колонок},
    description: '{описание таблицы}',
    category: '{категория}'
  },
];
```

### Файл 2: `src/components/admin/DatabaseTables.tsx`

Добавить в `fallbackTablesData`:

```typescript
const fallbackTablesData: TableInfo[] = [
  // ... существующие таблицы
  
  // НОВАЯ ТАБЛИЦА
  { 
    name: '{table_name}',
    size: '48 kB',
    columns: {количество},
    description: '{описание}',
    category: '{категория}'
  },
];
```

---

## 🔄 ШАГ 3: KEEP-ALIVE СИСТЕМА (обязательно!)

### SQL для добавления в keep-alive:

```sql
INSERT INTO keep_alive_config (table_name, is_enabled, priority, required_fields, notes) VALUES
('{table_name}', true, {priority}, '{
  "required_field_1": "default_value",
  "required_field_2": "%%PLACEHOLDER%%"
}', '{описание таблицы}')

ON CONFLICT (table_name) DO UPDATE SET
  is_enabled = EXCLUDED.is_enabled,
  priority = EXCLUDED.priority,
  required_fields = EXCLUDED.required_fields,
  notes = EXCLUDED.notes,
  updated_at = NOW();
```

### Приоритеты:

```
Priority 1: Критичные системные таблицы
Priority 2: Независимые основные таблицы (без FK)
Priority 3: Таблицы с FK (зависят от других)
Priority 50: Некритичные таблицы
Priority 99: Отключенные (логи, временные)
```

### Плейсхолдеры для тестовых данных:

```
%%UUID%%       → случайный UUID
%%RANDOM%%     → случайное число (0-999999)
%%RANDOM8%%    → 8-значное число
%%TIMESTAMP%%  → Unix timestamp
%%DATE%%       → текущая дата
%%NOW%%        → текущее время
%%FUTUREDATE%% → дата через 30 дней
```

---

## 🔒 ШАГ 4: RLS POLICIES (если нужны)

### Когда нужны RLS:

- ✅ Таблица содержит пользовательские данные
- ✅ Нужен контроль доступа (кто может читать/писать)
- ❌ Системные таблицы (обычно не нужны)

### Шаблон RLS policies:

```sql
-- Включить RLS
ALTER TABLE {table_name} ENABLE ROW LEVEL SECURITY;

-- Политика: пользователи видят только свои записи
CREATE POLICY "Users can view own records"
ON {table_name}
FOR SELECT
USING (auth.uid() = user_id);

-- Политика: пользователи могут создавать свои записи
CREATE POLICY "Users can insert own records"
ON {table_name}
FOR INSERT
WITH CHECK (auth.uid() = user_id);

-- Политика: админы видят всё
CREATE POLICY "Admins can view all"
ON {table_name}
FOR SELECT
USING (auth.jwt() ->> 'role' = 'admin');
```

**Для tenant системы RLS НЕ нужны** (доступ через токены в URL).

---

## 📝 ШАГ 5: ДОКУМЕНТАЦИЯ

### Обновить файлы:

#### 1. `database/README.md`

```markdown
## Таблица: {table_name}

**Описание:** {что делает}
**Категория:** {категория}
**Priority:** {приоритет}

**Поля:**
- id (UUID)
- created_at (TIMESTAMPTZ)
- ...

**Индексы:**
- idx_{table_name}_created

**Связи:**
- FK к {другая_таблица}
```

#### 2. `DATABASE_ARCHITECTURE.md`

Добавить таблицу в общую схему.

---

## 🤖 АВТОМАТИЗАЦИЯ (будущее)

### CLI команда для создания таблицы:

```bash
npm run create-table <table_name>

# Автоматически:
# 1. Создаёт SQL файл по шаблону
# 2. Добавляет в админку
# 3. Добавляет в keep-alive
# 4. Создаёт документацию
```

### Git pre-commit hook:

```bash
# .git/hooks/pre-commit
# Проверяет что при создании таблицы обновлены все файлы

if git diff --cached --name-only | grep -q "database/.*\.sql"; then
  echo "Detected new database migration"
  echo "Checking required files..."
  
  # Проверка что обновлены:
  if ! git diff --cached | grep -q "src/pages/api/admin/tables.ts"; then
    echo "❌ Error: Update src/pages/api/admin/tables.ts"
    exit 1
  fi
  
  if ! git diff --cached | grep -q "src/components/admin/DatabaseTables.tsx"; then
    echo "❌ Error: Update src/components/admin/DatabaseTables.tsx"
    exit 1
  fi
  
  echo "✅ All required files updated"
fi
```

---

## 📋 БЫСТРАЯ СПРАВКА

### Шаблон для копирования:

```markdown
## Создаю новую таблицу: {table_name}

### 1. SQL скрипт
- [ ] Создать database/{N}_create_{table_name}.sql
- [ ] Обязательные поля: id, created_at, updated_at
- [ ] Индексы для основных запросов
- [ ] Комментарии COMMENT ON

### 2. Админка
- [ ] Добавить в src/pages/api/admin/tables.ts (knownTables)
- [ ] Добавить в src/components/admin/DatabaseTables.tsx (fallbackTablesData)

### 3. Keep-Alive
- [ ] INSERT INTO keep_alive_config
- [ ] Указать priority
- [ ] Указать required_fields с плейсхолдерами

### 4. RLS (если нужно)
- [ ] ALTER TABLE ENABLE ROW LEVEL SECURITY
- [ ] CREATE POLICY для чтения
- [ ] CREATE POLICY для записи

### 5. Проверка
- [ ] Таблица видна в Database → Tables
- [ ] Таблица видна в /admin/database
- [ ] Можно добавить/удалить запись
- [ ] Keep-alive обрабатывает таблицу
- [ ] Тестовые данные созданы

### 6. Документация
- [ ] Обновить database/README.md
- [ ] Добавить описание в DATABASE_ARCHITECTURE.md
```

---

## ✅ ДЛЯ НАШИХ ТАБЛИЦ СДЕЛАНО:

```
✅ tenants:
  ✅ SQL скрипт
  ✅ Админка (2 файла)
  ✅ Keep-alive добавлена
  ❌ RLS не нужна
  ✅ Тестовые данные

✅ saved_properties:
  ✅ SQL скрипт
  ✅ Админка (2 файла)
  ✅ Keep-alive добавлена
  ❌ RLS не нужна
  ✅ Тестовые данные

✅ access_attempts:
  ✅ SQL скрипт
  ✅ Админка (2 файла)
  ✅ Keep-alive (disabled - логи)
  ❌ RLS не нужна
  ❌ Тестовые данные не нужны (заполнится автоматически)
```

---

## 📄 ДОКУМЕНТ СОЗДАН:

**`DATABASE_TABLE_CREATION_POLICY.md`** - обязательная политика для всех будущих таблиц!

---

## ❓ ЧТО ДАЛЬШЕ?

**A)** Переходим к **Этапу 2: Telegram Bot** (база готова!)  
**B)** Исправляем keep-alive (401 ошибки) сейчас  
**C)** Документируем процесс создания таблиц

**Рекомендую A** - база данных для бота готова на 100%! Можем кодить бота! 🚀