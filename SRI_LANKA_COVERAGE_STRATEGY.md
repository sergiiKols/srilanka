# 🇱🇰 Стратегия покрытия всей Шри-Ланки POIs

## 📊 Масштаб задачи

### **Текущее состояние:**
- 📍 **Покрыто:** Южное побережье (Galle, Unawatuna, Hikkaduwa, Mirissa, Weligama)
- 🗺️ **POIs:** 45 объектов в 4 городах
- 📏 **Радиус:** ~50 км побережья

### **Вся Шри-Ланка:**
- 🏝️ **Площадь:** 65,610 км²
- 🌆 **Основных городов:** ~25-30
- 🏖️ **Популярных туристических зон:** ~15-20
- 📍 **Потенциальных POIs:** **5,000 - 50,000+**

---

## 🎯 Оценка количества POIs для полного покрытия

### **Категории и примерное количество:**

| Категория | По всей стране | Приоритетные | Источник |
|-----------|----------------|--------------|----------|
| 🏖️ **Пляжи** | ~100 | ~30 | Google Maps, WikiTravel |
| 🍽️ **Рестораны** | ~5,000+ | ~200-300 | Google Places API |
| 🏨 **Отели/Гестхаусы** | ~10,000+ | ~500 | Booking.com, Airbnb |
| 🕍 **Храмы/Культура** | ~500+ | ~50-100 | UNESCO, TripAdvisor |
| 🏥 **Больницы** | ~100 | ~20-30 | Govt data, Google Maps |
| 💊 **Аптеки** | ~1,000+ | ~50-100 | Google Places |
| 🏧 **Банкоматы** | ~2,000+ | ~100-150 | Bank websites |
| 🚌 **Транспорт** | ~500+ | ~100 | SLTB, private operators |
| 🏄 **Активности** | ~200+ | ~50 | TripAdvisor, Lonely Planet |
| 🛒 **Магазины** | ~3,000+ | ~100-150 | Google Places |

**ИТОГО для БАЗОВОГО покрытия:** ~1,000 - 1,500 POIs  
**ИТОГО для ПОЛНОГО покрытия:** ~20,000 - 50,000 POIs

---

## ⚠️ Проблема ручного подхода

### **Если парсить вручную через AI:**

**Базовое покрытие (1,500 POIs):**
```
1,500 POIs × 3 минуты = 4,500 минут = 75 часов = ~10 рабочих дней
Стоимость: 1,500 × $0.16 = $240
```

**Полное покрытие (20,000 POIs):**
```
20,000 POIs × 3 минуты = 60,000 минут = 1,000 часов = ~125 рабочих дней
Стоимость: 20,000 × $0.16 = $3,200
```

❌ **Вывод:** Ручной подход НЕ масштабируется!

---

## 🚀 Решение: Автоматизация + Внешние API

### **Стратегия 1: Google Places API** ⭐ РЕКОМЕНДУЕТСЯ

#### **Как работает:**
```typescript
// Автоматический импорт POIs из Google Places
const results = await googlePlaces.nearbySearch({
  location: { lat: 6.9271, lng: 79.8612 }, // Colombo
  radius: 50000, // 50 км
  type: 'restaurant' // или другой тип
});
```

#### **Преимущества:**
- ✅ **Огромная база данных** (миллионы мест)
- ✅ **Актуальные данные** (обновляются постоянно)
- ✅ **Полная информация:**
  - Название, адрес, координаты
  - Часы работы
  - Телефон, сайт
  - Рейтинги и отзывы
  - Фотографии
- ✅ **Быстро:** 1000+ POIs за минуты
- ✅ **Дешево:** $200/месяц для 100K запросов (или бесплатно до $200/месяц кредита)

#### **Недостатки:**
- ⚠️ Требует настройки API
- ⚠️ Нужен скрипт для импорта
- ⚠️ Много "мусорных" данных (нужна фильтрация)

#### **Стоимость Google Places API:**
- **Free tier:** $200 кредита в месяц
- **Nearby Search:** $32 за 1000 запросов
- **Place Details:** $17 за 1000 запросов

**Для 1,500 POIs:**
```
Nearby Search (10 регионов × 10 категорий): 100 запросов
Place Details (для полной информации): 1,500 запросов

100 × $0.032 = $3.20
1,500 × $0.017 = $25.50
ИТОГО: ~$29 (или БЕСПЛАТНО с $200 кредитом)
```

---

### **Стратегия 2: OpenStreetMap (OSM) API** 💎 БЕСПЛАТНО

#### **Как работает:**
```typescript
// Overpass API query
const query = `
  [out:json];
  area["ISO3166-1"="LK"]->.searchArea;
  (
    node["amenity"="restaurant"](area.searchArea);
    way["amenity"="restaurant"](area.searchArea);
  );
  out center;
`;
```

#### **Преимущества:**
- ✅ **100% БЕСПЛАТНО**
- ✅ **Open source данные**
- ✅ **Хорошее покрытие** (особенно популярных мест)
- ✅ **Полные координаты**
- ✅ **Можно скачать всю страну сразу**

#### **Недостатки:**
- ⚠️ Меньше деталей чем Google
- ⚠️ Не всегда актуально (зависит от волонтеров)
- ⚠️ Меньше отзывов и рейтингов

#### **Стоимость:** $0

---

### **Стратегия 3: Комбинированный подход** 🎯 ОПТИМАЛЬНО

#### **Идея:**
Использовать несколько источников для максимального покрытия:

```
┌─────────────────────────────────────────┐
│   1. OpenStreetMap (бесплатно)          │
│      └─ Базовое покрытие (60-70%)       │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│   2. Google Places API                  │
│      └─ Дополнение + детали (20-30%)    │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│   3. TripAdvisor API (опционально)      │
│      └─ Рейтинги + отзывы (10%)         │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│   4. Ручная валидация                   │
│      └─ Проверка + очистка              │
└─────────────────────────────────────────┘
```

#### **Алгоритм:**
1. Скачать базу POIs из OSM для Шри-Ланки (~30 минут)
2. Обогатить данные через Google Places (~2 часа скрипта)
3. Дедупликация и очистка (~1 час)
4. Ручная валидация приоритетных мест (~2-3 дня)

**Итого:** 3-4 дня работы, ~$50-100 стоимость

---

## 📋 Пошаговый план покрытия всей Шри-Ланки

### **Фаза 1: Приоритетные регионы** (1-2 недели)

**Регионы для покрытия:**
1. 🏙️ **Colombo** (столица) - 200+ POIs
2. 🏖️ **South Coast** (уже есть) - 45 POIs ✅
3. 🏖️ **East Coast** (Trincomalee, Arugam Bay) - 100+ POIs
4. 🏔️ **Hill Country** (Kandy, Ella, Nuwara Eliya) - 150+ POIs
5. 🏛️ **Cultural Triangle** (Anuradhapura, Polonnaruwa, Sigiriya) - 80+ POIs

**Итого:** ~600 POIs

**Метод:**
- OpenStreetMap API (бесплатно)
- Google Places для деталей ($20-30)
- Ручная валидация ключевых мест

---

### **Фаза 2: Полное покрытие** (1-2 месяца)

**Все регионы:**
- Все города >50K населения
- Все туристические зоны
- Все национальные парки
- Все пляжи

**Итого:** ~1,500-2,000 POIs

**Метод:**
- Автоматизированный скрипт
- Batch импорт из OSM + Google Places
- Дедупликация
- Выборочная валидация

---

### **Фаза 3: Детализация** (постоянно)

**Crowdsourcing:**
- Пользователи добавляют свои места
- Модерация админами
- Обновление через API

---

## 💻 Технический план реализации

### **Что нужно создать:**

#### **1. Скрипт импорта из OpenStreetMap**
```typescript
// scripts/import-osm-pois.ts
import { fetchOSMData } from './osm-api';

const categories = [
  { osm: 'amenity=restaurant', our: 'food' },
  { osm: 'tourism=hotel', our: 'stay' },
  { osm: 'natural=beach', our: 'beach' },
  // ... etc
];

async function importPOIs() {
  for (const category of categories) {
    const data = await fetchOSMData('Sri Lanka', category.osm);
    const formatted = formatForOurDB(data, category.our);
    await saveToDB(formatted);
  }
}
```

#### **2. Скрипт обогащения через Google Places**
```typescript
// scripts/enrich-with-google.ts
import { GooglePlacesAPI } from './google-api';

async function enrichPOIs() {
  const pois = await loadPOIsFromDB();
  
  for (const poi of pois) {
    const details = await GooglePlacesAPI.findPlace(poi.name, poi.coordinates);
    
    if (details) {
      poi.phone = details.phone;
      poi.website = details.website;
      poi.rating = details.rating;
      poi.hours = details.opening_hours;
      await updatePOI(poi);
    }
  }
}
```

#### **3. Добавить в Supabase таблицу `pois`**
```sql
CREATE TABLE public.pois (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    
    -- Основное
    title TEXT NOT NULL,
    category TEXT NOT NULL,
    position POINT NOT NULL,
    
    -- Детали
    description TEXT,
    address TEXT,
    phone TEXT,
    website TEXT,
    hours TEXT,
    
    -- Метаданные
    rating DECIMAL(2,1),
    price_level INTEGER,
    source TEXT, -- 'osm', 'google', 'user'
    
    -- Статус
    is_verified BOOLEAN DEFAULT false,
    is_active BOOLEAN DEFAULT true,
    
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- Индексы
CREATE INDEX idx_pois_category ON pois(category);
CREATE INDEX idx_pois_position ON pois USING GIST(position);
CREATE INDEX idx_pois_verified ON pois(is_verified) WHERE is_verified = true;

-- RLS: Все читают, админы управляют
ALTER TABLE pois ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Everyone can read active POIs"
    ON pois FOR SELECT
    USING (is_active = true);

CREATE POLICY "Admins can manage POIs"
    ON pois FOR ALL
    TO authenticated
    USING (
        EXISTS (
            SELECT 1 FROM user_roles
            WHERE user_id = auth.uid() AND role = 'admin'
        )
    );
```

---

## 📊 Сравнение подходов для всей Шри-Ланки

| Подход | Покрытие POIs | Время | Стоимость | Качество |
|--------|---------------|-------|-----------|----------|
| **Ручной AI** | 1,500 | 75 часов | $240 | ⭐⭐⭐⭐⭐ |
| **OpenStreetMap** | 2,000+ | 2 дня | $0 | ⭐⭐⭐⭐ |
| **Google Places** | 5,000+ | 1 день | $50 | ⭐⭐⭐⭐⭐ |
| **Комбо (OSM + Google)** | 3,000+ | 3-4 дня | $50 | ⭐⭐⭐⭐⭐ |

---

## 🎯 Моя рекомендация

### **Для покрытия всей Шри-Ланки:**

#### **Этап 1: Быстрый старт (1 неделя)**
1. ✅ Используйте **OpenStreetMap** для базового покрытия
   - Бесплатно
   - 1,000-2,000 POIs автоматически
   - Я могу написать скрипт

2. ✅ Добавьте таблицу `pois` в Supabase
   - Для хранения импортированных данных
   - С возможностью редактирования админами

3. ✅ Обогатите приоритетные POIs через Google Places
   - Топ-200 мест
   - ~$10-20

**Результат:** 1,500+ POIs за неделю, ~$20

---

#### **Этап 2: Расширение (2-4 недели)**
1. ✅ Полный импорт из Google Places по регионам
2. ✅ Дедупликация и очистка
3. ✅ Добавление рейтингов и отзывов

**Результат:** 5,000+ POIs

---

#### **Этап 3: Поддержка (постоянно)**
1. ✅ Пользователи добавляют свои места
2. ✅ Админ-панель для модерации
3. ✅ Периодическое обновление из API

---

## 💰 Итоговая оценка стоимости

### **Минимальное покрытие (1,500 POIs):**
- OpenStreetMap: $0
- Google Places (детали для топ-200): $15
- Хостинг и API: $0 (Supabase free tier)
- **ИТОГО: ~$15**

### **Полное покрытие (5,000+ POIs):**
- OpenStreetMap: $0
- Google Places API: $50-100
- TripAdvisor API (опционально): $50
- **ИТОГО: ~$100-150**

---

## ✅ Следующие шаги

**Хотите, чтобы я:**
1. 🔧 Написал скрипт импорта из OpenStreetMap?
2. 📊 Создал таблицу `pois` в SQL схеме?
3. 🗺️ Показал пример интеграции с Google Places API?
4. 🚀 Помог настроить автоматический импорт?

**Выберите приоритет - и начнем!** 🇱🇰
