# 🤖 Отчёт: Управление личным Telegram аккаунтом для автоматизации

## 📋 ЗАДАЧА (Что понял)

Необходимо реализовать систему автоматизации для работы с личным Telegram аккаунтом для:

1. **Размещение объявлений** - публикация заявок клиентов в Telegram каналы/группы
2. **Получение предложений** - автоматический приём сообщений от арендодателей в личку
3. **Парсинг данных объектов** - извлечение информации о недвижимости из сообщений
4. **ИИ-диалог с арендодателями** - использование Groq AI для запроса недостающей информации
5. **Размещение на карте** - автоматическое добавление объектов на карты клиентов

---

## ✅ ЧТО УЖЕ ЕСТЬ В СИСТЕМЕ

### 1. Web App бот интеграция
- ✅ `src/pages/api/telegram-form.ts` - приём данных от Telegram Web App
- ✅ `src/lib/telegram.ts` - библиотека для работы с Telegram Bot API
- ✅ Формы для клиентов через Telegram бота
- ✅ Сохранение в Supabase

### 2. AI сервисы
- ✅ `src/services/groqService.ts` - Groq API для анализа текста (llama-3.3-70b)
- ✅ `src/services/perplexityService.ts` - Perplexity для расширения URL
- ✅ Парсинг описаний недвижимости
- ✅ Извлечение структурированных данных

### 3. Обработка предложений (Skills)
- ✅ `.agent/skills/parse-landlord-offer/` - парсинг предложений арендодателей
- ✅ `.agent/skills/store-property-photos/` - сохранение фото
- ✅ `.agent/skills/geocode-property-location/` - геокодирование
- ✅ `.agent/skills/link-offer-to-client-map/` - привязка к карте клиента
- ✅ `.agent/skills/notify-client-offer-received/` - уведомления

### 4. База данных
- ✅ Supabase с таблицами для properties, clients, offers
- ✅ Storage для фотографий

---

## 🎯 ЧТО НУЖНО РЕАЛИЗОВАТЬ

### АРХИТЕКТУРА РЕШЕНИЯ

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        TELEGRAM SERVERS                                 │
│                    (личные сообщения аккаунта)                          │
└────────────────────────────┬────────────────────────────────────────────┘
                             │ MTProto Protocol
                             │
┌────────────────────────────▼────────────────────────────────────────────┐
│                     VPS SERVER (отдельный)                              │
│  ┌───────────────────────────────────────────────────────────────────┐  │
│  │  TELEGRAM CLIENT API (GramJS/Telethon)                            │  │
│  │  - Авторизован под вашим личным аккаунтом                         │  │
│  │  - Слушает входящие сообщения 24/7                                │  │
│  │  - Фильтрует по хэштегам (#SL12345)                               │  │
│  └──────────────────────┬────────────────────────────────────────────┘  │
│                         │                                                │
│  ┌──────────────────────▼────────────────────────────────────────────┐  │
│  │  MESSAGE PROCESSOR                                                 │  │
│  │  - Парсит текст, фото, ссылки                                     │  │
│  │  - Определяет client_id (#SL12345)                                │  │
│  │  - Проверяет полноту данных                                       │  │
│  └──────────────────────┬────────────────────────────────────────────┘  │
│                         │                                                │
│  ┌──────────────────────▼────────────────────────────────────────────┐  │
│  │  AI CONVERSATION ENGINE (Groq)                                     │  │
│  │  - Если данных не хватает → генерирует вопросы                   │  │
│  │  - Отправляет от имени аккаунта                                   │  │
│  │  - Анализирует ответы арендодателя                                │  │
│  └──────────────────────┬────────────────────────────────────────────┘  │
│                         │                                                │
└─────────────────────────┼────────────────────────────────────────────────┘
                          │ HTTPS Request
┌─────────────────────────▼────────────────────────────────────────────────┐
│                    ВАШ ASTRO САЙТ                                        │
│  API: /api/import/landlord-offer                                         │
│  - Получает структурированные данные                                     │
│  - Использует существующие skills для обработки                          │
│  - Добавляет property на карту клиента                                   │
└──────────────────────────────────────────────────────────────────────────┘
```

---

## 🔧 РЕАЛИЗАЦИЯ: ТРИ КОМПОНЕНТА

### КОМПОНЕНТ 1: Telegram Client API Service (VPS)

**Технологии:**
- **GramJS** (Node.js) или **Telethon** (Python) - библиотеки для Telegram Client API
- **PM2** - для постоянной работы процесса
- **SQLite/PostgreSQL** - локальная БД для состояний диалогов

**Основные функции:**

#### 1.1 Авторизация и подключение
```javascript
// Псевдокод
const client = new TelegramClient(
  new StringSession(SESSION_STRING),
  API_ID,
  API_HASH
);
await client.start({
  phoneNumber: '+94771234567',
  password: '2FA_PASSWORD', // если есть
  onError: (err) => console.error(err),
});
```

#### 1.2 Слушатель входящих сообщений
```javascript
client.addEventHandler(async (event) => {
  const message = event.message;
  const sender = await message.getSender();
  
  // Фильтрация: только личные сообщения с хэштегом
  if (message.isPrivate && message.text.includes('#SL')) {
    await processLandlordMessage(message, sender);
  }
}, new NewMessage({}));
```

#### 1.3 Парсинг сообщений
```javascript
async function processLandlordMessage(message, sender) {
  // Извлечение данных
  const clientCode = extractHashtag(message.text); // #SL12345
  const description = message.text;
  const photos = message.media ? await downloadPhotos(message.media) : [];
  const googleMapsUrl = extractGoogleMapsUrl(message.text);
  const phone = extractPhone(message.text);
  const price = extractPrice(message.text);
  
  // Проверка полноты данных
  const missingData = checkCompleteness({
    photos, googleMapsUrl, phone, description, price
  });
  
  if (missingData.length > 0) {
    // Недостаёт данных → запускаем AI диалог
    await startAIConversation(sender.id, clientCode, missingData);
  } else {
    // Все данные есть → отправляем на сайт
    await sendToWebsite({
      clientCode, description, photos, googleMapsUrl, phone, price,
      landlordId: sender.id,
      landlordUsername: sender.username
    });
  }
}
```

#### 1.4 AI Conversation Engine (Groq)
```javascript
async function startAIConversation(landlordId, clientCode, missingData) {
  // Генерируем вопрос через Groq AI
  const prompt = `
    Арендодатель прислал неполную информацию об объекте.
    Недостаёт: ${missingData.join(', ')}
    
    Сгенерируй вежливое сообщение на английском, 
    запрашивающее эти данные. Будь кратким и дружелюбным.
  `;
  
  const aiResponse = await groqAPI.chat({
    model: 'llama-3.3-70b-versatile',
    messages: [{ role: 'user', content: prompt }]
  });
  
  // Отправляем вопрос от имени аккаунта
  await client.sendMessage(landlordId, {
    message: aiResponse.choices[0].message.content
  });
  
  // Сохраняем состояние диалога в БД
  await saveConversationState({
    landlordId,
    clientCode,
    awaitingData: missingData,
    step: 'waiting_for_response'
  });
}
```

#### 1.5 Обработка ответов от арендодателя
```javascript
async function handleFollowUpMessage(message, sender) {
  // Получаем состояние диалога
  const conversation = await getConversationState(sender.id);
  
  if (!conversation) return; // Новое сообщение, не продолжение
  
  // Парсим дополнительные данные
  const newData = parseMessage(message);
  
  // Обновляем данные
  const updatedOffer = mergeData(conversation.initialData, newData);
  
  // Проверяем полноту снова
  const stillMissing = checkCompleteness(updatedOffer);
  
  if (stillMissing.length === 0) {
    // Теперь всё есть!
    await client.sendMessage(sender.id, {
      message: "✅ Perfect! I have all the information. Processing your property..."
    });
    
    await sendToWebsite(updatedOffer);
    
    await client.sendMessage(sender.id, {
      message: "✅ Your property has been added! The client will review it and contact you if interested."
    });
    
    // Очищаем состояние
    await clearConversationState(sender.id);
  } else {
    // Ещё чего-то не хватает
    await startAIConversation(sender.id, conversation.clientCode, stillMissing);
  }
}
```

#### 1.6 Публикация объявлений
```javascript
async function publishClientRequest(requestData) {
  // Публикуем в Telegram канал или группу
  const message = formatRequestMessage(requestData);
  
  await client.sendMessage('YOUR_CHANNEL_ID', {
    message: message
  });
  
  // Или отправляем в множество групп
  for (const groupId of TARGET_GROUPS) {
    await client.sendMessage(groupId, { message });
    await sleep(5000); // Антиспам задержка
  }
}
```

---

### КОМПОНЕНТ 2: API Endpoint на сайте

**Файл:** `src/pages/api/import/landlord-offer.ts`

```typescript
import type { APIRoute } from 'astro';
import { analyzePropertyDescription } from '../../../services/groqService';
import { expandShortUrl } from '../../../services/perplexityService';

export const POST: APIRoute = async ({ request }) => {
  try {
    const data = await request.json();
    
    // Валидация данных
    const { 
      clientCode, 
      description, 
      photos, 
      googleMapsUrl, 
      phone, 
      price,
      landlordId,
      landlordUsername 
    } = data;
    
    // 1. Разворачиваем короткую ссылку Google Maps
    const fullUrl = await expandShortUrl(googleMapsUrl);
    const coordinates = extractCoordinatesFromUrl(fullUrl);
    
    // 2. Анализируем описание через Groq AI
    const aiAnalysis = await analyzePropertyDescription(description, {
      photos,
      coordinates
    });
    
    // 3. Загружаем фото в Supabase Storage
    const photoUrls = await uploadPhotosToStorage(photos);
    
    // 4. Создаём property в БД
    const property = await createProperty({
      clientId: clientCode,
      title: aiAnalysis.title,
      description: aiAnalysis.cleanDescription,
      coordinates: coordinates,
      bedrooms: aiAnalysis.rooms,
      bathrooms: aiAnalysis.bathrooms,
      price: price,
      photos: photoUrls,
      propertyType: aiAnalysis.propertyType,
      amenities: aiAnalysis.amenities,
      features: aiAnalysis.features,
      landlordContact: phone,
      landlordTelegramId: landlordId,
      landlordTelegramUsername: landlordUsername,
      source: 'telegram_client_api',
      status: 'available'
    });
    
    // 5. Уведомляем клиента (через существующий skill)
    await notifyClientOfferReceived({
      clientId: clientCode,
      propertyId: property.id
    });
    
    return new Response(JSON.stringify({
      success: true,
      propertyId: property.id,
      message: 'Property imported successfully'
    }), {
      status: 200,
      headers: { 'Content-Type': 'application/json' }
    });
    
  } catch (error) {
    console.error('Error importing landlord offer:', error);
    return new Response(JSON.stringify({
      success: false,
      error: error.message
    }), {
      status: 500,
      headers: { 'Content-Type': 'application/json' }
    });
  }
};
```

---

### КОМПОНЕНТ 3: Управление через Admin панель

**Добавить в админку:**

1. **Мониторинг диалогов**
   - Список активных диалогов с арендодателями
   - Статус: "ожидает фото", "ожидает локацию", "обработан"
   - Возможность вмешаться вручную

2. **Логи сообщений**
   - История обработанных предложений
   - Какие вопросы задавал AI
   - Что ответил арендодатель

3. **Настройки AI**
   - Шаблоны вопросов
   - Язык общения (EN/RU)
   - Тон (формальный/дружелюбный)

4. **Управление публикациями**
   - Список Telegram групп/каналов для публикации
   - Шаблоны объявлений
   - Расписание публикаций

---

## 🚀 ЧТО РЕАЛЬНО РЕАЛИЗОВАТЬ

### ✅ ПОЛНОСТЬЮ РЕАЛЬНО:

1. **Авторизация Client API** - 100% работает
   - GramJS/Telethon стабильны и проверены
   - Telegram API не блокирует легитимное использование
   - Тысячи проектов используют это

2. **Чтение личных сообщений** - 100% работает
   - С фильтрацией по хэштегам
   - Скачивание фото из сообщений
   - Парсинг текста, ссылок, контактов

3. **Отправка сообщений от имени аккаунта** - 100% работает
   - Client API позволяет это делать
   - Выглядит как обычное сообщение от пользователя

4. **AI диалог через Groq** - 100% работает
   - Groq API уже подключен (`src/services/groqService.ts`)
   - Llama-3.3-70b отлично генерирует вопросы
   - Скорость: 500-800 токенов/сек

5. **Парсинг данных об объектах** - 90% готово
   - Уже есть парсинг описаний
   - Нужно добавить только извлечение из сообщений Telegram

6. **Интеграция с существующими skills** - 100% готово
   - Все skills уже работают
   - Нужно только вызвать их из нового API endpoint

### ⚠️ ОГРАНИЧЕНИЯ И РИСКИ:

1. **Нужен отдельный VPS сервер**
   - Стоимость: $5-10/месяц (DigitalOcean, Vultr)
   - Причина: Client API требует постоянное подключение
   - Нельзя использовать serverless (Vercel, Netlify)

2. **Риск блокировки аккаунта**
   - Вероятность: НИЗКАЯ (5-10%) при правильном использовании
   - Telegram блокирует за спам, а не за автоматизацию
   - **Решение:** Использовать отдельный бизнес аккаунт
   - **НЕ использовать** личный аккаунт с семьей/друзьями

3. **Первоначальная настройка**
   - Сложность: средняя
   - Время: 2-4 часа
   - Требует: SMS код, возможно 2FA пароль
   - Нужны навыки работы с сервером (SSH, PM2)

4. **Telegram ToS (Terms of Service)**
   - Формально автоматизация может нарушать ToS
   - На практике: Telegram НЕ блокирует за легитимное использование
   - Блокируют за: спам, массовую рассылку, накрутку
   - НЕ блокируют за: чтение своих сообщений, ответы арендодателям

### 🔒 БЕЗОПАСНОСТЬ:

1. **Фильтрация по хэштегам обязательна**
   - Обрабатывать ТОЛЬКО сообщения с #SL кодом
   - Остальные сообщения полностью игнорировать
   - НЕ логировать личную переписку

2. **Отдельный бизнес аккаунт**
   - Создать новый Telegram аккаунт только для бизнеса
   - Не использовать личный аккаунт
   - В случае блокировки - потеряете только бизнес аккаунт

3. **Rate limiting**
   - Не отправлять >30 сообщений в минуту
   - Задержки между сообщениями 5-10 сек
   - Telegram имеет мягкие лимиты

---

## 📊 СРАВНЕНИЕ ПОДХОДОВ

### ПОДХОД 1: Bot API + Пересылка (ТЕКУЩИЙ)

✅ **Плюсы:**
- Уже работает в системе
- Безопасно (нет рисков)
- Просто в поддержке
- Бесплатно

❌ **Минусы:**
- Требует ручной пересылки каждого сообщения
- Не масштабируется при большом объёме
- Клиент не может публиковать сам

**Время на обработку:** 5-10 секунд на сообщение

### ПОДХОД 2: Client API (ПРЕДЛАГАЕМЫЙ)

✅ **Плюсы:**
- 100% автоматизация
- Масштабируется до 1000+ сообщений/день
- AI диалог с арендодателями
- Публикация объявлений автоматически
- Нет ручного труда

❌ **Минусы:**
- Нужен VPS ($5-10/мес)
- Риск блокировки (5-10%)
- Сложнее в настройке
- Требует мониторинга

**Время на обработку:** мгновенно (0 секунд)

### ПОДХОД 3: Гибридный

✅ **Лучшее из двух миров:**
- Первые 1-2 месяца: Bot API + пересылка
- Проверяете спрос и объём
- Если объём >20 сообщений/день → переходите на Client API
- Используете отдельный бизнес аккаунт

**Рекомендуется для минимизации рисков**

---

## 💰 СТОИМОСТЬ РЕАЛИЗАЦИИ

### Client API Solution:

**Инфраструктура:**
- VPS сервер: $5-10/месяц (Vultr, DigitalOcean)
- Domain (опционально): $10/год

**Разработка:**
- Telegram Client Service: 8-12 часов
- API Endpoint: 4-6 часов
- AI Conversation Engine: 6-8 часов
- Admin Panel Integration: 4-6 часов
- Тестирование: 4-6 часов

**Итого:** 26-38 часов разработки

**AI API Costs:**
- Groq: $0.59/M input, $0.79/M output
- При 100 сообщений/день ≈ $5-10/месяц

---

## 📝 ПЛАН РЕАЛИЗАЦИИ

### ФАЗА 1: MVP (2-3 дня)

1. **День 1:**
   - Настроить VPS сервер
   - Установить GramJS/Telethon
   - Авторизовать Client API
   - Реализовать базовое чтение сообщений

2. **День 2:**
   - Фильтрация по хэштегам
   - Парсинг текста, фото, ссылок
   - Отправка на API сайта
   - Базовое тестирование

3. **День 3:**
   - Интеграция с Groq для простых вопросов
   - Обработка ответов
   - Уведомления

### ФАЗА 2: AI Диалог (2-3 дня)

4. **День 4-5:**
   - Умный анализ недостающих данных
   - Генерация вопросов через Groq
   - Управление состоянием диалогов
   - Объединение данных из нескольких сообщений

5. **День 6:**
   - Тестирование AI диалогов
   - Настройка промптов
   - Обработка edge cases

### ФАЗА 3: Admin & Monitoring (2 дня)

6. **День 7:**
   - Админ панель для мониторинга
   - Логи диалогов
   - Статистика обработки

7. **День 8:**
   - Финальное тестирование
   - Документация
   - Запуск в production

**Итого:** 8 дней разработки (1-1.5 недели)

---

## 🎬 ЖИЗНЕННЫЙ ЦИКЛ (Пример)

### Сценарий 1: Полные данные сразу

1. Арендодатель пишет:
   ```
   #SL12345
   Villa 2BR with pool in Negombo
   $850/month
   Contact: +94 77 888 7777
   [5 photos]
   https://goo.gl/maps/xyz
   ```

2. Client API:
   - Получает сообщение
   - Парсит все данные
   - Проверяет: ✅ всё есть
   - Отправляет на сайт

3. Сайт:
   - Разворачивает Google Maps URL
   - Анализирует через Groq
   - Загружает фото
   - Добавляет на карту клиента #SL12345

4. Уведомления:
   - Арендодателю: "✅ Property added!"
   - Клиенту: "New property in Negombo"
   - Вам: "Property #123 processed"

**Время:** 30-60 секунд, полностью автоматически

### Сценарий 2: Недостающие данные

1. Арендодатель пишет:
   ```
   #SL12345
   I have villa in Negombo $800
   ```

2. Client API:
   - Парсит данные
   - Проверяет: ❌ нет фото, нет location, нет телефона
   - Запускает AI диалог

3. Groq генерирует вопрос:
   ```
   Thank you for your message! To add your property, 
   could you please provide:
   - Photos (minimum 3)
   - Google Maps location link
   - Your contact phone number
   - Number of bedrooms and bathrooms
   ```

4. Client API отправляет от вашего имени

5. Арендодатель отвечает:
   ```
   [4 photos]
   2 bedrooms, 1 bathroom
   Phone: +94 77 123 4567
   https://goo.gl/maps/abc
   ```

6. Client API:
   - Объединяет данные
   - Проверяет: ✅ теперь всё есть
   - Отправляет на сайт
   - Уведомляет: "✅ Property added!"

**Время:** 2-5 минут (зависит от скорости ответа), автоматически

---

## ⚖️ ФИНАЛЬНАЯ РЕКОМЕНДАЦИЯ

### ДЛЯ СТАРТА: Bot API (уже есть)

Если у вас:
- <20 сообщений в день
- Только начинаете
- Хотите минимизировать риски

→ **Используйте текущий Bot API + пересылку**

### ДЛЯ МАСШТАБА: Client API

Если у вас:
- >20-30 сообщений в день
- Много активных клиентов (10+)
- Нужна полная автоматизация
- Готовы к техническим вызовам

→ **Переходите на Client API**

### ОПТИМАЛЬНЫЙ ПУТЬ:

1. **Месяц 1:** Bot API + пересылка
   - Проверяете спрос
   - Собираете данные об объёме

2. **Месяц 2:** Если объём растёт
   - Создаёте отдельный бизнес Telegram аккаунт
   - Настраиваете Client API на VPS
   - Тестируете параллельно с Bot API

3. **Месяц 3:** Полный переход
   - Публикуете объявления с новым аккаунтом
   - Client API обрабатывает автоматически
   - Вы только мониторите

---

## 🎯 ИТОГОВЫЙ ОТВЕТ

### ЧТО ПОНЯЛ:
✅ Публикация заявок клиентов в Telegram  
✅ Приём предложений от арендодателей в личку  
✅ Парсинг данных об объектах  
✅ AI диалог для запроса недостающих данных  
✅ Автоматическое размещение на картах  

### ЧТО РЕАЛЬНО:
✅ **100% реально** технически  
✅ Все технологии есть и работают  
✅ У вас уже есть 70% компонентов  
⚠️ Требует VPS сервер ($5-10/мес)  
⚠️ Риск блокировки аккаунта (5-10%) - использовать отдельный аккаунт  

### ЧТО НУЖНО:
1. VPS сервер для Client API
2. Отдельный Telegram бизнес аккаунт
3. 8-10 дней разработки
4. Мониторинг и поддержка

### РЕКОМЕНДАЦИЯ:
🚀 **Начните с Bot API (уже есть)**  
📈 **Если объём >20 сообщений/день → Client API**  
🔒 **Обязательно используйте отдельный аккаунт**  

---

**Дата:** 2026-01-27  
**Статус:** Концепция и план готовы  
**Следующий шаг:** Решение - начинать с Bot API или сразу Client API?
