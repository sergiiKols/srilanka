# 🤖 ДИАГНОСТИКА: Telegram Бот 404 - План Действий

**Дата:** 2026-01-29  
**Статус:** ✅ Диагностика завершена

---

## 🔍 РЕЗУЛЬТАТЫ ДИАГНОСТИКИ

### ✅ Что работает:

1. **Telegram Bot API подключен**
   - Bot: `@AiTechLab_bot`
   - ID: `7958965950`
   - Имя: `AI_Tech_Lab`
   - Статус: ✅ Активен и доступен

2. **API Endpoints существуют**
   - ✅ `/api/telegram-form` - приём данных от Telegram Web App
   - ✅ `/api/tenant-request` - обработка заявок арендаторов
   - ✅ `/api/admin/*` - админские endpoints (17 штук)

3. **Environment Variables настроены (локально)**
   - ✅ `TELEGRAM_BOT_TOKEN` - настроен (46 символов)
   - ✅ `PUBLIC_SUPABASE_URL` - настроен
   - ✅ `.env` файл существует

4. **Файлы страниц существуют**
   - ✅ `/telegram-app` → `src/pages/telegram-app.astro`
   - ✅ `/tenant-app` → `src/pages/tenant-app.astro`
   - ✅ `/tenant-form` → `src/pages/tenant-form.astro`
   - ✅ `/test-tenant-form` → `src/pages/test-tenant-form.astro`

---

### ⚠️ Обнаруженные проблемы:

1. **Webhook НЕ настроен**
   ```
   Webhook URL: (не настроен)
   Режим работы: Polling только
   Pending updates: 3 (не обработанных сообщения)
   ```
   
   **Последствия:**
   - Бот не может получать сообщения через webhook
   - API endpoints `/api/telegram-form` не будут вызываться автоматически
   - Сообщения накапливаются в очереди (3 pending)

2. **SITE_API_URL не настроен**
   ```
   SITE_API_URL=отсутствует
   ```
   
   **Последствия:**
   - Невозможно настроить webhook в production
   - Telegram не знает куда отправлять запросы

3. **Dev сервер не запущен**
   ```
   Порт 4321: ЗАКРЫТ
   Node процессы: НЕ НАЙДЕНЫ
   ```
   
   **Последствия:**
   - Любые запросы к localhost:4321 → 404
   - Невозможно тестировать локально

---

## 🎯 ПЛАН ДЕЙСТВИЙ (по приоритету)

### 📍 ШАГ 0: Уточнить контекст (КРИТИЧНО!)

**Вопрос к вам:** Где именно возникает 404?

**Вариант А: Локальная разработка (localhost:4321)**
```
Симптомы:
- Открываете http://localhost:4321/telegram-app → 404
- Запросы к /api/* → Connection refused
```
→ **Решение:** Перейти к Шагу 1

**Вариант Б: Production (Vercel)**
```
Симптомы:
- Сайт открывается, но некоторые страницы → 404
- Или весь сайт недоступен
```
→ **Решение:** Перейти к Шагу 2

**Вариант В: Telegram бот не отвечает**
```
Симптомы:
- Отправляете сообщение боту → нет ответа
- Бот онлайн, но молчит
```
→ **Решение:** Перейти к Шагу 3

**Вариант Г: Telegram Web App не открывается**
```
Симптомы:
- Нажимаете кнопку в боте → белый экран или 404
- Web App загружается, но ошибка
```
→ **Решение:** Перейти к Шагу 4

---

### 🔧 ШАГ 1: Запустить локальный dev сервер

**Если проблема на localhost:**

```bash
# 1. Запустить dev сервер
npm run dev

# 2. Проверить что сервер запущен
# Должно показать:
# Local: http://localhost:4321/

# 3. Открыть в браузере
# http://localhost:4321/
# http://localhost:4321/telegram-app
# http://localhost:4321/tenant-form
```

**Ожидаемый результат:**
- ✅ Сервер запущен на порту 4321
- ✅ Главная страница открывается
- ✅ API endpoints доступны

**Если не работает:**
```bash
# Проверить логи на ошибки
# Проверить package.json: "dev": "astro dev"
# Проверить astro.config.mjs
```

---

### 🚀 ШАГ 2: Проверить Production деплой (Vercel)

**Если проблема на production:**

#### A. Проверить Build Logs

1. Открыть Vercel Dashboard
2. `Deployments` → последний деплой
3. Вкладка **"Building"** или **"Build Logs"**
4. Искать ошибки (красный текст)

**Типичные ошибки:**
```
❌ Error: Cannot find module '...'
❌ Build failed
❌ Type error
```

**Решение:** См. `VERCEL_DEPLOYMENT_TROUBLESHOOTING.md`

#### B. Проверить Environment Variables

1. Vercel Dashboard → Project Settings → Environment Variables
2. Проверить наличие:
   ```
   TELEGRAM_BOT_TOKEN=...
   PUBLIC_SUPABASE_URL=...
   PUBLIC_SUPABASE_ANON_KEY=...
   SITE_API_URL=https://ваш-домен.ru/api
   ```

**Критично:** `SITE_API_URL` должен быть настроен для webhook!

#### C. Проверить какие страницы 404

```bash
# Проверить доступность:
https://ваш-домен.ru/                  # Главная
https://ваш-домен.ru/telegram-app      # Telegram Web App
https://ваш-домен.ru/tenant-form       # Форма арендатора
https://ваш-домен.ru/api/telegram-form # API endpoint
```

**Если все страницы 404:**
- Проблема с билдом или routing
- Проверить `astro.config.mjs`
- Проверить `vercel.json`

**Если только некоторые страницы 404:**
- Возможно проблема с SSR routing
- Проверить `output: 'server'` в astro.config.mjs

---

### 📡 ШАГ 3: Настроить Webhook (если нужно)

**Когда нужен webhook:**
- Бот должен получать сообщения от пользователей
- Нужна автоматическая обработка входящих сообщений

**Когда НЕ нужен webhook:**
- Используется только Telegram Web App (формы)
- Бот не обрабатывает текстовые сообщения

#### Настройка webhook для Production:

```bash
# 1. Убедиться что SITE_API_URL настроен в Vercel
# Пример: SITE_API_URL=https://cryptonomads.ru/api

# 2. Установить webhook
curl -X POST https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook \
  -H "Content-Type: application/json" \
  -d '{"url": "https://ваш-домен.ru/api/telegram-form"}'

# 3. Проверить статус
curl https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo
```

**Ожидаемый ответ:**
```json
{
  "ok": true,
  "result": {
    "url": "https://ваш-домен.ru/api/telegram-form",
    "has_custom_certificate": false,
    "pending_update_count": 0
  }
}
```

#### Настройка для локальной разработки (с ngrok):

```bash
# 1. Установить ngrok (если нет)
# https://ngrok.com/download

# 2. Запустить ngrok
ngrok http 4321

# 3. Скопировать HTTPS URL (например: https://abc123.ngrok.io)

# 4. Установить webhook
curl -X POST https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook \
  -H "Content-Type: application/json" \
  -d '{"url": "https://abc123.ngrok.io/api/telegram-form"}'

# 5. Запустить dev сервер
npm run dev

# 6. Тестировать в Telegram
```

---

### 🎨 ШАГ 4: Проверить Telegram Web App

**Если Web App не открывается из бота:**

#### A. Проверить Menu Button

```bash
# Проверить текущий menu button
curl https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getMyCommands

# Установить menu button (если нужно)
curl -X POST https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setChatMenuButton \
  -H "Content-Type: application/json" \
  -d '{
    "menu_button": {
      "type": "web_app",
      "text": "Открыть форму",
      "web_app": {
        "url": "https://ваш-домен.ru/telegram-app"
      }
    }
  }'
```

#### B. Проверить страницу Web App

1. Открыть напрямую в браузере:
   ```
   https://ваш-домен.ru/telegram-app
   ```

2. Проверить консоль браузера (F12) на ошибки

3. Проверить что `telegram-web-app.js` загружается:
   ```javascript
   // В src/pages/telegram-app.astro должно быть:
   <script src="https://telegram.org/js/telegram-web-app.js"></script>
   ```

#### C. Проверить компонент TelegramForm

```typescript
// src/components/TelegramForm.tsx должен:
// 1. Инициализировать Telegram Web App
const tg = window.Telegram?.WebApp;

// 2. Отправлять данные на /api/telegram-form
await fetch('/api/telegram-form', {
  method: 'POST',
  body: JSON.stringify(payload)
});
```

---

## 🛠️ БЫСТРЫЕ КОМАНДЫ

### Диагностика:

```bash
# Проверить статус бота
node tmp_rovodev_test_bot.js

# Проверить dev сервер
npm run dev

# Проверить билд локально
npm run build
npm run preview

# Проверить webhook
curl https://api.telegram.org/bot<TOKEN>/getWebhookInfo
```

### Исправление:

```bash
# Запустить dev сервер
npm run dev

# Очистить кеш и пересобрать
rm -rf dist .astro node_modules/.vite
npm run build

# Удалить webhook (если мешает)
curl -X POST https://api.telegram.org/bot<TOKEN>/deleteWebhook

# Перезапустить Vercel деплой
git commit --allow-empty -m "Trigger rebuild"
git push
```

---

## 📋 ЧЕКЛИСТ ДЛЯ ПРОВЕРКИ

### Локальная разработка:
- [ ] Dev сервер запущен (`npm run dev`)
- [ ] `http://localhost:4321/` открывается
- [ ] `.env` файл настроен
- [ ] `TELEGRAM_BOT_TOKEN` валиден

### Production:
- [ ] Vercel деплой успешен (Build Logs ✅)
- [ ] Environment Variables настроены
- [ ] `SITE_API_URL` добавлен в Vercel
- [ ] Страницы открываются без 404

### Telegram:
- [ ] Бот отвечает на `getMe`
- [ ] Webhook настроен (если нужен)
- [ ] Web App URL правильный
- [ ] Menu button настроен

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

**После диагностики вернитесь и скажите:**

1. **Где именно 404?** (локально / production / в боте / в Web App)
2. **Что показывают логи?** (если есть ошибки)
3. **Какие шаги уже попробовали?**

И я помогу точечно решить проблему! 🚀

---

## 📚 СВЯЗАННЫЕ ДОКУМЕНТЫ

- `TELEGRAM_API_INTEGRATION.md` - настройка API и webhook
- `TELEGRAM_FORMS_COMPLETE.md` - документация по формам
- `VERCEL_DEPLOYMENT_TROUBLESHOOTING.md` - решение проблем Vercel
- `TENANT_FORM_SETUP_GUIDE.md` - настройка форм арендаторов

---

**Создано:** 2026-01-29  
**Автор:** AI Rovo Dev  
**Версия:** 1.0
