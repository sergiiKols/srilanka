# 📋 Структура вкладки Import - Полное описание

## 🎯 Общее описание

Вкладка **Import** предназначена для добавления объектов недвижимости (Supporting Points) на карту. Пользователь может импортировать объекты с помощью:
1. **AI-анализа** (PropertyImporterAI.tsx) - основной метод
2. **Ручного ввода** (PropertyImporter.tsx) - запасной метод

---

## 🤖 PropertyImporterAI.tsx - AI-импорт (Основной)

### Расположение кнопки:
- **Верхний правый угол** карты
- Кнопка: `🤖 Import`
- Градиент: индиго → фиолетовый
- Подсказка: "AI импорт объектов"

### Архитектура компонента:

#### 📊 State (useState):
```tsx
{
  step: 1,              // Шаг процесса (1, 2, или 3)
  coordinates: null,    // Координаты объекта [lat, lng]
  images: [],           // Загруженные фото (base64)
  imageUrls: [],        // URL фотографий
  description: '',      // Текстовое описание
  aiProcessing: false,  // Идет обработка AI
  aiResult: null,       // Результат AI анализа
  quickResult: null,    // Быстрый результат
  error: null,          // Ошибки
  sources: []          // Источники данных
}
```

---

## 🔄 Процесс импорта (3 шага)

### ШАГ 1: Загрузка фотографий 📸

**Цель:** Собрать визуальную информацию об объекте

**Методы загрузки:**
1. **Загрузить с диска** 💾
   - Кнопка открывает file picker
   - `<input type="file" multiple accept="image/*">`
   - Функция: `handleImageUpload()`
   - Конвертация в base64 через FileReader

2. **Добавить по URL** 🔗
   - Кнопка открывает prompt
   - Функция: `handleAddImageUrl()`
   - Добавляет URL в `imageUrls[]`

3. **Вставить из буфера** 📋 (УНИКАЛЬНО!)
   - Div с `onPaste={handlePaste}`
   - Функция: `handlePaste(e)`
   - Извлекает изображения из `e.clipboardData.items`
   - Поддерживает Ctrl+V

**Отображение:**
- Сетка 3 колонки
- Превью загруженных фото
- Кнопка удаления на каждом фото
- Счетчик: "X загружено"

**Кнопка перехода:** "Далее →"

---

### ШАГ 2: Google Maps URL + Описание 📍

**Цель:** Получить координаты и текстовое описание

#### Поле 1: Google Maps URL

**Input поле:**
```tsx
<textarea
  value={googleMapsInput}
  onChange={(e) => setGoogleMapsInput(e.target.value)}
  placeholder="Вставьте ссылку из Google Maps или координаты..."
  rows={3}
/>
```

**Поддерживаемые форматы:**
- Полный URL: `https://www.google.com/maps/place/.../@6.0335,80.2168,17z`
- Короткая ссылка: `https://goo.gl/maps/abc123`
- Короткая v2: `https://maps.app.goo.gl/xyz789`
- Координаты: `6.0335, 80.2168`
- Координаты с @: `@6.0335,80.2168,17z`

**Обработка коротких ссылок:**
- Если обнаружена `goo.gl` → вызывает `/api/expand-url`
- API разворачивает ссылку на сервере
- Метод: HEAD запрос с отслеживанием редиректов

#### Поле 2: Описание объекта

**Textarea:**
```tsx
<textarea
  value={state.description}
  onChange={(e) => setState({...prev, description: e.target.value})}
  rows={6}
  placeholder="Опишите объект недвижимости..."
/>
```

**Что описывать:**
- Тип объекта (вилла, апартаменты, комната)
- Количество спален и ванных
- Расстояние до пляжа
- Удобства (WiFi, бассейн, парковка, завтрак)
- Особенности (вид на море, сад, безопасность)
- Район расположения
- Цена за ночь

**Подсказка:**
```
💡 Пример: "Роскошная 3-комнатная вилла в Унаватуне. 
    50м до пляжа, приватный бассейн, WiFi 50 Мбит/с..."
```

**Кнопка анализа:** "🤖 Анализировать с AI"

---

### ШАГ 3: Обработка AI + Результат 🧠

**Триггер:** Нажатие "🤖 Анализировать с AI"

#### Процесс обработки:

**1. Разворачивание короткой ссылки** (если нужно)
```tsx
fetch('/api/expand-url', {
  method: 'POST',
  body: JSON.stringify({ url: googleMapsInput })
})
```

**2. Извлечение координат**
```tsx
const parsedCoords = await parseGoogleMapsURL(finalUrl);
```
- Функция: `utils/googleMapsParser.ts`
- Парсит URL или координаты
- Проверяет валидность для Шри-Ланки

**3. Анализ с помощью Groq AI**
```tsx
const result = await analyzePropertyWithGroqCached(
  state.description,
  { lat: parsedCoords.lat, lng: parsedCoords.lng },
  state.images.concat(state.imageUrls)
);
```

**API:** `services/groqService.ts`
- Модель: **llama-3.2-90b-vision-preview**
- Анализирует описание и фото
- Извлекает структурированные данные
- Кэширование результатов в localStorage

**4. Валидация с Google Places API**
```tsx
const validation = await fetch('/api/validate-poi', {
  method: 'POST',
  body: JSON.stringify({
    name: result.title,
    lat: parsedCoords.lat,
    lng: parsedCoords.lng
  })
});
```

**Что проверяется:**
- Существует ли место в Google Places
- Рейтинг и отзывы
- Реальное название
- Категория места
- Рабочие часы

#### Результат анализа:

**Карточка с данными:**
```tsx
{
  title: "Villa Ceylon Avenue",
  price: 425,
  rooms: 3,
  bathrooms: 3,
  beachDistance: 50,
  area: "Unawatuna",
  propertyType: "villa",
  wifiSpeed: 50,
  pool: true,
  parking: true,
  breakfast: true,
  petFriendly: false,
  security: "gated",
  amenities: ["Air Conditioning", "Pool", "Wifi"...],
  images: [...],
  coordinates: [6.0335, 80.2168]
}
```

**Отображение:**
- ✅ Зеленая галочка "Готово!"
- Список всех параметров
- Предпросмотр фото
- Координаты на мини-карте (опционально)

**Кнопки:**
- "✅ Добавить на карту" → вызывает `onImport(property)`
- "← Назад" → возврат к редактированию
- "❌ Отмена" → закрыть модал

---

## 📦 Результат импорта

### Что происходит после "Добавить на карту":

**1. Сохранение в state (Explorer.tsx)**
```tsx
setCustomProperties(prev => {
  const updated = [...prev, newProperty];
  localStorage.setItem('customProperties', JSON.stringify(updated));
  return updated;
});
```

**2. Сохранение в localStorage**
- Ключ: `customProperties`
- Формат: JSON массив объектов
- Персистентность: данные сохраняются между сессиями

**3. Отображение на карте**
- Маркер 🏠 (тип: 'stay')
- Позиция: `[lat, lng]`
- Радиус поиска: 2 км (если объект выбран)

**4. Доступность в фильтрах**
- Слой: "Supporting Point 🏠"
- Фильтры: цена, район, удобства, etc.
- Drawer: полная карточка с описанием

---

## 🛠️ Ключевые функции

### 1. parseGoogleMapsURL() - Парсинг координат

**Файл:** `src/utils/googleMapsParser.ts`

**Что парсит:**
```typescript
// URL форматы
"https://www.google.com/maps/@6.0335,80.2168,17z"
"https://goo.gl/maps/abc" // + разворачивание
"maps.app.goo.gl/xyz"     // + разворачивание

// Текстовые форматы
"6.0335, 80.2168"
"@6.0335,80.2168"
"6°02'00.6\"N 80°13'00.5\"E" // DMS формат
```

**Возврат:**
```typescript
{
  lat: 6.0335,
  lng: 80.2168,
  placeName?: "Villa Name" // если есть в URL
}
```

### 2. analyzePropertyWithGroqCached() - AI анализ

**Файл:** `src/services/groqService.ts`

**Входные данные:**
- `description: string` - текстовое описание
- `coordinates: {lat, lng}` - координаты
- `images: string[]` - массив фото (base64 или URL)

**Процесс:**
1. Проверяет кэш (localStorage)
2. Если нет в кэше → отправляет в Groq API
3. Получает структурированный JSON
4. Сохраняет в кэш
5. Возвращает результат

**Модель:** llama-3.2-90b-vision-preview
- Поддерживает vision (анализ фото)
- Быстрый (90B параметров)
- Бесплатный лимит: ~100 запросов/день

**Промпт:** (упрощенно)
```
Analyze this property in Sri Lanka.
Description: {description}
Coordinates: {lat}, {lng}
Images: {images}

Return JSON with: title, price, rooms, bathrooms, beachDistance,
propertyType, amenities, pool, parking, wifi...
```

### 3. Валидация через Google Places API

**Endpoint:** `/api/validate-poi`

**Файл:** `src/pages/api/validate-poi.ts`

**Процесс:**
1. Nearby Search - поиск в радиусе 50м
2. Place Details - получение полной информации
3. Сравнение с данными AI
4. Возврат статуса валидности

**Результат:**
```typescript
{
  exists: true,
  placeId: "ChIJ...",
  name: "Villa Ceylon",
  rating: 4.5,
  totalReviews: 120,
  types: ["lodging", "point_of_interest"],
  confidence: "high" | "medium" | "low"
}
```

---

## 🎨 UI/UX особенности

### Модальное окно:
- **Размер:** `max-w-4xl` (широкое)
- **Высота:** `max-h-[90vh]` (адаптивная)
- **Прокрутка:** внутри модала
- **Фон:** `bg-black/50` (затемнение)
- **z-index:** 3000 (поверх всего)

### Анимации:
- **Появление модала:** fade-in + scale
- **Смена шагов:** slide-in-from-right
- **AI обработка:** пульсирующий спиннер
- **Загрузка фото:** плавное появление превью

### Индикаторы прогресса:
```
Шаг 1/3 • Фотографии
━━━━━━━━━━━━━━━━━━━━━━

Шаг 2/3 • Информация
━━━━━━━━━━━━━━━━━━━━━━

Шаг 3/3 • Проверка
━━━━━━━━━━━━━━━━━━━━━━
```

---

## 🔌 Интеграция с картой

### Explorer.tsx связывание:

**State:**
```tsx
const [isImporterOpen, setIsImporterOpen] = useState(false);
const [customProperties, setCustomProperties] = useState<any[]>([]);
```

**Кнопка Import:**
```tsx
<button onClick={() => setIsImporterOpen(true)}>
  🤖 Import
</button>
```

**Модал:**
```tsx
{isImporterOpen && (
  <PropertyImporterAI
    onImport={handleImportProperty}
    onClose={() => setIsImporterOpen(false)}
  />
)}
```

**Обработчик импорта:**
```tsx
const handleImportProperty = (newProperty: any) => {
  setCustomProperties(prev => {
    const updated = [...prev, newProperty];
    localStorage.setItem('customProperties', JSON.stringify(updated));
    return updated;
  });
  setIsImporterOpen(false);
  console.log(`✅ Объект "${newProperty.title}" добавлен!`);
};
```

**Отображение на карте:**
```tsx
const allMarkers = [
  ...(activeLayers.includes('stay') 
    ? [...PROPERTIES, ...customProperties].map(p => ({
        ...p,
        type: 'stay',
        priceLKR: formatPriceLKR(p.rawPrice)
      })) 
    : []),
  ...filteredPOIs
];
```

---

## 📱 Поддерживаемые форматы данных

### Изображения:
- **JPEG, PNG, WebP** - основные форматы
- **Base64** - загруженные с диска
- **URL** - внешние ссылки
- **Clipboard** - вставка из буфера обмена

### Координаты:
- **Decimal degrees:** `6.0335, 80.2168`
- **Google Maps URL:** полные и короткие ссылки
- **DMS:** градусы, минуты, секунды
- **Валидация:** только Шри-Ланка (5-10°N, 79-82°E)

### Описание:
- **Свободный текст** - любой язык
- **Структурированное** - предпочтительно
- **Минимум:** название + тип + локация
- **Рекомендуемое:** 200-500 символов

---

## 🚀 Производительность

### Кэширование:
1. **AI результаты** - localStorage (по description+coords)
2. **Валидация** - sessionStorage (по placeId)
3. **Изображения** - base64 в памяти
4. **Custom properties** - localStorage (массив)

### Оптимизация:
- Lazy loading модала (только при открытии)
- Дебаунсинг ввода (300ms)
- Компрессия изображений (опционально)
- Кэш API запросов (24 часа)

---

## 🔒 Безопасность

### Валидация входных данных:
- ✅ Координаты в пределах Шри-Ланки
- ✅ Цена > 0 и < 10000
- ✅ Rooms/bathrooms: 1-20
- ✅ Beach distance: 0-10000m
- ✅ URL фото начинаются с http/https

### Санитизация:
- Escape HTML в описании
- Проверка типов файлов
- Лимит размера файлов (5MB)
- CORS для API запросов

---

## 📊 Типы данных (TypeScript)

### PropertyData:
```typescript
interface PropertyData {
  id: string;
  position: [number, number];
  title: string;
  price: string;
  rawPrice: number;
  rooms: number;
  bathrooms: number;
  beachDistance: number;
  area: string;
  propertyType: 'villa' | 'apartment' | 'house' | 'room';
  wifiSpeed: number;
  pool: boolean;
  parking: boolean;
  breakfast: boolean;
  petFriendly: boolean;
  security: 'standard' | 'high' | 'gated';
  type: 'stay';
  description: string;
  amenities: string[];
  images: string[];
}
```

### ImportState:
```typescript
interface ImportState {
  step: 1 | 2 | 3;
  coordinates: [number, number] | null;
  images: string[];
  imageUrls: string[];
  description: string;
  aiProcessing: boolean;
  aiResult: PropertyData | null;
  quickResult: any | null;
  error: string | null;
  sources: string[];
}
```

---

## 🎯 Итоговый результат

После успешного импорта пользователь получает:

1. ✅ **Объект на карте** - маркер 🏠 в нужном месте
2. ✅ **Полная карточка** - все данные в PropertyDrawer
3. ✅ **Фильтрация** - работает со всеми фильтрами
4. ✅ **Сохранение** - данные в localStorage
5. ✅ **Редактирование** - можно удалить через карточку
6. ✅ **Персистентность** - данные между сессиями

---

## 🔄 Альтернативный метод: PropertyImporter.tsx

**Отличия от AI версии:**
- ❌ Нет AI анализа
- ❌ Нет валидации Google Places
- ✅ Полностью ручной ввод
- ✅ Быстрее (нет API запросов)
- ✅ Больше контроля
- ✅ Работает оффлайн

**Использование:** Запасной вариант, если AI не работает

---

## 📝 Примеры использования

### Пример 1: Импорт виллы
1. Загрузить 3-5 фото виллы
2. Вставить: `https://goo.gl/maps/abc123`
3. Описать: "3-bedroom villa, 50m to beach, pool, WiFi 50Mbps"
4. Нажать "Анализировать с AI"
5. Проверить результат
6. "Добавить на карту"

### Пример 2: Быстрый импорт
1. Skip фото (опционально)
2. Вставить координаты: `6.0335, 80.2168`
3. Краткое описание: "Budget room, Mirissa"
4. AI дополнит данные
5. Добавить

---

## 💡 Советы и лучшие практики

### Для лучших результатов AI:
- 📸 Загрузить 3-5 качественных фото
- 📝 Подробное описание (200+ символов)
- 🏷️ Указать тип объекта явно
- 💰 Указать цену
- 📍 Точные координаты
- 🌐 Проверить район (area)

### Типичные ошибки:
- ❌ Короткие ссылки без разворачивания → использовать API
- ❌ Координаты вне Шри-Ланки → проверить
- ❌ Нет описания → AI не может проанализировать
- ❌ Слишком много фото (>10) → медленная загрузка

---

## 🎉 Заключение

Вкладка **Import** - это мощный инструмент для:
- Быстрого добавления объектов недвижимости
- AI-анализа описаний и фотографий
- Валидации данных через Google Places API
- Сохранения пользовательских объектов

**Результат:** Пользователь может создать свою коллекцию Supporting Points за минуты! 🚀
