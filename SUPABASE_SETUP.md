# 🚀 Настройка Supabase для сохранения объектов недвижимости

## 📋 Шаг 1: Создание проекта в Supabase

1. Перейдите на [https://supabase.com](https://supabase.com)
2. Нажмите **"Start your project"** или **"New Project"**
3. Войдите через GitHub или создайте аккаунт
4. Создайте новый проект:
   - **Name**: `sri-lanka-properties` (или любое другое имя)
   - **Database Password**: Создайте надежный пароль (сохраните его!)
   - **Region**: Выберите ближайший регион (например, `Southeast Asia (Singapore)`)
   - **Pricing Plan**: Выберите **Free** (достаточно для старта)
5. Нажмите **"Create new project"**
6. Дождитесь создания проекта (~2 минуты)

---

## 🔑 Шаг 2: Получение API ключей

1. После создания проекта перейдите в **Project Settings** (иконка шестеренки внизу слева)
2. Выберите **API** в меню слева
3. Скопируйте следующие значения:
   - **Project URL** → `PUBLIC_SUPABASE_URL`
   - **anon public** (API key) → `PUBLIC_SUPABASE_ANON_KEY`

4. Откройте файл `.env` в корне проекта
5. Замените значения:
   ```env
   PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
   PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
   ```

---

## 🗄️ Шаг 3: Создание таблицы базы данных

1. В Supabase перейдите в **SQL Editor** (иконка ⚡ слева)
2. Нажмите **"+ New query"**
3. Откройте файл `supabase_schema.sql` из корня проекта
4. Скопируйте **весь SQL код** из файла
5. Вставьте код в SQL Editor
6. Нажмите **"Run"** (или `Ctrl+Enter`)
7. Дождитесь выполнения (должно появиться сообщение "Success")

### ✅ Что создается:
- ✅ Таблица `properties` для хранения объектов недвижимости
- ✅ Row Level Security (RLS) - каждый пользователь видит только свои объекты
- ✅ Индексы для быстрого поиска
- ✅ Triggers для автообновления `updated_at`
- ✅ Storage bucket для изображений
- ✅ Полезные SQL функции

---

## 🔐 Шаг 4: Настройка аутентификации

### Email/Password (включен по умолчанию)
1. Перейдите в **Authentication** → **Providers**
2. Убедитесь, что **Email** включен (зеленая галочка)
3. Включите **"Confirm email"** если хотите подтверждение по email

### Google OAuth (опционально, но рекомендуется)
1. Перейдите в **Authentication** → **Providers**
2. Найдите **Google** и нажмите на него
3. Включите провайдер
4. Следуйте инструкциям для получения:
   - **Client ID**
   - **Client Secret**
   
   **Получение Google OAuth credentials:**
   1. Перейдите в [Google Cloud Console](https://console.cloud.google.com)
   2. Создайте новый проект или выберите существующий
   3. Перейдите в **APIs & Services** → **Credentials**
   4. Нажмите **"Create Credentials"** → **"OAuth 2.0 Client ID"**
   5. Выберите **"Web application"**
   6. В **"Authorized redirect URIs"** добавьте:
      ```
      https://your-project-ref.supabase.co/auth/v1/callback
      ```
   7. Скопируйте **Client ID** и **Client Secret**
   8. Вставьте их в Supabase

5. Сохраните изменения

---

## 🖼️ Шаг 5: Настройка Storage (для изображений)

1. Перейдите в **Storage** (иконка 📦 слева)
2. Bucket `property-images` уже создан через SQL
3. Если нужно создать вручную:
   - Нажмите **"New bucket"**
   - Name: `property-images`
   - Public: **Yes** (чтобы изображения были доступны по URL)
   - File size limit: `50 MB` (по умолчанию)
   - Allowed MIME types: `image/*`

---

## 🧪 Шаг 6: Тестирование подключения

1. Убедитесь, что dev сервер запущен:
   ```bash
   npm run dev
   ```

2. Откройте приложение в браузере: `http://localhost:4321`

3. Откройте консоль браузера (F12)

4. Проверьте подключение - не должно быть ошибок о Supabase

5. Попробуйте зарегистрироваться:
   - Нажмите на кнопку авторизации (будет добавлена позже)
   - Зарегистрируйтесь через email или Google
   - Проверьте, что пользователь создался в **Authentication** → **Users**

---

## 📊 Шаг 7: Проверка таблицы

1. Перейдите в **Table Editor** (иконка 📊 слева)
2. Выберите таблицу `properties`
3. Вы должны увидеть структуру таблицы с колонками:
   - `id`, `user_id`, `title`, `property_type`, и т.д.
4. После импорта объектов через приложение, они появятся здесь

---

## 🔒 Безопасность

### ✅ Что уже настроено:
- **Row Level Security (RLS)** - пользователи видят только свои объекты
- **Encrypted connections** - все запросы через HTTPS
- **JWT tokens** - безопасная авторизация
- **Password hashing** - пароли хранятся в зашифрованном виде
- **Email verification** - подтверждение email (опционально)

### 🛡️ Политики безопасности:
```sql
-- Только владелец может читать свои объекты
Users can view own properties

-- Только владелец может создавать объекты
Users can create own properties

-- Только владелец может обновлять свои объекты
Users can update own properties

-- Только владелец может удалять свои объекты
Users can delete own properties
```

---

## 🎯 Следующие шаги

После настройки Supabase:

1. ✅ Завершите интеграцию с компонентом `PropertyImporterAI`
2. ✅ Добавьте кнопку авторизации в интерфейс
3. ✅ Замените `localStorage` на Supabase для сохранения объектов
4. ✅ Добавьте синхронизацию объектов между устройствами
5. ✅ (Опционально) Добавьте загрузку изображений в Storage

---

## 🆘 Troubleshooting

### Ошибка: "Missing Supabase environment variables"
- Проверьте, что `.env` файл существует
- Убедитесь, что переменные начинаются с `PUBLIC_`
- Перезапустите dev сервер после изменения `.env`

### Ошибка: "Invalid API key"
- Проверьте, что скопировали **anon public** ключ (не service_role!)
- Убедитесь, что нет лишних пробелов в `.env`

### Ошибка: "Row Level Security policy violation"
- Убедитесь, что пользователь авторизован
- Проверьте, что RLS политики созданы в SQL Editor

### Ошибка: "relation 'properties' does not exist"
- Выполните SQL схему из файла `supabase_schema.sql`
- Проверьте выполнение в SQL Editor

---

## 📚 Полезные ссылки

- [Supabase Documentation](https://supabase.com/docs)
- [Row Level Security](https://supabase.com/docs/guides/auth/row-level-security)
- [Storage Guide](https://supabase.com/docs/guides/storage)
- [Auth Providers](https://supabase.com/docs/guides/auth/social-login)

---

## 💡 Бесплатный план Supabase включает:

- ✅ 500 MB Database space
- ✅ 1 GB File storage
- ✅ 2 GB Bandwidth
- ✅ 50,000 Monthly Active Users
- ✅ Unlimited API requests
- ✅ Social OAuth providers

**Этого достаточно для старта и тестирования!** 🚀
