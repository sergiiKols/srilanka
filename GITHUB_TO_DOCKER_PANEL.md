# 🚀 Развертывание из GitHub через Docker Hub

## 📋 План действий

1. ✅ Создать аккаунт Docker Hub (если нет)
2. ✅ Настроить GitHub Secrets
3. ✅ Запушить код с GitHub Actions
4. ✅ Создать сервис в панели Docker
5. ✅ Настроить переменные окружения
6. ✅ Запустить!

---

## ШАГ 1: Docker Hub аккаунт (2 минуты)

### Если НЕТ аккаунта:
1. Зайдите на https://hub.docker.com/signup
2. Зарегистрируйтесь (бесплатно)
3. Запомните **username** (например: `myusername`)

### Если ЕСТЬ аккаунт:
- Вспомните свой **username**
- Сгенерируйте **Access Token**:
  1. https://hub.docker.com/settings/security
  2. New Access Token
  3. Description: `GitHub Actions`
  4. Permissions: `Read, Write, Delete`
  5. Generate → **скопируйте токен** (показывается один раз!)

---

## ШАГ 2: Настроить GitHub Secrets (3 минуты)

1. Откройте ваш репозиторий на GitHub
2. **Settings** (вкладка сверху)
3. **Secrets and variables** → **Actions**
4. **New repository secret**

Добавьте 2 секрета:

### Секрет 1:
- Name: `DOCKER_USERNAME`
- Value: `ваш_docker_hub_username`
- Add secret

### Секрет 2:
- Name: `DOCKER_PASSWORD`
- Value: `ваш_access_token` (из Шага 1)
- Add secret

---

## ШАГ 3: Обновить GitHub Actions workflow (уже готов!)

Файл `.github/workflows/docker-deploy.yml` уже создан!

Нужно только изменить одну строку:

```yaml
# Найдите строку:
IMAGE_NAME: ghcr.io/${{ github.repository }}

# Замените на:
IMAGE_NAME: ваш_docker_username/sri-lanka
```

Или я могу создать упрощенную версию прямо сейчас!

---

## ШАГ 4: Запушить в GitHub

После того как обновим workflow:

```bash
git add .
git commit -m "Add Docker deployment"
git push origin main
```

GitHub Actions автоматически:
1. ✅ Соберет Docker образ
2. ✅ Запушит в Docker Hub
3. ✅ Образ будет доступен: `username/sri-lanka:latest`

---

## ШАГ 5: Создать сервис в панели Docker

Сейчас вы на экране "No services added yet"

Нажмите **"+ Create Service"**

Укажите:
- **Name**: `sri-lanka-app`
- **Image**: `ваш_username/sri-lanka:latest`
- **Port**: `3000:80` (или `3000:3000`)

---

## 🎯 Сейчас что делаем?

**Вариант 1:** У вас уже есть Docker Hub аккаунт?
- **Да** → дайте username, я обновлю workflow
- **Нет** → создайте за 2 минуты, затем дайте username

**Вариант 2:** Хотите, чтобы я создал упрощенный workflow прямо сейчас?
- Скажите "создавай" и ваш Docker Hub username

Что выбираете? 🚀
