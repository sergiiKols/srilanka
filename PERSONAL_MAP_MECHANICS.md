# 🗺️ МЕХАНИКА ИНДИВИДУАЛЬНЫХ КАРТ

**Дата:** 2026-01-29  
**Цель:** Объяснить как создаются, хранятся и работают личные карты

---

## 🎯 КОНЦЕПЦИЯ

### Одно предложение:
**У каждого арендатора есть своя уникальная карта с URL привязанным к его Telegram ID, на которой отображаются только его сохранённые объекты.**

---

## 📍 ГДЕ И КОГДА СОЗДАЁТСЯ КАРТА

### Момент создания: При первом `/start`

```
Пользователь → Нажимает /start в боте
    ↓
Бот проверяет: Есть ли этот user в БД?
    ↓
  ┌─────────┴─────────┐
  │                   │
НЕТ                 ДА
  │                   │
  ↓                   ↓
СОЗДАТЬ           ПОКАЗАТЬ
запись            меню
в БД
  │
  ↓
Генерируется URL карты:
/map/{telegram_user_id}
  │
  ↓
Сохраняется в таблице tenants
```

---

## 🗄️ КАК ХРАНИТСЯ

### Таблица: `tenants`

```sql
CREATE TABLE tenants (
  id UUID PRIMARY KEY,
  telegram_user_id BIGINT UNIQUE NOT NULL,  ← Telegram ID пользователя
  personal_map_url TEXT UNIQUE,              ← URL карты
  created_at TIMESTAMPTZ
);

-- Пример записи:
INSERT INTO tenants VALUES (
  'uuid-123-456',
  1000089271,                                     ← Telegram ID
  'https://site.com/map/1000089271',              ← URL карты
  NOW()
);
```

**Важно:**
- `telegram_user_id` = уникальный ID пользователя из Telegram
- `personal_map_url` = готовая ссылка на карту
- URL карты = `/map/` + `telegram_user_id`

---

## 🔗 ФОРМАТ URL КАРТЫ

### Структура:
```
https://srilanka-37u2.vercel.app/map/{telegram_user_id}
                                     ^^^^^^^^^^^^^^^^
                                     ID пользователя из Telegram
```

### Примеры:
```
Пользователь с ID 1000089271:
→ https://site.com/map/1000089271

Пользователь с ID 555666777:
→ https://site.com/map/555666777

Пользователь с ID 123456789:
→ https://site.com/map/123456789
```

**Каждый ID = уникальная карта!**

---

## 📦 ПРИВЯЗКА ОБЪЕКТОВ К КАРТЕ

### Таблица: `saved_properties`

```sql
CREATE TABLE saved_properties (
  id UUID PRIMARY KEY,
  tenant_id UUID REFERENCES tenants(id),     ← Связь с арендатором
  telegram_user_id BIGINT NOT NULL,          ← Дублируем для быстрого поиска
  
  -- Данные объекта
  photos TEXT[],
  latitude DECIMAL(10, 8),
  longitude DECIMAL(11, 8),
  description TEXT,
  
  created_at TIMESTAMPTZ
);

-- Индекс для быстрого поиска объектов по пользователю
CREATE INDEX idx_saved_props_user ON saved_properties(telegram_user_id);
```

### Как происходит привязка:

```
Арендатор отправляет объект в бота
    ↓
Бот получает telegram_user_id
    ↓
Находит tenant в БД по telegram_user_id
    ↓
Создаёт запись в saved_properties:
  - tenant_id = ID арендатора из tenants
  - telegram_user_id = ID из Telegram (дублируем)
  - photos, coordinates, description...
    ↓
Объект привязан к карте арендатора!
```

---

## 🎨 КАК РАБОТАЕТ ОТОБРАЖЕНИЕ

### Когда пользователь открывает карту:

```
1. Пользователь открывает:
   https://site.com/map/1000089271
   
2. Сервер получает параметр из URL:
   telegram_user_id = 1000089271
   
3. Запрос к БД:
   SELECT * FROM saved_properties 
   WHERE telegram_user_id = 1000089271
   
4. Получены объекты этого пользователя:
   [
     { id: 1, photos: [...], lat: 6.92, lng: 79.86, ... },
     { id: 2, photos: [...], lat: 6.93, lng: 79.85, ... },
     { id: 3, photos: [...], lat: 6.94, lng: 79.87, ... }
   ]
   
5. Отображаем на карте:
   - Создаём маркеры для каждого объекта
   - Центрируем карту по объектам
   - Добавляем popups с информацией
```

---

## 🔐 ПРИВАТНОСТЬ

### Кто может видеть карту?

**Технически:** Любой у кого есть ссылка  
**Практически:** Только сам пользователь

### Почему приватно:

```
URL содержит Telegram ID пользователя:
/map/1000089271

Чтобы открыть чужую карту нужно:
1. Знать Telegram ID другого человека
2. Угадать или узнать этот ID

Telegram ID:
- Не показывается публично
- Известен только самому пользователю
- Сложно угадать (миллиарды комбинаций)
```

### Дополнительная защита (опционально):

```sql
-- Можно добавить проверку владельца
CREATE TABLE map_access (
  telegram_user_id BIGINT,
  access_token TEXT UNIQUE,  -- Секретный токен
  created_at TIMESTAMPTZ
);

-- Тогда URL будет:
/map/{telegram_user_id}?token={secret_token}
```

Но для MVP это избыточно - достаточно просто Telegram ID в URL.

---

## 📱 ОТКУДА БЕРЁТСЯ ССЫЛКА НА КАРТУ

### Бот отправляет ссылку в разных местах:

#### 1. При первом `/start`:
```
👋 Привет! Твоя личная карта создана:

🗺️ https://site.com/map/1000089271

На карте будут отображаться все объекты 
которые ты сохранишь.

[📤 Добавить объект]
```

#### 2. После сохранения объекта:
```
✅ Объект сохранён!

📊 Всего сохранено: 5 объектов

[🗺️ Открыть карту]  ← Кнопка с URL
```

#### 3. Кнопка "Моя карта" в меню:
```
/start → Меню:

[📤 Добавить объект]  [🗺️ Моя карта]
                       ^^^^^^^^^^^^
                       URL: /map/{user_id}
```

---

## 🔄 ЖИЗНЕННЫЙ ЦИКЛ КАРТЫ

### 1. Создание (первый /start):
```
Пользователь: /start
    ↓
Бот: Проверка в БД
    ↓
Создание записи:
  tenants {
    telegram_user_id: 1000089271,
    personal_map_url: '/map/1000089271'
  }
    ↓
Карта готова! (пустая)
```

### 2. Добавление объектов:
```
Пользователь: Отправляет фото + данные
    ↓
Бот: Сохраняет в saved_properties
    ↓
saved_properties {
  telegram_user_id: 1000089271,
  photos: [...],
  coordinates: {...}
}
    ↓
Объект появился на карте!
```

### 3. Просмотр карты:
```
Пользователь: Открывает /map/1000089271
    ↓
Сервер: SELECT * WHERE telegram_user_id = 1000089271
    ↓
Отображение всех объектов пользователя
```

### 4. Удаление объекта:
```
Пользователь: Удаляет объект
    ↓
Бот: DELETE FROM saved_properties WHERE id = X
    ↓
Объект исчез с карты
```

### 5. Карта существует вечно:
```
Запись в tenants НЕ удаляется
Карта доступна всегда
Даже если нет объектов - показывается пустая карта
```

---

## 🎬 ПРИМЕРЫ СЦЕНАРИЕВ

### Сценарий 1: Новый пользователь

```
1. Иван открывает бота → /start
2. Бот создаёт запись:
   tenants { telegram_user_id: 111, url: '/map/111' }
3. Бот: "Твоя карта: https://site.com/map/111"
4. Иван открывает → пустая карта
5. Иван добавляет 3 объекта → на карте 3 маркера
6. Иван открывает карту → видит свои 3 объекта
```

### Сценарий 2: Возвращающийся пользователь

```
1. Мария уже использовала бота месяц назад
2. У неё уже есть запись в tenants
3. Мария: /start
4. Бот: "С возвращением! У тебя 12 сохранённых объектов"
5. Мария: [🗺️ Моя карта]
6. Открывается /map/222 → видит все 12 объектов
```

### Сценарий 3: Несколько пользователей

```
Иван (ID: 111):
→ Карта: /map/111
→ Объекты: 5 штук в Негомбо
→ Видит только свои 5 объектов

Мария (ID: 222):
→ Карта: /map/222
→ Объекты: 12 штук в Коломбо
→ Видит только свои 12 объектов

Петр (ID: 333):
→ Карта: /map/333
→ Объекты: 2 штуки в Галле
→ Видит только свои 2 объекта

Карты НЕ пересекаются!
Каждый видит только свои объекты!
```

---

## 🔍 ПОИСК ОБЪЕКТОВ

### Запрос к БД:

```sql
-- Для карты пользователя 1000089271
SELECT 
  id,
  photos,
  latitude,
  longitude,
  description,
  price,
  created_at
FROM saved_properties
WHERE telegram_user_id = 1000089271
ORDER BY created_at DESC;

-- Результат:
-- [ объект_1, объект_2, объект_3, ... ]
-- Только объекты этого пользователя!
```

### На странице карты:

```javascript
// src/pages/map/[telegram_user_id].astro

export const prerender = false; // SSR страница

const { telegram_user_id } = Astro.params;

// Загружаем объекты только этого пользователя
const { data: properties } = await supabase
  .from('saved_properties')
  .select('*')
  .eq('telegram_user_id', telegram_user_id);

// properties = массив объектов пользователя
// Отображаем их на карте
```

---

## 📊 СТАТИСТИКА

### Хранится в таблице tenants:

```sql
CREATE TABLE tenants (
  id UUID PRIMARY KEY,
  telegram_user_id BIGINT,
  personal_map_url TEXT,
  saved_properties_count INT DEFAULT 0,  ← Счётчик объектов
  last_active_at TIMESTAMPTZ,            ← Последняя активность
  created_at TIMESTAMPTZ
);

-- Обновление счётчика при добавлении объекта:
UPDATE tenants 
SET saved_properties_count = saved_properties_count + 1,
    last_active_at = NOW()
WHERE telegram_user_id = 1000089271;
```

---

## ✅ ИТОГОВАЯ СХЕМА

```
┌─────────────────────────────────────────────┐
│  ПОЛЬЗОВАТЕЛЬ (Telegram ID: 1000089271)     │
└─────────────────────┬───────────────────────┘
                      │
                      ↓
          ┌───────────────────────┐
          │   Таблица: tenants    │
          ├───────────────────────┤
          │ telegram_user_id: 111 │
          │ personal_map_url:     │
          │ "/map/111"            │
          └───────────┬───────────┘
                      │
                      ↓
         ┌────────────────────────┐
         │ Таблица:               │
         │ saved_properties       │
         ├────────────────────────┤
         │ telegram_user_id: 111  │
         │ photos: [...]          │
         │ coordinates: {...}     │
         ├────────────────────────┤
         │ telegram_user_id: 111  │
         │ photos: [...]          │
         │ coordinates: {...}     │
         ├────────────────────────┤
         │ telegram_user_id: 111  │
         │ photos: [...]          │
         │ coordinates: {...}     │
         └────────────┬───────────┘
                      │
                      ↓
         ┌────────────────────────┐
         │  КАРТА: /map/111       │
         │                        │
         │   📍 Объект 1          │
         │   📍 Объект 2          │
         │   📍 Объект 3          │
         │                        │
         │ Видит только владелец! │
         └────────────────────────┘
```

---

## 🎯 КРАТКИЕ ОТВЕТЫ НА ВОПРОСЫ

### По какой команде создаётся?
**`/start`** - при первом запуске бота

### Где создаётся?
**В таблице `tenants`** - создаётся запись с URL карты

### Как хранится?
```
tenants: личные данные + URL карты
saved_properties: объекты привязанные к telegram_user_id
```

### Как привязываются объекты?
**По `telegram_user_id`** - каждый объект содержит ID владельца

### Как отображается?
**SSR страница** `/map/[telegram_user_id]` загружает объекты по ID из URL

---

**Всё понятно? Механика карт ясна?** 🗺️
