# 🧩 ПРОБЛЕМА ГРУППИРОВКИ СООБЩЕНИЙ

**Дата:** 2026-01-29  
**Проблема:** Как понять что несколько сообщений относятся к одному объекту?

---

## 🔴 ПРОБЛЕМА

### Сценарий:
```
Арендатор хочет сохранить 10 объектов подряд.
Каждый объект = 3-5 сообщений:
  - Фото 1
  - Фото 2
  - Фото 3
  - Текст с описанием
  - Google Maps ссылка

ВОПРОС: Как бот поймёт где заканчивается объект #1 и начинается объект #2?
```

### Пример потока сообщений:
```
[Сообщение 1] Фото виллы в Негомбо
[Сообщение 2] Ещё фото виллы
[Сообщение 3] https://maps.google.com/?q=6.9271,79.8612
[Сообщение 4] 2BR, $800/month
[Сообщение 5] Фото дома в Коломбо ← НОВЫЙ ОБЪЕКТ!
[Сообщение 6] Ещё фото дома
[Сообщение 7] https://maps.google.com/?q=6.9333,79.8500
[Сообщение 8] 3BR, $1200/month
...
```

**Проблема:** Бот не знает что сообщения 1-4 = объект #1, а 5-8 = объект #2.

---

## 💡 ВОЗМОЖНЫЕ РЕШЕНИЯ

### РЕШЕНИЕ 1: Команда-разделитель (самое простое) ⭐

**Концепция:**
Арендатор явно указывает когда начинается новый объект.

**Как работает:**
```
Арендатор отправляет:

/new - начинаю новый объект
[Фото 1]
[Фото 2]
https://maps.google.com/?q=6.9271,79.8612
2BR villa, $800/month

/new - ещё один объект
[Фото 3]
https://maps.google.com/?q=6.9333,79.8500
3BR house, $1200/month

/done - всё, сохранить
```

**Преимущества:**
✅ Простая реализация
✅ Понятно пользователю
✅ Точный контроль

**Недостатки:**
❌ Нужно вводить команды
❌ Дополнительные действия

---

### РЕШЕНИЕ 2: Кнопки "Добавить" / "Готово" (UI-friendly) ⭐⭐⭐

**Концепция:**
Используем inline кнопки для управления процессом.

**Как работает:**

**Шаг 1:** Арендатор нажимает "📤 Добавить объект"

Бот отвечает:
```
📤 Добавление объекта #1

Отправь мне:
• Фото (можно несколько)
• Google Maps ссылку
• Описание

[✅ Готово, сохранить]  [➕ Ещё объект]
```

**Шаг 2:** Арендатор отправляет данные:
```
[Фото 1]
[Фото 2]
https://maps.google.com/?q=6.9271,79.8612
2BR villa, $800/month
```

Бот накапливает в памяти:
```javascript
userSessions[userId] = {
  currentObject: 1,
  objects: [
    {
      id: 1,
      photos: [url1, url2],
      text: "...",
      googleMapsUrl: "..."
    }
  ]
};
```

**Шаг 3:** Арендатор нажимает кнопку:

**Вариант A:** "➕ Ещё объект"
```
Бот отвечает:
📤 Добавление объекта #2

Отправь данные для следующего объекта.

[✅ Готово, сохранить]  [➕ Ещё объект]
```

**Вариант B:** "✅ Готово, сохранить"
```
Бот сохраняет все объекты и отвечает:
✅ Сохранено 2 объекта!

Объект #1: 2BR Villa, Negombo
Объект #2: 3BR House, Colombo

[🗺️ Открыть карту]
```

**Преимущества:**
✅ Удобный UI с кнопками
✅ Понятный процесс
✅ Можно добавлять много объектов за раз
✅ Визуальная обратная связь (объект #1, #2, #3...)

**Недостатки:**
❌ Нужно нажимать кнопки
❌ Чуть сложнее реализация (состояния)

---

### РЕШЕНИЕ 3: Автоматическая группировка по времени (рискованное)

**Концепция:**
Бот считает что сообщения в течении N секунд = один объект.

**Как работает:**
```
Логика:
- Если между сообщениями < 60 секунд → один объект
- Если пауза > 60 секунд → новый объект
- Google Maps URL = триггер "объект завершён"
```

**Пример:**
```
10:00:00 - [Фото 1]              |
10:00:05 - [Фото 2]              | Объект #1
10:00:10 - https://maps...       | (пауза < 60 сек)
10:00:15 - 2BR villa             |

--- пауза 90 секунд ---

10:01:45 - [Фото 3]              |
10:01:50 - https://maps...       | Объект #2
10:01:55 - 3BR house             |
```

**Преимущества:**
✅ Автоматическая группировка
✅ Не требует команд от пользователя

**Недостатки:**
❌ Ненадёжно (пользователь может медленно печатать)
❌ Может объединить разные объекты
❌ Может разбить один объект на два
❌ Сложная логика таймеров

---

### РЕШЕНИЕ 4: Детекция Google Maps как маркера завершения (полуавтомат) ⭐⭐

**Концепция:**
Google Maps URL = "объект готов, сохранить".

**Как работает:**
```
Логика:
1. Пользователь отправляет фото → накапливаем
2. Пользователь отправляет текст → накапливаем
3. Пользователь отправляет Google Maps URL → СОХРАНИТЬ ОБЪЕКТ!
4. Сброс буфера, готовы к следующему объекту
```

**Пример:**
```
[Фото 1] → буфер: {photos: [1]}
[Фото 2] → буфер: {photos: [1,2]}
"2BR villa" → буфер: {photos: [1,2], text: "2BR villa"}
https://maps... → СОХРАНИТЬ! буфер очищен

[Фото 3] → буфер: {photos: [3]}
https://maps... → СОХРАНИТЬ! буфер очищен
```

**Бот отвечает после каждого сохранения:**
```
✅ Объект #1 сохранён!
📍 2BR Villa, Negombo

Отправь следующий объект или нажми [🏁 Завершить]
```

**Преимущества:**
✅ Полуавтоматическая группировка
✅ Google Maps URL = естественный маркер
✅ Меньше кликов чем в решении #2
✅ Интуитивно понятно

**Недостатки:**
❌ Требует Google Maps URL в каждом объекте
❌ Если URL забыли → объект "висит" в буфере

---

### РЕШЕНИЕ 5: Медиа-группы Telegram (для фото) + детекция

**Концепция:**
Telegram автоматически группирует фото отправленные вместе (media_group).

**Как работает:**
```
Telegram автоматически группирует:

Пользователь выбирает 3 фото → Отправить
    ↓
Telegram отправляет:
  message.media_group_id = "123456789"
  message[0]: фото 1 (media_group_id: 123456789)
  message[1]: фото 2 (media_group_id: 123456789)
  message[2]: фото 3 (media_group_id: 123456789)
```

**Логика бота:**
```javascript
const mediaGroups = {}; // хранилище групп

bot.on('message', (msg) => {
  if (msg.media_group_id) {
    // Это группа фото
    if (!mediaGroups[msg.media_group_id]) {
      mediaGroups[msg.media_group_id] = [];
    }
    mediaGroups[msg.media_group_id].push(msg.photo);
    
    // Ждём все фото группы (debounce 1 секунда)
    clearTimeout(groupTimers[msg.media_group_id]);
    groupTimers[msg.media_group_id] = setTimeout(() => {
      processMediaGroup(msg.media_group_id);
    }, 1000);
  }
});
```

**Комбинация с решением #4:**
```
1. Фото группа → сохраняем в буфер
2. Текст → добавляем в буфер
3. Google Maps URL → СОХРАНИТЬ объект
```

**Преимущества:**
✅ Автоматическая группировка фото
✅ Используем встроенную функцию Telegram
✅ Меньше путаницы с фото

---

## 🎯 РЕКОМЕНДУЕМОЕ РЕШЕНИЕ

### КОМБИНАЦИЯ: Решение #2 (Кнопки) + Решение #5 (Media Groups)

**Почему:**
- ✅ Удобно для пользователя (кнопки)
- ✅ Понятный процесс
- ✅ Автоматическая группировка фото
- ✅ Визуальная обратная связь
- ✅ Можно добавить 10+ объектов за раз

**Как это работает:**

```
┌─────────────────────────────────────────┐
│  Арендатор нажимает "📤 Добавить"       │
└─────────────────────────────────────────┘
                 ↓
┌─────────────────────────────────────────┐
│  Бот: "📤 Объект #1"                    │
│  [✅ Сохранить]  [➕ Ещё]  [❌ Отмена]  │
└─────────────────────────────────────────┘
                 ↓
┌─────────────────────────────────────────┐
│  Арендатор:                             │
│    - Выбирает 3 фото → Отправить        │
│      (Telegram группирует автоматически)│
│    - Пишет текст                        │
│    - Отправляет Google Maps             │
└─────────────────────────────────────────┘
                 ↓
┌─────────────────────────────────────────┐
│  Бот накапливает в сессии:              │
│    object#1: {                          │
│      photos: [1,2,3],                   │
│      text: "2BR villa",                 │
│      googleMaps: "..."                  │
│    }                                    │
└─────────────────────────────────────────┘
                 ↓
         Нажимает кнопку?
                 ↓
        ┌────────┴────────┐
        │                 │
    [➕ Ещё]        [✅ Сохранить]
        │                 │
        ↓                 ↓
┌───────────────┐   ┌──────────────┐
│ Бот: "Объект  │   │ Бот сохраняет│
│ #2"           │   │ все объекты  │
│ Отправь данные│   │ в БД         │
└───────────────┘   └──────────────┘
        │                 │
        ↓                 ↓
   (повтор цикла)    ✅ Завершено!
```

---

## 💻 РЕАЛИЗАЦИЯ (Псевдокод)

```javascript
const userSessions = {}; // хранилище сессий пользователей

// Структура сессии
class AddingSession {
  constructor(userId) {
    this.userId = userId;
    this.objects = [];
    this.currentObject = {
      id: 1,
      photos: [],
      text: '',
      googleMapsUrl: null
    };
  }
  
  addPhoto(photoUrl) {
    this.currentObject.photos.push(photoUrl);
  }
  
  addText(text) {
    this.currentObject.text += text + '\n';
  }
  
  addGoogleMapsUrl(url) {
    this.currentObject.googleMapsUrl = url;
  }
  
  startNextObject() {
    // Сохраняем текущий объект
    this.objects.push({ ...this.currentObject });
    
    // Создаём новый
    this.currentObject = {
      id: this.objects.length + 1,
      photos: [],
      text: '',
      googleMapsUrl: null
    };
  }
  
  getAllObjects() {
    // Добавляем текущий объект если есть данные
    if (this.currentObject.photos.length > 0 || this.currentObject.text) {
      this.objects.push({ ...this.currentObject });
    }
    return this.objects;
  }
}

// Кнопка "Добавить объект"
bot.on('callback_query', async (query) => {
  if (query.data === 'add_property') {
    const userId = query.from.id;
    const chatId = query.message.chat.id;
    
    // Создаём сессию
    userSessions[userId] = new AddingSession(userId);
    
    await bot.sendMessage(chatId,
      '📤 Добавление объекта #1\n\n' +
      'Отправь:\n' +
      '• Фото (можно несколько)\n' +
      '• Google Maps ссылку\n' +
      '• Описание',
      {
        reply_markup: {
          inline_keyboard: [[
            { text: '✅ Сохранить', callback_data: 'save_objects' },
            { text: '➕ Ещё объект', callback_data: 'next_object' },
            { text: '❌ Отмена', callback_data: 'cancel_adding' }
          ]]
        }
      }
    );
  }
  
  if (query.data === 'next_object') {
    const userId = query.from.id;
    const session = userSessions[userId];
    
    if (session) {
      session.startNextObject();
      
      await bot.sendMessage(query.message.chat.id,
        `📤 Добавление объекта #${session.currentObject.id}\n\n` +
        'Отправь данные для следующего объекта.',
        {
          reply_markup: {
            inline_keyboard: [[
              { text: '✅ Сохранить всё', callback_data: 'save_objects' },
              { text: '➕ Ещё объект', callback_data: 'next_object' },
              { text: '❌ Отмена', callback_data: 'cancel_adding' }
            ]]
          }
        }
      );
    }
  }
  
  if (query.data === 'save_objects') {
    const userId = query.from.id;
    const session = userSessions[userId];
    
    if (session) {
      const objects = session.getAllObjects();
      
      // Сохраняем все объекты
      let saved = 0;
      for (const obj of objects) {
        const result = await savePropertyToAPI(userId, obj);
        if (result.success) saved++;
      }
      
      await bot.sendMessage(query.message.chat.id,
        `✅ Сохранено объектов: ${saved}\n\n` +
        objects.map((o, i) => 
          `${i+1}. ${o.text.substring(0, 30)}...`
        ).join('\n'),
        {
          reply_markup: {
            inline_keyboard: [[
              { text: '🗺️ Открыть карту', url: getMapUrl(userId) }
            ]]
          }
        }
      );
      
      // Очистка сессии
      delete userSessions[userId];
    }
  }
});

// Обработка сообщений
bot.on('message', async (msg) => {
  const userId = msg.from.id;
  const session = userSessions[userId];
  
  // Если есть активная сессия добавления
  if (session) {
    // Фото
    if (msg.photo) {
      const photoId = msg.photo[msg.photo.length - 1].file_id;
      const file = await bot.getFile(photoId);
      const photoUrl = getPhotoUrl(file);
      
      session.addPhoto(photoUrl);
      
      await bot.sendMessage(msg.chat.id,
        `✅ Фото добавлено к объекту #${session.currentObject.id}\n` +
        `📷 Всего фото: ${session.currentObject.photos.length}`
      );
    }
    
    // Текст или Google Maps
    if (msg.text) {
      const googleMapsUrl = extractGoogleMapsUrl(msg.text);
      
      if (googleMapsUrl) {
        session.addGoogleMapsUrl(googleMapsUrl);
        await bot.sendMessage(msg.chat.id,
          `✅ Google Maps добавлен к объекту #${session.currentObject.id}`
        );
      } else {
        session.addText(msg.text);
        await bot.sendMessage(msg.chat.id,
          `✅ Описание добавлено к объекту #${session.currentObject.id}`
        );
      }
    }
    
    // Показываем статус
    const current = session.currentObject;
    const hasPhotos = current.photos.length > 0;
    const hasText = current.text.length > 0;
    const hasUrl = current.googleMapsUrl !== null;
    
    const status = 
      `📊 Статус объекта #${current.id}:\n` +
      `${hasPhotos ? '✅' : '⬜'} Фото (${current.photos.length})\n` +
      `${hasUrl ? '✅' : '⬜'} Google Maps\n` +
      `${hasText ? '✅' : '⬜'} Описание`;
    
    await bot.sendMessage(msg.chat.id, status);
  }
});
```

---

## 📊 СРАВНЕНИЕ РЕШЕНИЙ

| Решение | Удобство | Надёжность | Сложность | Рекомендация |
|---------|----------|------------|-----------|--------------|
| #1 Команды `/new` | ⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐ | Для опытных пользователей |
| #2 Кнопки | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | **РЕКОМЕНДУЮ** ✅ |
| #3 Авто-группировка | ⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐ | Ненадёжно |
| #4 Google Maps маркер | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐ | Хорошо но зависит от URL |
| #5 Media Groups | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐ | Дополнение к #2 |

---

## ✅ ФИНАЛЬНАЯ РЕКОМЕНДАЦИЯ

### Использовать: **Решение #2 (Кнопки) + Решение #5 (Media Groups)**

**Преимущества:**
- ✅ Интуитивный UI
- ✅ Чёткое разделение объектов
- ✅ Автоматическая группировка фото
- ✅ Визуальная обратная связь (статус объекта)
- ✅ Можно добавлять много объектов за раз
- ✅ Невозможно случайно объединить разные объекты

**User Experience:**
```
Простой flow:
1. Нажал "📤 Добавить"
2. Отправил фото + текст + ссылку
3. Нажал "➕ Ещё" (или "✅ Сохранить")
4. Повторил для следующего объекта
5. Нажал "✅ Сохранить всё"
6. Готово! 🎉
```

---

**Что думаете об этом решении?** 🤔
